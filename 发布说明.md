# 段言积木组合平台 v1.0 发布说明

> 发布时间：2026-08-10 ｜ 协议版本：1.0.0（`协议.md`）｜ 包：`duan-blocks 1.0.1`（演示海报/视频重发）

## 一句话

把「用户自然语言需求」映射为「预置积木的组合装配 + 契约级接线 + 内联生成 + 自动运行」，
**常态零 AI token、全离线**；能力缺失时由本地规则或 LLM 兜底生成新积木并**永久沉淀**，
下次零 token 复用。

## 演示素材

![duan-blocks 概念海报](https://raw.githubusercontent.com/skywalk163/duan/main/blocks_pkg/duan-blocks-poster.png)

- 🎬 概念演示视频（10s，1080P）：[`demo_video/Cinematic_product_launch_video_2026-08-10T13-02-22.mp4`](https://raw.githubusercontent.com/skywalk163/duan/main/demo_video/Cinematic_product_launch_video_2026-08-10T13-02-22.mp4)
- 🖥️ 本地交互演示页：`uvicorn duan_blocks.api:app --port 8123` → `http://localhost:8123/`

## 关键指标（150 块规模）

| 指标 | 数值 |
|---|---|
| 积木数 | **150 块**（数据/文本/中文/财务/工具/网络/生成/日期/校验/格式/集合/几何） |
| 真实语料留出段 Hit@1 | concept **1.0** / hybrid **1.0**（80 条口语化，40 条留出） |
| 主基准（89 条带校验） | Hit@1=MRR=链路正确率=**1.0**，FN/FP=0/0 |
| 冒烟 | **150/150** 块真跑通过 |
| 选块延迟 | concept **0.41ms**/条（吞吐 2440/s）、hybrid **0.63ms**/条（1578/s） |
| 计划缓存 | 命中即 5ms 复用整套方案 |

## 快速上手

> ✅ **已发布到 PyPI**（2026-08-10）：`duan-blocks 1.0.1` — https://pypi.org/project/duan-blocks/

```bash
# 1) 安装（运行时 + 积木平台）
pip install duan            # 段言语言
pip install duan-blocks     # 积木组合平台
pip install duan-blocks[zh] # + 拼音/繁简等中文能力（可选）

# 2) 命令行：一行需求出结果（零 token）
duan-combo "对一批数字求和再算平均" --输入 "[1,2,3,4,5]"
duan-combo "把这段中文转成拼音" --输入 '"你好"' --json

# 3) Web 服务：交互式演示页 + JSON API
uvicorn duan_blocks.api:app --port 8123
#   GET  /       演示页
#   POST /combo  {"requirement":"半径5的圆有多大","input":"5"}
```

`duan-combo` 定位积木库顺序：`DUAN_BLOCKS_LIB` 环境变量 → 包内数据（wheel 自带）→ 仓库内。

## 五段式数据流

```
用户需求 → [选块] 概念图+TF-IDF 混合（零 token）
        → [校验] 本地概念交集（第二道闸）
        → [接线] 契约级类型闸门（列表[数] vs 列表[文本]）
        → [粘合] 内联为单一 .duan（成语式宏展开）
        → [运行] cli/duan.py run（rc==0 且非空输出才算成功，失败自动换候选/兜底）
```

## 兜底沉淀闭环（v1.0 演示）

```
"把5转成二进制"（库外）
  → 无候选 → 本地规则生成「二进制」积木 → 注册入库
  → 运行输出 [1, 0, 1] ✓
  → 概念补齐后同需求第二次直接命中（零 token 复用）
```

## 版本里程碑

- v0.14 契约级接线 → v0.17 评测驱动（链路正确率 0.387→0.984）
- v0.21 混合选块 + 扰动集 → v0.22 自动别名 + 泛化 0.70→1.0
- v0.23 执行闭环 → v0.24 库外 5 项清零 → v1.0 协议定稿/失败可诊断/pip 化
- v0.25 扩库 150 + 真实语料（留出段 0.1→1.0）
- 3.1 Web/API + wheel 打包 + 压测（hybrid 补召回 190x 提速）
- 3.2 兜底生成端到端闭环（本地规则）

## 已知边界（诚实声明）

- 拼音/繁简/历法依赖可选第三方包（pypinyin/opencc/lunardate），缺失时对应块运行期报错；
- 真实 LLM 兜底需配置 `llm_config.json` 的 api_key（未配时本地规则覆盖 7 类常见库外意图）；
- 真·句向量仅 `DUAN_EMBED_REAL=1` 实验启用（Windows 批量不稳定）。
