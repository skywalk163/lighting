# 积木：CSV解析（格式领域 L0）
# 契约：输入 文本, 文本 → 输出 列表[列表[文本]]
导出 CSV解析
段落 CSV解析 接收 文入, 隔符：
    设 行表 为 分割字符串(文入, "\n")
    设 结表 为 []
    设 位次 为 0
    当 位次 小于 长度(行表)：
        设 行文 为 去除空白(行表[位次])
        如果 字符串长度(行文) 大于 0：
            结表.追加(分割字符串(行文, 隔符))
        设 位次 为 位次 加 1
    返回 结表
