# 积木：键值解析（格式领域 L0）
# 契约：输入 文本 → 输出 字典（把 a=1;b=2 这样的串解析成键值表）
导出 键值解析
段落 键值解析 接收 文入：
    设 词典 为 字典创建()
    设 段表 为 分割字符串(文入, ";")
    设 位次 为 0
    当 位次 小于 长度(段表)：
        设 片文 为 去除空白(段表[位次])
        如果 字符串包含(片文, "=")：
            设 对表 为 分割字符串(片文, "=")
            字典设置(词典, 去除空白(对表[0]), 去除空白(对表[1]))
        设 位次 为 位次 加 1
    返回 词典
