# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：格式 · 名称：聚合_格式_文本_2项
# 描述：自动织成：对文本输入并行执行 JSON解析、键值解析，返回结果列表

# ── 积木：JSON解析（格式）──
段落 JSON解析 接收 文入：
    返回 解析JSON(文入)

# ── 积木：键值解析（格式）──
段落 键值解析 接收 文入：
    设 词典 为 字典创建()
    设 段表 为 分割字符串(文入, ";")
    设 位次 为 0
    当 位次 小于 长度(段表)：
        设 片文 为 去除空白(段表[位次])
        如果 字符串包含(片文, "=")：
            设 对表 为 分割字符串(片文, "=")
            字典设置(词典, 去除空白(对表[0]), 去除空白(对表[1]))
        设 位次 为 位次 加 1
    返回 词典

段落 聚合_格式_文本_2项 接收 文本：
    设 赵果1 为 JSON解析(文本)。
    设 赵果2 为 键值解析(文本)。
    返回 [赵果1, 赵果2]。

导出 聚合_格式_文本_2项
