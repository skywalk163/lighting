# 积木：成对取小（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对取小）
导出 对最小
段落 对最小 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(如果 表甲[i] 小于 表乙[i] 则 表甲[i] 否则 表乙[i])
        设 i 为 i 加 1
    返回 结果
