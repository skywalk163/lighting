# 积木：滑动_最小_2（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（2期滑动取最小）
导出 滑小2
段落 滑小2 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 列表[i加1] 则 列表[i] 否则 列表[i加1])
        设 i 为 i 加 1
    返回 结果
