# 积木：滑动_均值_3_加权（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（3期加权滑动均值）
导出 滑均3加权
段落 滑均3加权 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加((列表[i] 乘 1 加 列表[i加1] 乘 2 加 列表[i加2] 乘 1) 除 4)
        设 i 为 i 加 1
    返回 结果
