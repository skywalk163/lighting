# 积木：滑动最小（数组领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（对列表做滑动最小）
导出 滑动最小
段落 滑动最小 接收 列表, 窗宽：
    设 结果 为 []
    设 i 为 0
    当 i 加 窗宽 小于 等于 长度(列表)：
        设 窗口 为 []
        设 j 为 0
        当 j 小于 窗宽：
            窗口.追加(列表[i 加 j])
            设 j 为 j 加 1
        结果.追加(0)
        设 i 为 i 加 1
    返回 结果
