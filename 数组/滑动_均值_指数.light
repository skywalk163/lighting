# 积木：滑动_均值_指数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（指数滑动均值）
导出 滑均指数
段落 滑均指数 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(列表[i] 乘 0.5 加 列表[i加1] 乘 0.3 加 列表[i加2] 乘 0.2)
        设 i 为 i 加 1
    返回 结果
