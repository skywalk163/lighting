"""段言积木组合库（积木库）。

把「用户自然语言需求」映射为「一组积木的选择 + 契约级参数接线」，由粘合器内联合成为
可运行的段言 .duan 源码，最后用 cli/duan.py run 执行。常态零 AI token；仅在「选不准」时
调用 LLM 兜底生成新积木并沉淀为永久积木。

契约见《段言积木组合协议 v1.0》（积木库/协议.md）。本库随段言主包一同分发（pyproject.toml
的 packages.find 已含 `积木库*`）；库内模块仍可用「把 积木库/ 加入 sys.path 后裸 import」
的既有方式运行，亦可作 `积木库.xxx` 子包导入。
"""
__version__ = "1.2.0"
