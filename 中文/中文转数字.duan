# 积木：中文转数字（中文领域 L0）
# 契约：输入 文本 → 输出 数（中文读法转整数，如 一百二十三 → 123）
# 与「数字转中文」互逆；支持 十/百/千/万/亿 与省略首位的「十三」
导出 读数
段落 读数 接收 文本：
    设 数字表 为 ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    设 总计 为 0
    设 级值 为 0
    设 当前 为 0
    设 负号 为 0
    设 i 为 0
    当 i 小于 长度(文本)：
        设 字 为 文本[i]
        如果 字 等于 "负"：
            设 负号 为 1
        否则：
            设 数位 为 0 减 1
            设 k 为 0
            当 k 小于 10：
                如果 字 等于 数字表[k]：
                    设 数位 为 k
                设 k 为 k 加 1
            如果 数位 大于等于 0：
                设 当前 为 数位
            否则如果 字 等于 "十"：
                如果 当前 等于 0：
                    设 当前 为 1
                设 级值 为 级值 加 (当前 乘 10)
                设 当前 为 0
            否则如果 字 等于 "百"：
                设 级值 为 级值 加 (当前 乘 100)
                设 当前 为 0
            否则如果 字 等于 "千"：
                设 级值 为 级值 加 (当前 乘 1000)
                设 当前 为 0
            否则如果 字 等于 "万"：
                设 总计 为 总计 加 ((级值 加 当前) 乘 10000)
                设 级值 为 0
                设 当前 为 0
            否则如果 字 等于 "亿"：
                设 总计 为 (总计 加 级值 加 当前) 乘 100000000
                设 级值 为 0
                设 当前 为 0
        设 i 为 i 加 1
    设 结果 为 总计 加 级值 加 当前
    如果 负号 等于 1：
        设 结果 为 0 减 结果
    返回 结果
