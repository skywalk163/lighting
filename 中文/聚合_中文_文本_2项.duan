# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：中文 · 名称：聚合_中文_文本_2项
# 描述：自动织成：对文本输入并行执行 中文转数字、汉字计数，返回结果列表

# ── 积木：中文转数字（中文）──
段落 读数 接收 文本：
    设 数字表 为 ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    设 总计 为 0
    设 级值 为 0
    设 当前 为 0
    设 负号 为 0
    设 i 为 0
    当 i 小于 长度(文本)：
        设 字 为 文本[i]
        如果 字 等于 "负"：
            设 负号 为 1
        否则：
            设 数位 为 0 减 1
            设 k 为 0
            当 k 小于 10：
                如果 字 等于 数字表[k]：
                    设 数位 为 k
                设 k 为 k 加 1
            如果 数位 大于等于 0：
                设 当前 为 数位
            否则如果 字 等于 "十"：
                如果 当前 等于 0：
                    设 当前 为 1
                设 级值 为 级值 加 (当前 乘 10)
                设 当前 为 0
            否则如果 字 等于 "百"：
                设 级值 为 级值 加 (当前 乘 100)
                设 当前 为 0
            否则如果 字 等于 "千"：
                设 级值 为 级值 加 (当前 乘 1000)
                设 当前 为 0
            否则如果 字 等于 "万"：
                设 总计 为 总计 加 ((级值 加 当前) 乘 10000)
                设 级值 为 0
                设 当前 为 0
            否则如果 字 等于 "亿"：
                设 总计 为 (总计 加 级值 加 当前) 乘 100000000
                设 级值 为 0
                设 当前 为 0
        设 i 为 i 加 1
    设 结果 为 总计 加 级值 加 当前
    如果 负号 等于 1：
        设 结果 为 0 减 结果
    返回 结果

# ── 积木：汉字计数（中文）──
段落 汉字计数 接收 串入：
    设 洋字 为 "0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ .,:;!?-_()"
    设 计数 为 0
    设 位次 为 0
    当 位次 小于 字符串长度(串入)：
        设 本字 为 字符串获取(串入, 位次)
        如果 字符串包含(洋字, 本字)：
            设 位次 为 位次 加 1
        否则：
            设 计数 为 计数 加 1
            设 位次 为 位次 加 1
    返回 计数

段落 聚合_中文_文本_2项 接收 文本：
    设 赵果1 为 读数(文本)。
    设 赵果2 为 汉字计数(文本)。
    返回 [赵果1, 赵果2]。

导出 聚合_中文_文本_2项
