# 积木：数字转中文（中文领域 L0）
# 契约：输入 数(整数) → 输出 文本（中文读法，如 123 → 一百二十三）
# 支持到万亿级；负数前缀「负」；10~19 按口语省略首位「一」
导出 念数
段落 念数 接收 数值：
    设 数字表 为 ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    设 权表 为 ["", "十", "百", "千"]
    设 级表 为 ["", "万", "亿", "万亿"]
    设 整值 为 整数(数值)
    如果 整值 等于 0：
        返回 "零"
    设 负号 为 0
    如果 整值 小于 0：
        设 负号 为 1
        设 整值 为 0 减 整值
    设 原值 为 整值
    设 结果 为 ""
    设 级号 为 0
    当 整值 大于 0：
        设 本级 为 整值 模 10000
        设 整值 为 整值 整除 10000
        如果 本级 大于 0：
            设 段文 为 ""
            设 权号 为 0
            设 零标 为 0
            设 余量 为 本级
            当 余量 大于 0：
                设 当位 为 余量 模 10
                如果 当位 等于 0：
                    如果 长度(段文) 大于 0：
                        设 零标 为 1
                否则：
                    设 片段 为 数字表[当位] 加 权表[权号]
                    如果 零标 等于 1：
                        设 片段 为 片段 加 "零"
                        设 零标 为 0
                    设 段文 为 片段 加 段文
                设 余量 为 余量 整除 10
                设 权号 为 权号 加 1
            如果 整值 大于 0 且 本级 小于 1000：
                设 段文 为 "零" 加 段文
            设 结果 为 段文 加 级表[级号] 加 结果
        设 级号 为 级号 加 1
    如果 原值 大于等于 10 且 原值 小于 20：
        设 结果 为 截取(结果, 1, 长度(结果))
    如果 负号 等于 1：
        设 结果 为 "负" 加 结果
    返回 结果
