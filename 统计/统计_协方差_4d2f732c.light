# 积木：统计_协方差（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_协方差）
导出 协方差
段落 协方差 接收 列表：
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 结果 为 结果 加 (列表[i] 减 均值) 乘 (列表2[i] 减 均值2)
        设 i 为 i 加 1
    返回 结果
