# 积木：邮箱校验（校验领域 L0）
# 契约：输入 地址(文本) → 输出 逻辑（邮箱地址格式是否合法）
导出 邮箱验
段落 邮箱验 接收 地址：
    设 净 为 地址.去除空白()
    如果 非 (净.包含("@"))：
        返回 假
    设 段表 为 净.分割("@")
    如果 非 (长度(段表) 等于 2)：
        返回 假
    设 户名 为 段表[0]
    设 域名 为 段表[1]
    如果 户名.长度() 小于 1：
        返回 假
    如果 非 (域名.包含("."))：
        返回 假
    如果 域名.开头(".")：
        返回 假
    如果 域名.结尾(".")：
        返回 假
    如果 净.包含(" ")：
        返回 假
    返回 真
