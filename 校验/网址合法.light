# 积木：网址合法（校验领域 L0）
# 契约：输入 文本 → 输出 逻辑（检查 URL 格式是否合法）
导出 网址合法
段落 网址合法 接收 串入：
    设 净文 为 去除空白(串入)
    如果 字符串包含(净文, " ")：
        返回 假
    如果 开头(净文, "http://")：
        返回 字符串包含(净文, ".")
    如果 开头(净文, "https://")：
        返回 字符串包含(净文, ".")
    返回 假
