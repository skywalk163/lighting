# 积木：身份证（校验领域 L0）
# 契约：输入 号码(文本) → 输出 逻辑（18 位身份证 ISO 7064 校验位是否正确）
导出 身份证
段落 身份证 接收 号码：
    设 串号 为 号码.去除空白().转大写()
    如果 非 (串号.长度() 等于 18)：
        返回 假
    设 权表 为 [7, 9, 10, 5, 8, 4, 2, 1, 6, 3, 7, 9, 10, 5, 8, 4, 2]
    设 校表 为 "10X98765432"
    设 数集 为 "0123456789"
    设 累计 为 0
    设 i 为 0
    当 i 小于 17：
        设 单字 为 串号[i]
        如果 非 (数集.包含(单字))：
            返回 假
        设 单项 为 整数(单字) 乘 权表[i]
        设 累计 为 累计 加 单项
        设 i 为 i 加 1
    设 余数 为 累计 模 11
    设 期望 为 校表[余数]
    如果 串号[17] 等于 期望：
        返回 真
    返回 假
