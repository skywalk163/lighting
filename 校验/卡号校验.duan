# 积木：卡号校验（校验领域 L0）
# 契约：输入 卡号(文本) → 输出 逻辑（Luhn 算法校验银行卡号是否合法）
导出 卡校验
段落 卡校验 接收 卡号：
    设 净 为 卡号.替换(" ", "").替换("-", "")
    设 数集 为 "0123456789"
    设 位数 为 净.长度()
    如果 位数 小于 12：
        返回 假
    设 总 为 0
    设 i 为 0
    当 i 小于 位数：
        设 字 为 净[位数 减 1 减 i]
        如果 非 (数集.包含(字))：
            返回 假
        设 值 为 整数(字)
        如果 i 模 2 等于 1：
            设 值 为 值 乘 2
            如果 值 大于 9：
                设 值 为 值 减 9
        设 总 为 总 加 值
        设 i 为 i 加 1
    如果 总 模 10 等于 0：
        返回 真
    返回 假
