# 积木：手机校验（校验领域 L0）
# 契约：输入 号码(文本) → 输出 逻辑（中国大陆 11 位手机号格式是否合法）
导出 手机验
段落 手机验 接收 号码：
    设 净 为 号码.替换(" ", "").替换("-", "")
    如果 非 (净.长度() 等于 11)：
        返回 假
    如果 非 (净.开头("1"))：
        返回 假
    设 数集 为 "0123456789"
    设 次位集 为 "3456789"
    如果 非 (次位集.包含(净[1]))：
        返回 假
    设 i 为 0
    当 i 小于 11：
        如果 非 (数集.包含(净[i]))：
            返回 假
        设 i 为 i 加 1
    返回 真
