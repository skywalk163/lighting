# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：校验 · 名称：聚合_校验_文本_6项
# 描述：自动织成：对文本输入并行执行 卡号校验、身份证、邮箱校验、手机校验、密码强度、纯数字，返回结果列表

# ── 积木：卡号校验（校验）──
段落 卡校验 接收 卡号：
    设 净 为 卡号.替换(" ", "").替换("-", "")
    设 数集 为 "0123456789"
    设 位数 为 净.长度()
    如果 位数 小于 12：
        返回 假
    设 总 为 0
    设 i 为 0
    当 i 小于 位数：
        设 字 为 净[位数 减 1 减 i]
        如果 非 (数集.包含(字))：
            返回 假
        设 值 为 整数(字)
        如果 i 模 2 等于 1：
            设 值 为 值 乘 2
            如果 值 大于 9：
                设 值 为 值 减 9
        设 总 为 总 加 值
        设 i 为 i 加 1
    如果 总 模 10 等于 0：
        返回 真
    返回 假

# ── 积木：身份证（校验）──
段落 身份证 接收 号码：
    设 串号 为 号码.去除空白().转大写()
    如果 非 (串号.长度() 等于 18)：
        返回 假
    设 权表 为 [7, 9, 10, 5, 8, 4, 2, 1, 6, 3, 7, 9, 10, 5, 8, 4, 2]
    设 校表 为 "10X98765432"
    设 数集 为 "0123456789"
    设 累计 为 0
    设 i 为 0
    当 i 小于 17：
        设 单字 为 串号[i]
        如果 非 (数集.包含(单字))：
            返回 假
        设 单项 为 整数(单字) 乘 权表[i]
        设 累计 为 累计 加 单项
        设 i 为 i 加 1
    设 余数 为 累计 模 11
    设 期望 为 校表[余数]
    如果 串号[17] 等于 期望：
        返回 真
    返回 假

# ── 积木：邮箱校验（校验）──
段落 邮箱验 接收 地址：
    设 净 为 地址.去除空白()
    如果 非 (净.包含("@"))：
        返回 假
    设 段表 为 净.分割("@")
    如果 非 (长度(段表) 等于 2)：
        返回 假
    设 户名 为 段表[0]
    设 域名 为 段表[1]
    如果 户名.长度() 小于 1：
        返回 假
    如果 非 (域名.包含("."))：
        返回 假
    如果 域名.开头(".")：
        返回 假
    如果 域名.结尾(".")：
        返回 假
    如果 净.包含(" ")：
        返回 假
    返回 真

# ── 积木：手机校验（校验）──
段落 手机验 接收 号码：
    设 净 为 号码.替换(" ", "").替换("-", "")
    如果 非 (净.长度() 等于 11)：
        返回 假
    如果 非 (净.开头("1"))：
        返回 假
    设 数集 为 "0123456789"
    设 次位集 为 "3456789"
    如果 非 (次位集.包含(净[1]))：
        返回 假
    设 i 为 0
    当 i 小于 11：
        如果 非 (数集.包含(净[i]))：
            返回 假
        设 i 为 i 加 1
    返回 真

# ── 积木：密码强度（校验）──
段落 密码强度 接收 密码：
    设 数集 为 "0123456789"
    设 小集 为 "abcdefghijklmnopqrstuvwxyz"
    设 大集 为 "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    设 有数 为 0
    设 有小 为 0
    设 有大 为 0
    设 有符 为 0
    设 i 为 0
    当 i 小于 密码.长度()：
        设 字 为 密码[i]
        如果 数集.包含(字)：
            设 有数 为 1
        否则如果 小集.包含(字)：
            设 有小 为 1
        否则如果 大集.包含(字)：
            设 有大 为 1
        否则：
            设 有符 为 1
        设 i 为 i 加 1
    设 分 为 有数 加 有小 加 有大 加 有符
    如果 密码.长度() 大于 7：
        设 分 为 分 加 1
    返回 分

# ── 积木：纯数字（校验）──
段落 纯数字 接收 内容：
    设 净 为 内容.去除空白()
    如果 净.长度() 等于 0：
        返回 假
    设 数集 为 "0123456789"
    设 i 为 0
    当 i 小于 净.长度()：
        如果 非 (数集.包含(净[i]))：
            返回 假
        设 i 为 i 加 1
    返回 真

段落 聚合_校验_文本_6项 接收 卡号：
    设 赵果1 为 卡校验(卡号)。
    设 赵果2 为 身份证(卡号)。
    设 赵果3 为 邮箱验(卡号)。
    设 赵果4 为 手机验(卡号)。
    设 赵果5 为 密码强度(卡号)。
    设 赵果6 为 纯数字(卡号)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4, 赵果5, 赵果6]。

导出 聚合_校验_文本_6项
