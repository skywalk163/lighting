# 积木：分页参数（网络领域 L0）
# 契约：输入 数(总数), 数(每页) → 输出 [数]（各页 offset 列表，offset/limit 分页）
导出 分页
段落 分页 接收 总数, 每页：
    设 结果 为 []
    设 i 为 0
    当 (i 乘 每页) 小于 总数：
        结果.追加(i 乘 每页)
        设 i 为 i 加 1
    返回 结果
