# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：网络 · 名称：聚合_网络_文本_2项
# 描述：自动织成：对文本输入并行执行 地址转数、域名提取，返回结果列表

# ── 积木：地址转数（网络）──
段落 地址转数 接收 串入：
    设 段表 为 分割字符串(去除空白(串入), ".")
    设 累计 为 0
    设 位次 为 0
    当 位次 小于 长度(段表)：
        设 累计 为 (累计 乘 256) 加 整数(段表[位次])
        设 位次 为 位次 加 1
    返回 累计

# ── 积木：域名提取（网络）──
段落 域名提取 接收 串入：
    设 净文 为 去除空白(串入)
    如果 字符串包含(净文, "://")：
        设 段表 为 分割字符串(净文, "://")
        设 净文 为 段表[1]
    如果 字符串包含(净文, "/")：
        设 片表 为 分割字符串(净文, "/")
        设 净文 为 片表[0]
    如果 字符串包含(净文, ":")：
        设 端表 为 分割字符串(净文, ":")
        设 净文 为 端表[0]
    返回 净文

段落 聚合_网络_文本_2项 接收 地址：
    设 赵果1 为 地址转数(地址)。
    设 赵果2 为 域名提取(地址)。
    返回 [赵果1, 赵果2]。

导出 聚合_网络_文本_2项
