# 积木：域名提取（网络领域 L0）
# 契约：输入 文本 → 输出 文本（从 URL 里取出主机名）
导出 域名提取
段落 域名提取 接收 串入：
    设 净文 为 去除空白(串入)
    如果 字符串包含(净文, "://")：
        设 段表 为 分割字符串(净文, "://")
        设 净文 为 段表[1]
    如果 字符串包含(净文, "/")：
        设 片表 为 分割字符串(净文, "/")
        设 净文 为 片表[0]
    如果 字符串包含(净文, ":")：
        设 端表 为 分割字符串(净文, ":")
        设 净文 为 端表[0]
    返回 净文
