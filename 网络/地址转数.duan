# 积木：地址转数（网络领域 L0）
# 契约：输入 文本 → 输出 数（把点分十进制 IPv4 转成整数）
导出 地址转数
段落 地址转数 接收 串入：
    设 段表 为 分割字符串(去除空白(串入), ".")
    设 累计 为 0
    设 位次 为 0
    当 位次 小于 长度(段表)：
        设 累计 为 (累计 乘 256) 加 整数(段表[位次])
        设 位次 为 位次 加 1
    返回 累计
