# 积木：HTTP状态码分类（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：HTTP状态码分类）
导出 状态码分类
段落 状态码分类 接收 输入：
    返回 如果 输入 小于 200 则 '信息' 否则 (如果 输入 小于 300 则 '成功' 否则 (如果 输入 小于 400 则 '重定向' 否则 (如果 输入 小于 500 则 '客户端错误' 否则 '服务端错误')))
