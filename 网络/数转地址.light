# 积木：数转地址（网络领域 L0）
# 契约：输入 数 → 输出 文本（把整数还原成点分十进制 IPv4）
导出 数转地址
段落 数转地址 接收 数入：
    设 余量 为 整数(数入)
    设 甲段 为 余量 模 256
    设 余量 为 整数(余量 除 256)
    设 乙段 为 余量 模 256
    设 余量 为 整数(余量 除 256)
    设 丙段 为 余量 模 256
    设 余量 为 整数(余量 除 256)
    设 丁段 为 余量 模 256
    返回 转字符串(丁段) 加 "." 加 转字符串(丙段) 加 "." 加 转字符串(乙段) 加 "." 加 转字符串(甲段)
