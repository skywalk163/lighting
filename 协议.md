# 《段言积木组合协议 v1.0》（定稿）

> **v1.0 定稿声明（2026-08-10）**：自本版起，下列接口冻结为稳定契约，v1.0 之后的改动
> 必须走「破坏性变更流程」（见下），不得静默修改：
>
> - **契约格式**（`索引.json` 块条目字段）：`名称/领域/层级/描述/输入/输出/稳定性/路径/导出名`
>   为必填，`样例/选块可见` 为可选（`选块可见` 缺省 true）。类型一律结构化写法
>   （`列表[数]`/`字典[文本,数]`/`可选[T]`/`联合[A,B]`），裸 `列表` 向后兼容但体检器报 W4。
> - **方案 JSON**（粘合器输入）：`需求/共享/步骤/打印` 四字段，共享项含 `名/值/类型`，
>   步骤含 `块/领域/导出名/路径/参数`，打印为结果变量列表（`赵果N` 约定）。
> - **五段式数据流**：选块 → 校验 → 契约级接线 → 内联粘合 → 运行（`cli/duan.py run`）。
> - **失败信号约定**：段言 src 后端静默吞运行期错误，程序成功的唯一可靠信号是
>   **`rc==0 且 有非空 stdout`**（解析错误才 rc≠0，崩溃必空输出）。
> - **两条铁律**：`段落` 名 == `导出` 名；导出名之间不得互为前缀（词法器最长匹配会切碎）。
>
> **版本兼容策略**：`索引.json` 顶层 `协议版本` 字段标明契约版本；块条目允许带
> `兼容` 数组声明可被哪些协议版本消费（缺省视为仅本版）。消费方（选块/粘合/校验）
> 遇不兼容条目应显式告警并跳过，不得静默退化。
>
> **破坏性变更流程**：① 改动者在本文件「变更记录」追加条目（影响面/理由/迁移路径）；
> ② 递增 `索引.json` 顶层 `协议版本`；③ 跑五把尺子（体检/冒烟/选块/接线/扰动）确认
> 无回归后方可合入；④ 旧条目按 `兼容` 声明标注迁移状态，不得直接删除。

把「用户自然语言需求」映射为「一组积木的选择 + 契约级参数接线」，再由粘合器内联合成为
可运行的段言 `.duan` 源码，最后用 `cli/duan.py run` 执行。**常态零 AI token**；仅在
「选不准」时调用 LLM 兜底生成新积木，且生成结果沉淀为永久积木，下次零 token 复用。

本方案移植自 jikuai 的《块选择协议》，关键差异见文末「与 jikuai 的差异」。

## v0.25 扩库 150 + 真实语料（第 2 月）

- 98 → **150 块**：新增 集合/几何/概率统计/金融进阶/Web/加解密/批量文件/日期高级 八个方向，大量直连 stdlib（`集合操作`/`数学`/`哈希`/`历法`/`字符串处理`/`文件系统`/`JSON`），纯段言算法块手写。
- 新增 `评估/真实语料.json`（80 条口语/场景化，调参/留出各 40）与 `评估/真实跑分.py`（按段统计）；`概念别名补充.json` 补约 200 条新领域触发词。
- 结果：**留出段 concept 1.0 / hybrid 1.0**；主基准 1.0、扰动 concept 1.0 / hybrid 0.975 零回归。
- **三条段言坑（扩库实测）**：①段落名遮蔽 import 同名符号 → 递归过深，导出名必须避开 stdlib 符号名（`交集`→`求交`、`MD5`→`取MD5`）；②`长/宽` 是保留字，不能作参数名；③新块描述沾旧概念触发词会与旧块余弦平局被索引序抢答（"三次方"→幂运算、"去重"→去重唯一），新块描述须避开旧概念触发词或配专属概念。

## 五段式数据流（v0.15）

```
用户需求（字符串）
      │
      ▼
[选块]  embedding选块.py（默认；概念图向量零依赖，装了 sentence_transformers 自动升级真·句向量更细召回）
        选块.py（--关键词，字符重叠）/ 语义选块.py（--语义，TF-IDF）
      │     库外概念 → 概念图余弦≈0 → 返回空候选 → 直接走兜底
      ▼
[校验]  校验器.py（运行前第二道闸，零 token 本地 / 配置 key 后 LLM）
      │     需求具体能力概念 与 所选块 无交集 → MISMATCH → 兜底
      ▼
[契约级接线]  接线.py：按「配置常量优先 > 类型匹配 > 参数名相似 > 最近上游」接线
      │            （不可接则触发兜底）
      ▼
[粘合]  粘合.py：把选中积木源码「内联」进单一 .duan（成语式宏展开）
      │
      ▼
可运行段言代码 → python cli/duan.py run 组合结果.duan
      │
      ▼ （仅「选不准」时）
[兜底]  兜底生成器.py：按契约生成新积木写入 生成/ 并注册进 索引.json
      │
      ▼ （可选 --自动层级）
[层级织]  层级生成.py：把 生成/ + 全库积木自动织成 L1+（成语式宏升级）
```

## v0.15 新增能力

### 1. embedding 概念图选块（解决词法盲区）
- 每个块与查询都映射到「受控概念空间」的稠密向量（概念词典见 `embedding选块.py`）。
- **关键约束**：刻意不把过宽通用字（数/列/值）映射到具体概念，避免「数」把数列需求
  误引到统计块。
- 若查询命中库内某概念 → 对应块高相似，正常选中；若命中「库内无对应块」的概念
  （如 斐波那契/阶乘/素数）→ 向量与所有块余弦≈0 → 返回**空候选** → 上层直接兜底。
- 可选升级：环境装了 `sentence_transformers` 时自动改用真·句向量（仍零 token）。
- **这从根本上修复了 v0.14 的盲区**：旧关键词选块曾因共享「数」字把「斐波那契」误中
  「统计双指标」(7.8 分)，根本不触发兜底；现在概念图直接判为空候选。

### 2. LLM / 本地校验器（第二道闸）
- 即使选块召回了块，仍可能语义错配。校验器在「运行前」做最后一次判定。
- **本地校验（零 token）**：用概念图看「需求具体能力概念」与「所选块具体能力概念」
  是否有交集；仅共享通用领域词（如「中文」）不算通过。无交集 ⇒ 误选 ⇒ MISMATCH。
  （例：`斐波那契` vs `统计双指标` → 具体概念 {数列生成} 无交集 → MISMATCH）
- **LLM 校验（有 key）**：把「需求 + 所选块名/描述 + 接线方案」发给大模型判定。
- 判定失败才触发兜底生成，保证零 token 为常态。

### 3. 生成块自动织入 L1+（层级生成 v0.15）
- `层级生成.py --自动`：扫描全库（含 `生成/`）自动织成 L1 积木，幂等注册进索引。
- 两类织法：①按输入类型聚类的「聚合」L1（同输入多块并行，返回列表）；
  ②策划的高价值组合（如 `描述统计` = 均值+最大+最小+计数+**方差**+**中位数**，
  `中文数字` = 人民币大写+数字转中文）。
- `组合.py --自动层级` 可在组合时顺带触发自动织。
- **已验证**：`描述统计([1..6]) = [3.5,6,1,6,2.9167,3.5]`，其中方差/中位数来自 `生成/`。

## v0.16 新增能力

### 1. 真·句向量检索（更细语义召回）
- `embedding选块.py` 接入 sentence_transformers：把「块名 + 领域 + 描述」与「查询」编码成句向量，
  按余弦召回。能捕捉「金额写大写 ↔ 转大写」这类字符不重叠但语义相近的细粒度关系，召回比
  概念图向量更准。
- 块向量首次编码后缓存到 `积木库/.embed_cache/`（按模型名索引），后续调用秒级；可通过
  `DUAN_EMBED_MODEL` 指定模型、`DUAN_EMBED_FLOOR` 调阈值、`embedding选块.py --real` 强制走真向量。
- 概念图向量仍为零依赖默认路径；真向量不可用或失败时自动降级，零 token 为常态。

### 2. 真实 LLM 校验器（细微错配拦截）
- 校验器系统提示内置 few-shot，专门覆盖「斐波那契 vs 统计」「中文转拼音 vs 数字转中文」
  等细微错配；传给 LLM 的候选已富化含输入/输出类型与描述，判定更精准。
- 本地校验器新增「拼音转换」概念：即使无 key，「中文转拼音」与「数字转中文」因具体能力概念
  无交集被判 MISMATCH，不再被共享的「中文」通用词误导。
- 提供 `校验器.py --dry-run` 展示将发给 LLM 的完整请求体（含已选块输入/输出类型），便于在
  不配置 key 时验证接线正确。

## v0.17 新增能力（评测驱动）

### 1. 基准套件：先造尺子，再谈优化
- `评估/基准.json`：62 条真实需求（47 库内标注期望块 + 15 库外标注应兜底），
  `评估/跑分.py` 产出 Hit@1 / Hit@3 / MRR / 库外召回 / 库内误兜底率 / 校验器 FP·FN / 链路正确率。
- 跑分**只判定不生成**（绝不调用 `注册()`），因此可随时回归而不污染 `索引.json`。

### 2. 概念拆分 + 最长别名优先匹配
- 粗概念（`数列生成` / `数值运算` / `列表集合` / `文本清洗` / `极值跨度`）拆成 40+ 细概念，
  每个概念对应一种**具体能力**而非一个领域。
- `概念向量()` 从「任意子串命中」升级为**最长别名优先**：`人民币大写` 压过 `大写`、
  `前缀和` 压过 `累加`，短别名不再把语义完全不同的需求引到错误概念。
- 权重分层：名称命中 1.0 > 描述命中 0.5 > 领域标签 0.25，通用领域概念整体降权 0.25，
  修掉「同域块被拉平成同分」的问题。
- **能力边界约束**：查询与块只共享通用领域概念（如都带「文本」）时相似度直接置 0 →
  空候选 → 正确兜底。库外概念（拼音/繁简/标准差/base64/UUID…）刻意注册但无对应块，
  让系统「知道自己不知道」。
- **效果**：链路正确率 0.387 → **0.984**，Hit@1 0.362 → **0.979**，库外召回 0.467 → **1.000**。

### 3. 真向量降级为实验选项（数据推翻了 v0.16 的默认）
同一把尺子下，text2vec 真·句向量 **0.823 / 3733ms**，反而不如概念图 **0.984 / 0.1ms**——
中文短查询与长块描述的句向量高度趋同，把「求和/最大/计数」冲散到统计块上。
v0.17 起默认走概念图，真向量需 `--real` 或 `DUAN_EMBED_REAL=1` 显式开启。

### 4. 类型闸门 + 纠正优先于生成
- **类型闸门**：非链式并行装配时，所有步骤共用同一输入，故过滤掉「签名接不上」的块
  （如列表输入下的 `保留分`），避免合成出 `留分([1,2,3])` 这类必然崩溃的代码。
- **纠正优先**：能力缺失预检发现本地规则块**已存在于索引**时，改为把它提到候选首位
  （纠正选块），而不是重复生成同名块污染索引。

## v0.18 新增能力（类型系统 v2）

### 1. 结构化类型：从「列表」到「列表[数]」
`类型.py` 提供一套可组合的类型表达式，取代 v0.17 的裸标签：

| 形态 | 例子 | 含义 |
|---|---|---|
| 基础 | `数` `文本` `逻辑` `任意` `空` | 标量 |
| 列表 | `列表[数]` `列表[文本]` `列表[任意]` | 元素类型协变 |
| 字典 | `字典[文本,数]` | 键/值各自匹配 |
| 可选 | `可选[数]` | 等价于 `联合[数,空]` |
| 联合 | `联合[数,文本]` | 归一化：展平嵌套、去重、含 `任意` 则退化 `任意` |

裸标签（`列表`）向后兼容，等价于 `列表[任意]`。全库 42 块已由
`评估/迁移_类型v2.py` 一次性迁移完毕（35 处标注升级，零漏标零非法）。

### 2. 匹配度取代布尔兼容
`类型.匹配度(need, have)` 返回 0.0~1.0 的实数，而不是「兼容/不兼容」：

| 情形 | 分值 |
|---|---|
| 精确同构（`列表[数]` ← `列表[数]`） | **1.00** |
| 联合分支命中（`联合[数,文本]` ← `数`） | **0.95** |
| 渐进匹配（任一侧含 `任意`，含裸 `列表`） | **0.60** |
| 不兼容（`列表[数]` ← `列表[文本]`） | **0.00** |

有了实数分值，接线器才能在「都能接」的多个候选里挑**最贴切**的那个，
而不是撞上第一个「不报错」的。

### 3. 接线器 v2：类型优先的排序键
排序键从 v0.17 的「常量 > 类型全等 > 名相似 > 最近上游」改为：

```
(类型匹配度↓, 本步未重复占用, 上游产物优先(组=0), 名相似度↓, 位置序)
```

- **共享输入按声明序**（组=1），**步骤产物按最近上游优先**（组=0）——修掉
  「多个共享输入时主数据被最后一个变量抢走」的老 bug。
- **本步已用集合**：同一步骤内不重复占用同一个变量。
- **回退标记**：某步接不上任何上游产物、只能退回共享输入时打 `_回退`，
  由 `回退步()` 暴露给 `组合.py` 打印告警——`--链式` 不再**静默**退化成并行。

### 4. 类型闸门升级
`组合._可单参调用` 从「输入类型字符串全等」改为 `匹配度(块入参, 共享类型) > 0`，
既拦得住 `列表[文本]` 喂给 `求和`，又不误伤 `列表[任意]` 这类渐进标注。

### 5. 契约体检器（第三把尺子）
`评估/体检.py` 守两条铁律：**段落名 == 导出名**、**导出名互不互为前缀**
（段言词法器全文件预扫描 + 最长匹配，前缀重叠会把标识符切碎）。
检查项：E1 名称重复 / E2 导出名重复 / E3 路径缺失 / E4 段落缺失 / E5 导出缺失 /
E6 类型非法 / E7 签名数不符，警告 W1 前缀风险 / W2 描述过短 / W4 裸类型。

> 首跑即抓到真雷：`均值` 的导出名竟是 `中位`，与 `中位数` 互为前缀，且两者同在
> `描述统计.duan` 内——靠最长匹配侥幸没炸。已改名 `中位` → `平均`。

### 6. 从属子句降权（句法特征）+ 校验器 R2
中文需求习惯把「怎么做」写在前、「要什么」写在后：

| 需求 | 前置步骤 | 目标 |
|---|---|---|
| 排序**后**处在中间位置的那个值 | 排序 | 中位数 |
| 求和**再**算平均 | 求和汇总 | 均值平均 |
| **先**排序**再**取最大的那个数 | 排序 | 最大值 |

v0.17 之前两层概念同权，`排序列表` 与 `中位数` 余弦完全打平（0.686/0.686），
top1 随索引顺序抖动——这就是基准里最后一条失败项 **D13**。

- **选块侧**：命中片段紧跟从属标记（后/之后/然后/再/完/以后）或被「先/首先」引导时，
  该概念权重 ×0.6。余弦对整体缩放不敏感，因此**单概念查询完全不受影响**，
  只有多概念查询才被重排——最小扰动。
- **校验器侧（R2 目标能力覆盖）**：把需求概念分成「目标」与「从属」两层，
  若所选块只盖住从属概念、目标层一个都没盖上 ⇒ 判定误选。
  即便选块偶然排错，这道闸也会指出「目标能力『中位数』无块覆盖」。

基准同步扩到 **70 条**（新增 S01–S08 句法系列），结果：
`Hit@1 0.9793 → 1.0000`、`链路正确率 0.9839 → 1.0000`。
新用例还顺带抓出两个真缺口（「还剩几个」未映射到计数、「拼起来」未映射到文本拼接），
已补进概念词典——**这正是基准的价值：新尺子刻度更细，就量得出旧问题**。

### 7. 两把尺子的分工
| 尺子 | 度量 | 对什么无感 |
|---|---|---|
| `评估/跑分.py`（基准.json 62 条） | **选块**：候选对不对 | 接线错误 |
| `评估/接线跑分.py`（接线基准.json 26 条） | **接线**：接对线了吗 | 选块错误 |
| `评估/体检.py` | **契约**：索引与源码是否自洽 | 语义质量 |

类型 v2 上线时选块指标 0.9839 → 0.9839（纹丝不动），收益**全部**落在接线：
`接线跑分.py` 1.0000 vs `--粗类型` 对照 0.7692，失败的 6 条全是会生成崩溃代码的真场景
（`切分文本` → `求和`/`均值`/`取偶数`、`中文数字` → `求和`、文本列表下 `求和`/`方差`/`中位数` 未被闸门滤掉）。

## 组合.py / 校验器.py 关键开关（v0.17+）
| 开关 | 作用 |
|---|---|
| `embedding选块.py --real` / `DUAN_EMBED_REAL=1` | 强制走真·句向量（默认已改为概念图） |
| `校验器.py --dry-run` | 仅展示将发给 LLM 的请求体，不实际调用 |
| `评估/跑分.py --策略 概念图\|embedding\|语义\|关键词` | 在基准上量化选块与兜底判定质量 |
| `评估/跑分.py --对比 <历史报告>` | 与历史报告逐指标对比，防止回退 |
| `评估/接线跑分.py` | 在接线基准上量化接线与类型闸门（26 条） |
| `评估/接线跑分.py --粗类型` | 把契约降级为 v0.17 裸类型，量化类型 v2 的净收益 |
| `评估/体检.py` | 契约体检：索引 ↔ 源码 一致性 + 前缀风险 |
| `评估/迁移_类型v2.py [--写入]` | 幂等地把索引里的裸类型升级为结构化类型 |

## 方案 JSON 结构（粘合器输入）
```json
{
  "需求": "对一批数字求和再算平均",
  "共享": [{"名": "赵料", "值": "[1,2,3,4,5]", "类型": "列表[数]"}],
  "步骤": [
    {"块": "求和", "领域": "数据", "导出名": "汇总", "路径": "数据/求和.duan", "参数": ["赵料"]},
    {"块": "均值", "领域": "数据", "导出名": "平均", "路径": "数据/均值.duan", "参数": ["赵料"]}
  ],
  "打印": ["赵果1", "赵果2"]
}
```

## 组合.py 关键开关
| 开关 | 作用 |
|---|---|
| （默认） | embedding 概念图选块 |
| `--关键词` | v0 字符重叠选块（配合校验器仍可拦下旧盲区误选） |
| `--语义` | TF-IDF + 同义词选块 |
| `--链式` | 契约级精确接线（前步结果自动喂后步） |
| `--无校验` | 跳过运行前校验器 |
| `--无兜底` | 关闭 LLM 兜底（选不准直接退出） |
| `--自动层级` | 把 生成/ 积木自动织成 L1+ |
| `--阈值` | 触发兜底的分数阈值 |

## 与 jikuai 的差异
- jikuai 块是扁平目录 + 顺次 glue；段言用**内联成语式宏**展开，单文件即跑。
- 选块：jikuai 用 embeddings 检索（bench_retrieval 在 vector_index=None 时退化为
  TF-IDF+同义词）；段言 v0.15 用**概念图向量**做零 token 召回 + **校验器**做第二道闸，
  比 jikuai 多一层「判定」。
- 层级：jikuai 有 blocks→pipelines→scenarios 三层目录；段言用 `层级生成.py` 把任意
  低级积木组合固化成高级积木并注册，可层层向上（L1→L2→…），且 `生成/` 块也参与编织。
- 兜底：jikuai 未内置；段言有 `兜底生成器.py`（LLM + 本地规则降级），生成块永久沉淀。

## v0.19 / v0.20 新增（第四把尺子 + 扩库到 92 块）

### 第四把尺子：冒烟（`评估/冒烟.py`）
前三把尺子（选块 / 接线 / 体检）都**不执行代码**，而段言真正的坑只在运行期暴露：
词法前缀吞并、关键字前缀、`己`=self。冒烟按 `样例`（或由类型推导实参）真跑每一块，
并比对 `期望` 值。第一次开跑就抓出 7 块「索引里契约完美、运行即崩」的假积木。

### 自动织的两道闸（v0.20）
1. **冒烟自检**：同输入类型 ≠ 同输入语义。`地址转数 + 域名提取` 都接文本，并联后
   无论喂什么都必崩一个；`JSON解析 + 键值解析` 同理。织完先真跑一次，跑不通就不注册。
2. **`选块可见` 透传**：自动聚合是组合脚手架，不是一项「能力」，标 `选块可见: False`
   让它退出候选池。`generate()` 早期会丢掉这个键，聚合块又回到候选里遮蔽精确块
   （跑分 C04 「人民币大写和中文读数」被 `聚合_中文_数_3项` 抢走就是这么退化的）。

### 语言层修复：词法器 `_` 扫描（v0.19）
`src/lexer.py` 的 `_scan_user_definitions` 收集用户定义名时字符集漏了 `_`，
`聚合_财务_数_2项` 只注册到 `聚合`，后续走坏路径把数字丢掉，解析报「期望冒号得到数字」。
四处循环补 `or source[k] == '_'` 后 6 个名字变体全通过，词法器测试 15 passed 零回归。

### 概念词典维护铁律
粗概念会让同类块的概念向量完全相同（余弦一律 1.0，选块器无从分辨）。
新增领域必须**拆到能力粒度**：日期拆成 闰年判定/月份天数/年内日序/儒略日/日期差值/
星期计算/日期格式/时间戳转换，校验拆成 卡号/身份证/邮箱/手机/密码强度/纯数字。
别名要**长到能吃掉相邻歧义词**：「把每个单词的首字母改成大写」若只注册 `首字母大写`，
最长匹配会让 `大写` 单独命中「大小写转换」，`转大写` 就以 0.97 : 0.44 抢走结果。

### 校验器 R3：目标概念全覆盖
R2 只查「交集非空」，「用正则表达式把邮箱地址提取出来」只匹配到邮箱块也会放行。
R3 要求需求里的目标概念**全部被覆盖**，否则判组合不完整 → 兜底。

## v0.21 混合选块与计划缓存

### 基准满分 ≠ 泛化：扰动集是照妖镜
基准 89 条 Hit@1=1.0 会让人误判系统已经很强。造 `评估/扰动集.json`（40 条**刻意避开块名
用字**的迂回说法，如「这组数据波动有多大」对应 `标准差`、「头一个字母变大写」对应 `词首大写`）
一测，概念图 Hit@1 只有 **0.375**，且 **18/40 完全空候选**。
结论：**瓶颈在召回不在排序**。基准里的说法早被写进概念词典，等于自己考自己。

### 两级选块（混合选块.py）
1. **主召回** 走概念图（受控概念空间、0.4ms、库外硬约束保兜底能力）。
2. **补召回**：主召回为空或 top1 弱于 `DUAN_HYBRID_FLOOR` 时，用 TF-IDF 字符 n-gram 补一批，
   打上 `补召回` 标记（分数做压制，不与概念分直接可比）。
3. **重排**：top1 与后续差距小于 `DUAN_HYBRID_DELTA`（默认 0.03）视为「并列群」，
   群内按 `ALPHA * 概念分 + (1-ALPHA) * 语义分` 重排；群外顺序不动。

代价是耗时 0.4ms → 3.6ms，仍远低于真向量的 3733ms。真向量在 Windows 批量调用下会硬崩
（单条正常），保持 `--real` 实验选项不作默认。

### 防自欺：调参段 / 留出段
扰动集 R01–R20 为**调参段**（允许照着它补概念词典别名），R21–R40 为**留出段**（只测不调）。
补完词典后：调参段 1.00（含过拟合，不可信），**留出段 hybrid 0.70 vs concept 0.50** —— 这才是
可以对外说的泛化数字。

### 计划缓存（计划缓存.py）
键 = `规范需求 + 库指纹 + 策略指纹`。
- **库指纹**覆盖每块的 名称/路径/导出名/输入类型/输出类型/选块可见。兜底生成了新块、
  或谁改了契约，指纹立刻变化 → 旧缓存**整体作废**，不会拿过期方案硬套。
- **输入类型**单独比对：接线是按类型规划的，`列表[数]` 的计划不能给 `文本` 用。
- 只缓存「计划」不缓存「结果」：换一组输入值照样命中，重新执行即可。
- 规划耗时 13.2ms → 2.5ms（**5.3x**）；真正的大头是配了 LLM key 时省下的校验/兜底调用。

**踩过的坑**：`阈值` 参数在选块后会被就地填成策略默认值（None → 0.06），
读缓存在填充前、写缓存在填充后，两头算出的键永远不同 —— 写得进去、读不出来，
表现为「条目数 1、累计命中 0」。修法是函数入口先存 `阈值原`，读写都用它。

## v0.22 / v0.23 新增（补泛化 + 执行闭环）

### 自动别名扩充：`auto_alias.py` + `概念别名补充.json`
v0.21 的扰动集留出段（R21–40）只有 **0.70**（concept 0.50），瓶颈在**召回**——用户换种说法（"11 位号码""把 IP 变成一个整数""连本带利"）就接不住，而库里**块其实都有**，只是概念词典没收录这些口语别名。
- 新增 `auto_alias.py`：从**块自身元数据**（名称 / 描述括号样例）榨同义说法，再叠加一份**口语同义词典**（连本带利 / 366 天 / 同时整除 / 跳过 / 精确到 / 变成对象 / IP 变成 / 11 位 / 数列 / 上下限 …），生成 `概念别名补充.json`（131 条别名 / 77 概念）。无 LLM key 也能跑——这正是本系统「零 token」哲学的体现。
- `embedding选块.py` 在 import 时合并 `概念别名补充.json` 进概念词典（还原基准词典再判定，避免重复生成把旧别名当已有过滤掉）。
- 修复 `auto_alias` 真 bug：块名兜底用 `_解概念()` 解析块名，把「取奇数」误归到「取偶数」污染别名表 → 查询「只保留奇数」接不到（U05）。修法：块名本身已是概念 key 时强制归该概念。

### 具体度决胜（平局重排，不拖累主基准）
召回空修好后，留出段 R34/R38 暴露**同义抢答**：「两个数最大能同时整除」被「最大」抢去 `最大值`、「第几页该跳过多少条」被「多少条」抢去 `计数长度`。在 `embedding_select` 加**复合意图门控 + 具体度决胜**：
- 复合意图门控：查询显式带「同时/都/两个/一并」且点名 ≥2 具体概念时，对只满足部分概念的块按**覆盖比例**降权（让 `统计双指标`=均值+极差 压过 `范围跨度`=极差），修复 D15；顺序型需求（算完 X 再看看 Y）不触发，避免误伤 S07。
- 具体度决胜：余弦**完全平局**时优先选「查询命中别名更长」的候选（「同时整除」4 字 > 「最大」2 字），只重排平局、不改全局排序，故不拖累主基准。

效果：扰动集留出段 **concept 0.70 → 1.0**（远超 0.90 目标），主基准（89 条带校验）Hit@1 0.9881（残余 D15 后由覆盖度门控补到 1.0）。

### 执行闭环（v0.23）：生成后自动运行、崩溃自动换候选
- **关键发现**：`cli/duan.py run` 的 src 后端会**静默吞掉运行期错误**——`a[99]` 越界、`1 除 0` 都返回 `rc=0` 且 stdout 为空（**仅解析错误才 rc≠0**）。因此「靠 rc 判成功」不可靠，成功信号改为 **`rc==0 且 有非空 stdout`**（正常方案必含打印步骤，崩溃才空输出）。
- `组合.py` 新增 `_运行_单次`（合成→运行→捕获输出，超时 60s/异常隔离）与 `_成功(rc,out)` 判定；`_cli` 运行段改为带重试闭环：主方案跑挂 → 自动换次优候选重跑 → 都挂触发兜底生成重跑；显式跟踪成功标志，避免崩溃 rc=0 误判成功绕过兜底。内联 `装配` 抽为模块级 `_装配` 供重试复用（同逻辑）。
- 双评测零回归：泛化 concept 1.0 / hybrid 0.975；主基准（89 条带校验）全 1.0、库外召回 1.0、误兜底 0、FN·FP 0/0。

## v0.24 库外清零（B 扩库第一步）

- 库外 5 项能力（拼音/繁简/正则/base64/文件IO）从「应兜底」转入库内：新块 转拼音/简转繁/繁转简/提取邮箱/Base64编码/读取CSV，92 → 98 块。
- 实现全部走段言自带 stdlib（`中文文本`/`中文NLP`/`正则表达式`/`编码`/`CSV读写器`）：正则/base64/文件IO 零第三方依赖（块内 `从《模块》导入《符号》` 直连 stdlib）；拼音/繁简用 pypinyin/opencc（可选依赖，无 opencc 时 `中文NLP` 自动降级内置映射表）。
- 概念词典无需改动：库外概念（拼音转换/繁简转换/正则/编解码/文件IO）本就注册，新块名称/描述命中触发词后块向量自动挂上概念，选块侧天然接通。
- 基准 89 条库外归零：Hit@1=MRR=链路正确率=1.0、FN/FP=0/0；扰动集留出段 concept 1.0 / hybrid 0.975 零回归；冒烟 98/98；接线 1.0。
- **语义选块修复**：默认不再无条件尝试真向量（venv 装了 sentence_transformers 后批量调用硬崩/卡死），仅 `DUAN_EMBED_REAL=1` 显式启用——与 embedding选块.py 约定对齐，混合选块二级补召回回归纯 TF-IDF。
- **冒烟修复**：成功判定对齐 v0.23（`rc==0 且 有非空输出`），运行期崩溃块不再被误判为通过。

## 诚实边界（v0.16）
- 「中文转拼音 vs 数字转中文」这类细微错配 **v0.16 已覆盖**：本地校验器靠新增的「拼音转换」
  概念离线拦截；配置 key 后 LLM 校验器的 few-shot 进一步兜底。仍建议生产环境配置真实 LLM。
- 真·句向量需 sentence_transformers（及模型权重，约数百 MB）；未安装时自动降级概念图向量，
  功能不缺失，仅召回粒度略粗。
- 概念词典需随积木库扩充而维护；新增领域应同步补概念触发词。
- 自动织的「聚合」L1 限制 2–6 项，避免过宽聚合；更大组合走策划配方。
