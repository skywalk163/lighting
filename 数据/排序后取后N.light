# 积木：排序后取后N（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（排序后取后N个）
导出 排序取后N
段落 排序取后N 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(列表.排序().截取(长度(列表) 减 甲, 长度(列表)))
        设 i 为 i 加 1
    返回 结果
