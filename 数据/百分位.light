# 积木：百分位（数据领域 L0）
# 契约：输入 列表[数], 数 → 输出 数（线性插值分位数，分位取 0~100）
导出 百分位
段落 百分位 接收 表入, 分位：
    设 总量 为 长度(表入)
    如果 总量 等于 0：
        返回 0
    设 序表 为 排序(表入)
    设 位址 为 (总量 减 1) 乘 分位 除 100
    设 低址 为 整数(位址)
    设 高址 为 低址 加 1
    如果 高址 大于 总量 减 1：
        返回 序表[总量 减 1]
    设 余量 为 位址 减 低址
    返回 序表[低址] 加 ((序表[高址] 减 序表[低址]) 乘 余量)
