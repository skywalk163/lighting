# 积木：求绝对值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求绝对值）
导出 绝对值
段落 绝对值 接收 列表：
    设 结果 为 列表
    设 i 为 0
    当 i 小于 长度(列表)：
        设 结果 为 结果 如果 列表[i] 大于 0 则 列表[i] 否则 (0 减 列表[i]) 列表[i]
        设 i 为 i 加 1
    返回 结果
