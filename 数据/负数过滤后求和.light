# 积木：负数过滤后求和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤负数后求和）
导出 负过和
段落 负过和 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 0 则 结果 加 列表[i] 否则 结果)
        设 i 为 i 加 1
    返回 结果
