# 积木：前缀和（数据领域 L0）
# 契约：输入 列表[数] → 输出 列表[数]（逐项累加序列）
导出 前缀和
段落 前缀和 接收 表入：
    设 结表 为 []
    设 累计 为 0
    设 位次 为 0
    当 位次 小于 长度(表入)：
        设 累计 为 累计 加 表入[位次]
        结表.追加(累计)
        设 位次 为 位次 加 1
    返回 结表
