# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：数据 · 名称：描述统计
# 描述：基础描述统计：对列表并行给出 均值/最大/最小/计数/方差/中位数

# ── 积木：均值（数据）──
段落 中位 接收 列表：
    设 合计 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 合计 为 合计 加 列表[i]
        设 i 为 i 加 1
    返回 合计 除 长度(列表)

# ── 积木：最大（数据）──
段落 极大 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

# ── 积木：最小（数据）──
段落 极小 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 小于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

# ── 积木：计数（数据）──
段落 计数 接收 列表：
    返回 长度(列表)

# ── 积木：方差（生成）──
段落 方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 计数)

# ── 积木：中位数（生成）──
段落 中位数 接收 序列：
    设 排序表 为 []
    设 i 为 0
    当 i 小于 长度(序列)：
        排序表.追加(序列[i])
        设 i 为 i 加 1
    排序表.排序()
    设 计数 为 长度(排序表)
    设 中 为 整数(计数 除 2)
    如果 计数 模 2 等于 0：
        返回 ((排序表[中 减 1] 加 排序表[中]) 除 2)
    否则：
        返回 排序表[中]

段落 描述统计 接收 序列：
    设 赵果1 为 中位(序列)。
    设 赵果2 为 极大(序列)。
    设 赵果3 为 极小(序列)。
    设 赵果4 为 计数(序列)。
    设 赵果5 为 方差(序列)。
    设 赵果6 为 中位数(序列)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4, 赵果5, 赵果6]。

导出 描述统计
