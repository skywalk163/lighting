# 积木：标准差（数据领域 L0）
# 契约：输入 列表[数] → 输出 数（总体标准差）
导出 标准差
段落 标准差 接收 表入：
    设 总量 为 长度(表入)
    如果 总量 等于 0：
        返回 0
    设 累计 为 0
    设 位次 为 0
    当 位次 小于 总量：
        设 累计 为 累计 加 表入[位次]
        设 位次 为 位次 加 1
    设 平数 为 累计 除 总量
    设 方累 为 0
    设 步次 为 0
    当 步次 小于 总量：
        设 差值 为 表入[步次] 减 平数
        设 方累 为 方累 加 (差值 乘 差值)
        设 步次 为 步次 加 1
    返回 四舍五入((方累 除 总量) 幂 0.5, 6)
