# 积木：加权平均（数据领域 L0）
# 契约：按权重列表计算加权平均数
导出 加权平均
段落 加权平均 接收 数值，权重：
    设 加权和 为 0
    设 权重和 为 0
    设 i 为 0
    当 i 小于 长度(数值)：
        设 加权和 为 加权和 加 数值[i] 乘 权重[i]
        设 权重和 为 权重和 加 权重[i]
        设 i 为 i 加 1
    返回 加权和 除 权重和
