# 积木：累积最大后差分（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（累积最大后差分）
导出 累积大差
段落 累积大差 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 大于 结果 则 列表[i] 减 结果 否则 0)
        设 i 为 i 加 1
    返回 结果
