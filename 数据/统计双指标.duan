# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：2 · 领域：数据 · 名称：统计双指标
# 描述：对数值列表同时给出算术均值与极差跨度，作为基础描述统计组合（L2 = L0 均值 + L1 范围跨度）

# ── 积木：均值（数据）──
段落 平均 接收 列表：
    设 合计 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 合计 为 合计 加 列表[i]
        设 i 为 i 加 1
    返回 合计 除 长度(列表)

# ── 积木：范围跨度（数据）──
段落 极大 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

段落 极小 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 小于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

段落 跨度 接收 序列：
    设 赵果1 为 极大(序列)。
    设 赵果2 为 极小(序列)。
    返回 赵果1 减 赵果2。

段落 双标 接收 序列：
    设 赵果1 为 平均(序列)。
    设 赵果2 为 跨度(序列)。
    返回 [赵果1, 赵果2]。

导出 双标
