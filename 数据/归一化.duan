# 积木：归一化（数据领域 L0）
# 契约：输入 列表[数] → 输出 列表[数]（线性缩放到 0~1）
导出 归一化
段落 归一化 接收 表入：
    设 总量 为 长度(表入)
    如果 总量 等于 0：
        返回 []
    设 顶值 为 表入[0]
    设 底值 为 表入[0]
    设 位次 为 1
    当 位次 小于 总量：
        如果 表入[位次] 大于 顶值：
            设 顶值 为 表入[位次]
        如果 表入[位次] 小于 底值：
            设 底值 为 表入[位次]
        设 位次 为 位次 加 1
    设 跨度 为 顶值 减 底值
    设 结表 为 []
    设 步次 为 0
    当 步次 小于 总量：
        如果 跨度 等于 0：
            结表.追加(0)
        否则：
            结表.追加(四舍五入((表入[步次] 减 底值) 除 跨度, 6))
        设 步次 为 步次 加 1
    返回 结表
