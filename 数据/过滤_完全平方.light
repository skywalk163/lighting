# 积木：过滤_完全平方（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤完全平方数）
导出 滤完全平方
段落 滤完全平方 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 整数(平方根(列表[i])) 乘 整数(平方根(列表[i])) 等于 列表[i]：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
