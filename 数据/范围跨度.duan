# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：数据 · 名称：范围跨度
# 描述：列表最大值减最小值，即数据的极差/跨度，衡量离散程度

# ── 积木：最大（数据）──
段落 极大 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

# ── 积木：最小（数据）──
段落 极小 接收 列表：
    设 当前 为 列表[0]
    设 i 为 1
    当 i 小于 长度(列表)：
        如果 列表[i] 小于 当前：
            设 当前 为 列表[i]
        设 i 为 i 加 1
    返回 当前

段落 跨度 接收 序列：
    # 取最大值
    设 赵果1 为 极大(序列)。
    # 取最小值
    设 赵果2 为 极小(序列)。
    返回 赵果1 减 赵果2。

导出 跨度
