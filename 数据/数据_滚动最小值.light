# 积木：数据_滚动最小值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滚动最小值）
导出 滚动最小值
段落 滚动最小值 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 列表[i加1] 则 (如果 列表[i] 小于 列表[i加2] 则 列表[i] 否则 列表[i加2]) 否则 (如果 列表[i加1] 小于 列表[i加2] 则 列表[i加1] 否则 列表[i加2]))
        设 i 为 i 加 1
    返回 结果
