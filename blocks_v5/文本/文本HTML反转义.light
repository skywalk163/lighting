# 积木：HTML反转义（文本领域，自动生成）
# 契约：输入 [文本] → 输出 文本（反转义HTML实体）
导出 文本HTML反转义
段落 文本HTML反转义 接收 输入：
    设 结果 为 ""
    设 i 为 0
    当 i 小于 长度(输入)：
        设 字符 为 输入[i]
        如果 字符 等于 "&"：
            设 结果 为 结果 加 "&"
        否则 如果 字符 等于 "<"：
            设 结果 为 结果 加 "<"
        否则 如果 字符 等于 ">"：
            设 结果 为 结果 加 ">"
        否则 如果 字符 等于 '"'：
            设 结果 为 结果 加 '"'
        否则：
            设 结果 为 结果 加 字符
        设 i 为 i 加 1
    返回 结果