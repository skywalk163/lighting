# 积木：最大值（计算机领域，自动生成）
# 契约：输入 [列表] → 输出 数（列表最大值）
导出 最大计算机
段落 最大计算机 接收 列表:
    设 结果 为 列表[0]
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 大于 结果:
            设 结果 为 列表[i]
        设 i 为 i 加 1
    返回 结果
