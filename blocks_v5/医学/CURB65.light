# 积木：CURB-65评分（医学领域，自动生成）
# 契约：输入 [数] → 输出 数（计算肺炎CURB-65评分）
导出 CURB65
段落 CURB65 接收 年龄, 意识, 尿素, 呼吸, 血压:
    返回 选择(意识 小于 15, 1, 0 加 如果 尿素 大于 7 则 1 否则 0 加 如果 呼吸 大于 30 则 1 否则 0 加 如果 血压 小于 90 则 1 否则 0 加 如果 年龄 大于 65 则 1 否则 0)
