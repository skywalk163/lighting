# 积木：TOTP时间基密码（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（基于时间的一次性密码）
导出 TOTP密码
段落 TOTP密码 接收 列表, cb_追加, totp:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('totp'))
        设 i 为 i 加 1
    返回 结果
