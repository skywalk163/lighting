# 积木：Pedersen承诺（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（Pedersen承诺）
导出 Pedersen密码
段落 Pedersen密码 接收 列表, cb_追加, pedersen:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('pedersen'))
        设 i 为 i 加 1
    返回 结果
