# 积木：SHA3哈希（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（SHA3-256哈希）
导出 SHA3
段落 SHA3 接收 列表, cb_追加, sha3:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('sha3'))
        设 i 为 i 加 1
    返回 结果
