# 积木：NaccacheStern（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（Naccache-Stern）
导出 Naccache
段落 Naccache 接收 列表, cb_追加, ns:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('ns'))
        设 i 为 i 加 1
    返回 结果
