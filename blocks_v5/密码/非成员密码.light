# 积木：非成员证明（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（非成员证明）
导出 非成员密码
段落 非成员密码 接收 列表, cb_追加, nonmembership:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('nonmembership'))
        设 i 为 i 加 1
    返回 结果
