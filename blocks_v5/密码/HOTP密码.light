# 积木：HOTP计数器密码（密码领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（基于计数器的一次性密码）
导出 HOTP密码
段落 HOTP密码 接收 列表, cb_追加, hotp:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(文本.编码('hotp'))
        设 i 为 i 加 1
    返回 结果
