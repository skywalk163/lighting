# 积木：各元素JB统计（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素Jarque-Bera统计）
导出 各JB统计
段落 各JB统计 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加((偏度 乘 偏度 除 6 加 峰度 乘 峰度 除 24) 乘 长度(列表))
        设 i 为 i 加 1
    返回 结果
