# 积木：各元素指数加权（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素指数加权平均）
导出 各指数加权
段落 各指数加权 接收 列表, cb_追加, 加权:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(0.9 乘 列表[i] 加 0.1 乘 加权)
        设 i 为 i 加 1
    返回 结果
