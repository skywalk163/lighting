# 积木：计数在0到1（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（计数在0到1之间的个数）
导出 零一间数
段落 零一间数 接收 列表:
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 大于 0 且 列表[i] 小于 1:
            设 结果 为 结果 加 1
        设 i 为 i 加 1
    返回 结果
