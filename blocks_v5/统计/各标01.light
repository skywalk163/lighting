# 积木：各元素标准化到01（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素标准化到0-1）
导出 各标01
段落 各标01 接收 列表, cb_追加, 最小, 最大:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加((列表[i] 减 最小) 除 (最大 减 最小))
        设 i 为 i 加 1
    返回 结果
