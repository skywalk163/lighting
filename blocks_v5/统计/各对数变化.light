# 积木：各元素对数变化（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素对数变化）
导出 各对数变化
段落 各对数变化 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[i].对数() 减 列表[i 减 1].对数())
        设 i 为 i 加 1
    返回 结果
