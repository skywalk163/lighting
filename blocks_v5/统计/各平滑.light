# 积木：各元素平滑（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素指数平滑）
导出 各平滑
段落 各平滑 接收 列表, cb_追加, 平滑:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(0.5 乘 列表[i] 加 0.5 乘 平滑)
        设 i 为 i 加 1
    返回 结果
