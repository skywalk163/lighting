# 积木：各元素平方根倒数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素平方根的倒数）
导出 各平方根倒
段落 各平方根倒 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(1 除 列表[i].平方根())
        设 i 为 i 加 1
    返回 结果
