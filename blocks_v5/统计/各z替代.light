# 积木：各元素z分数替代（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素z分数替代）
导出 各z替代
段落 各z替代 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加((列表[i] 减 均值) 除 标准差)
        设 i 为 i 加 1
    返回 结果
