# 积木：各元素偏自相关（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素偏自相关系数）
导出 各偏自相关
段落 各偏自相关 接收 列表, cb_追加, p_i_加甲:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[i] 乘 列表[i 加 2])
        设 i 为 i 加 1
    返回 结果
