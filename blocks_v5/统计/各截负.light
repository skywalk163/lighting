# 积木：各元素截断负数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素截断保留负数）
导出 各截负
段落 各截负 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 列表[i] 小于 0 则 列表[i] 否则 0)
        设 i 为 i 加 1
    返回 结果
