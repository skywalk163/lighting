# 积木：各元素分箱到4（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素分箱到4个箱）
导出 各分箱4
段落 各分箱4 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(整数(列表[i] 除 25) 乘 25)
        设 i 为 i 加 1
    返回 结果
