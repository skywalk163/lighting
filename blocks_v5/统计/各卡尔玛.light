# 积木：各元素卡尔玛（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素卡尔玛比率）
导出 各卡尔玛
段落 各卡尔玛 接收 列表, cb_追加, 无风险, 最大回撤:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加((均值 减 无风险) 除 最大回撤)
        设 i 为 i 加 1
    返回 结果
