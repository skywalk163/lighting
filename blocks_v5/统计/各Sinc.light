# 积木：各元素Sinc（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素Sinc函数）
导出 各Sinc
段落 各Sinc 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 列表[i] 等于 0 则 1 否则 列表[i].正弦() 除 列表[i])
        设 i 为 i 加 1
    返回 结果
