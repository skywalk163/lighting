# 积木：各元素移动最小值（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素移动最小值）
导出 各移动最小
段落 各移动最小 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 最小 则 列表[i] 否则 最小)
        设 i 为 i 加 1
    返回 结果
