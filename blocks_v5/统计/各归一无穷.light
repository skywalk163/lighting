# 积木：各元素归一化无穷（统计领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素无穷范数归一化）
导出 各归一无穷
段落 各归一无穷 接收 列表, cb_追加, 最大:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[i] 除 最大)
        设 i 为 i 加 1
    返回 结果
