# 积木：前k大元素（排序领域，自动生成）
# 契约：输入 [列表] → 输出 列表（TopK最大元素，默认取前3）
导出 前k大元素
段落 前k大元素 接收 列表：
    设 k 为 3
    设 排序后 为 排序(列表)
    设 结果 为 []
    设 起 为 长度(排序后) 减 k
    如果 起 小于 0：
        设 起 为 0
    设 i 为 起
    当 i 小于 长度(排序后)：
        结果.追加(排序后[i])
        设 i 为 i 加 1
    返回 结果