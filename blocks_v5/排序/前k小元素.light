# 积木：前k小元素（排序领域，自动生成）
# 契约：输入 [列表] → 输出 列表（TopK最小元素，默认取前3）
导出 前k小元素
段落 前k小元素 接收 列表：
    设 k 为 3
    设 排序后 为 排序(列表)
    设 结果 为 []
    设 i 为 0
    当 i 小于 k 且 i 小于 长度(排序后)：
        结果.追加(排序后[i])
        设 i 为 i 加 1
    返回 结果