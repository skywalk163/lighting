# 积木：逻辑时序组合（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（时序组合逻辑）
导出 时序组合
段落 时序组合 接收 列表, cb_追加, 时钟, 条件:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 时钟 则 列表[i] 且 条件 否则 结果)
        设 i 为 i 加 1
    返回 结果
