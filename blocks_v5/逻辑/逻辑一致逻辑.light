# 积木：逻辑一致（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（一致逻辑）
导出 逻辑一致逻辑
段落 逻辑一致逻辑 接收 列表, cb_追加, 条件, 丙:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[i] 等于 条件 且 条件 等于 丙)
        设 i 为 i 加 1
    返回 结果
