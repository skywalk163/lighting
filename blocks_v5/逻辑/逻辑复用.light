# 积木：逻辑复用器（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（复用器逻辑）
导出 逻辑复用
段落 逻辑复用 接收 列表, cb_追加, 选择, 条件:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 选择 则 列表[i] 否则 条件)
        设 i 为 i 加 1
    返回 结果
