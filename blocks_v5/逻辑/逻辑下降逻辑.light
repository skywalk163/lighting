# 积木：逻辑下降沿（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（下降沿检测）
导出 逻辑下降逻辑
段落 逻辑下降逻辑 接收 列表, cb_追加, p_非值, 旧值:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(p_非值 列表[i] 且 旧值)
        设 i 为 i 加 1
    返回 结果
