# 积木：逻辑阈值（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（阈值逻辑）
导出 逻辑阈值逻辑
段落 逻辑阈值逻辑 接收 列表, cb_追加, 阈值:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 列表[i] 大于 阈值 则 1 否则 0)
        设 i 为 i 加 1
    返回 结果
