# 积木：逻辑符号（逻辑领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（符号逻辑）
导出 逻辑符号
段落 逻辑符号 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 列表[i] 大于 0 则 1 否则 (如果 列表[i] 小于 0 则 (0 减 1) 否则 0))
        设 i 为 i 加 1
    返回 结果
