# 积木：插值搜索（搜索领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（插值搜索）
导出 插值搜索
段落 插值搜索 接收 列表, cb_追加, 甲, 乙, 丙:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[整数(甲 加 (乙 减 甲) 乘 (丙 减 列表[甲]) 除 (列表[乙] 减 列表[甲]))])
        设 i 为 i 加 1
    返回 结果
