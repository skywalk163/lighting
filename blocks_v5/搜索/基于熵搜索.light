# 积木：基于熵搜索（搜索领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（基于熵搜索）
导出 基于熵搜索
段落 基于熵搜索 接收 列表, cb_追加, 乙:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(0 减 列表[i] 乘 对数(列表[i]) 减 乙 乘 对数(乙))
        设 i 为 i 加 1
    返回 结果
