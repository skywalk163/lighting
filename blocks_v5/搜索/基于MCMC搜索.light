# 积木：基于MCMC搜索（搜索领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（MCMC搜索）
导出 基于MCMC搜索
段落 基于MCMC搜索 接收 列表, cb_追加, 接受率:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 随机() 小于 接受率 则 列表[i] 否则 列表[i 加 1])
        设 i 为 i 加 1
    返回 结果
