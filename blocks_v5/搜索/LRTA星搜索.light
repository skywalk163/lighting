# 积木：LRTA搜索（搜索领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（LRTA*搜索）
导出 LRTA星搜索
段落 LRTA星搜索 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(列表[i] 加 列表[i 加 1])
        设 i 为 i 加 1
    返回 结果
