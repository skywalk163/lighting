# 积木：取完全平方（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出完全平方数）
导出 完全平方
段落 完全平方 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 整数(列表[i].平方根()) 乘 整数(列表[i].平方根()) 等于 列表[i]:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
