# 积木：取回文数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出回文数）
导出 回文数
段落 回文数 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 文本(列表[i]) 等于 文本(列表[i]).反转():
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
