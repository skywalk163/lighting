# 积木：取小数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出小数元素）
导出 小数
段落 小数 接收 列表, 不等于, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 不等于 整数(列表[i]):
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
