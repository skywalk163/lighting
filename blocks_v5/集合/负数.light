# 积木：取负数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出负数）
导出 负数
段落 负数 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 小于 0:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
