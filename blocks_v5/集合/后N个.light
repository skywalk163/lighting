# 积木：取后N个（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（取后N个元素）
导出 后个数
段落 后个数 接收 列表, 个数, 大于等于, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 i 大于等于 长度(列表) 减 个数:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
