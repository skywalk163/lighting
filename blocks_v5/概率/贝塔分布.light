# 积木：贝塔分布（概率领域）
# 契约：f(x) = x^(alpha-1)(1-x)^(beta-1)/B(alpha,beta)
# 需要 math.gamma 支持，当前返回简单近似
导出 贝塔分布
段落 贝塔分布 接收 alpha, beta, x, cb_幂, 次方:
    如果 x 小于 0 或 x 大于 1:
        返回 0
    设 结果 为 cb_幂(x, alpha 减 1) 乘 cb_幂(1 减 x, beta 减 1)
    返回 结果