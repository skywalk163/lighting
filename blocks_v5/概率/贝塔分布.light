# 积木：贝塔分布（概率领域）
# 契约：f(x) = x^(α-1)(1-x)^(β-1)/B(α,β)
# 需要 math.gamma 支持，当前返回简单近似
导出 贝塔分布
段落 贝塔分布 接收 α, β, x：
    如果 x 小于 0 或 x 大于 1：
        返回 0
    设 结果 为 (x 的 (α 减 1) 次方) 乘 ((1 减 x) 的 (β 减 1) 次方)
    返回 结果