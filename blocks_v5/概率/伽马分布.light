# 积木：伽马分布（概率领域）
# 契约：f(x)=beta^alpha x^(alpha-1) e^(-betax)/Γ(alpha)
# 需要 math.gamma 支持，当前返回简单近似
导出 伽马分布
段落 伽马分布 接收 形状, 尺度, x, cb_幂, 次方, 除以:
    如果 x 小于 0:
        返回 0
    设 结果 为 cb_幂(x, 形状 减 1) 乘 cb_幂(2.71828, -x 除以 尺度)
    返回 结果