# 积木：滚动均值三期（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（3期滚动均值）
导出 滚动均值3
段落 滚动均值3 接收 列表, cb_追加, p_i_加甲:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加((列表[i] 加 列表[i 加 1] 加 列表[i 加 2]) 除 3)
        设 i 为 i 加 1
    返回 结果
