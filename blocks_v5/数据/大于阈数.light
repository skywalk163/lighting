# 积木：计数大于阈值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（计数大于阈值的元素）
导出 大于阈数
段落 大于阈数 接收 列表, 阈值:
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 大于 阈值:
            设 结果 为 结果 加 1
        设 i 为 i 加 1
    返回 结果
