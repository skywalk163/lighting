# 积木：过滤模2零（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤偶数）
导出 过滤模二零
段落 过滤模二零 接收 列表, cmp阈值, mod阈值, cb_追加:
    设 结果 为 []
    设 i 为 p_cmp值
    当 i 小于 长度(列表):
        如果 列表[i] 模 p_mod值 等于 p_cmp值:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果