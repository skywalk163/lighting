# 积木：成对投影（数据领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对投影）
导出 成对投影数据
段落 成对投影数据 接收 表甲, 表乙, cb_追加, 甲, 乙:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙):
        结果.cb_追加(表甲[i] 甲 乘 (甲 乘 乙 除 (甲 乘 甲)) 乘 表乙[i])
        设 i 为 i 加 1
    返回 结果
