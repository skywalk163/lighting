# 积木：成对平均（数据领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对取平均）
导出 成对平均
段落 成对平均 接收 表甲, 表乙, p_零, cb_追加, p_二:
    设 结果 为 []
    设 i 为 p_零
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙):
        结果.cb_追加((表甲[i] 加 表乙[i]) 除 p_二)
        设 i 为 i 加 1
    返回 结果
