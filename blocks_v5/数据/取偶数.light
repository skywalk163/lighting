# 积木：取偶数（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出偶数）
导出 取偶数
段落 取偶数 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 模 2 等于 0:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
