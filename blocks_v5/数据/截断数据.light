# 积木：各元素截断（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素截断到0-100）
导出 截断数据
段落 截断数据 接收 列表, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        结果.cb_追加(如果 列表[i] 小于 0 则 0 否则 (如果 列表[i] 大于 100 则 100 否则 列表[i]))
        设 i 为 i 加 1
    返回 结果
