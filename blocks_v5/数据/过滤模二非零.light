# 积木：过滤模2非零（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤奇数）
导出 过滤模二非零
段落 过滤模二非零 接收 列表：
    设 结果 为 []
    设 i 为 零
    当 i 小于 长度(列表)：
        如果 非 (列表[i] 模 二 等于 零)：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果