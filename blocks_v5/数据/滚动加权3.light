# 积木：滚动加权3（数据领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（3期加权移动）
导出 滚动加权3
段落 滚动加权3 接收 列表, 窗宽：
    设 结果 为 []
    设 i 为 零
    当 真：
        如果 i 加 窗宽 大于 长度(列表)：
            跳出
        设 窗口 为 []
        设 j 为 零
        当 j 小于 窗宽：
            窗口.追加(列表[i 加 j])
            设 j 为 j 加 1
        结果.追加((窗口[0] 乘 1 加 窗口[1] 乘 二 加 窗口[二] 乘 三) 除 6)
        设 i 为 i 加 1
    返回 结果
