# 积木：滚动最小值3（数据领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（3期滚动最小值）
导出 滚动最小3数据
段落 滚动最小3数据 接收 列表, 窗宽：
    设 结果 为 []
    设 i 为 零
    当 真：
        如果 i 加 窗宽 大于 长度(列表)：
            跳出
        设 窗口 为 []
        设 j 为 零
        当 j 小于 窗宽：
            设 窗口 为 追加(窗口, 列表[i 加 j])
            设 j 为 j 加 1
        如果 窗口[0] 小于 窗口[1]：
            如果 窗口[0] 小于 窗口[二]：
                设 结果 为 追加(结果, 窗口[0])
            否则：
                设 结果 为 追加(结果, 窗口[二])
        否则：
            如果 窗口[1] 小于 窗口[二]：
                设 结果 为 追加(结果, 窗口[1])
            否则：
                设 结果 为 追加(结果, 窗口[二])
        设 i 为 i 加 1
    返回 结果