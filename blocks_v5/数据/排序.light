# 积木：排序列表（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（对列表升序排序）
导出 排序
段落 排序 接收 列表：
    设 长度 为 长度(列表)
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度：
        结果.追加(列表[i])
        设 i 为 i 加 1
    设 i 为 0
    当 i 小于 长度：
        设 j 为 i 加 1
        当 j 小于 长度：
            如果 结果[i] 大于 结果[j]：
                设 缓存 为 结果[i]
                设 结果[i] 为 结果[j]
                设 结果[j] 为 缓存
            设 j 为 j 加 1
        设 i 为 i 加 1
    返回 结果