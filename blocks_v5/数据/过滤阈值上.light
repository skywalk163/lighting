# 积木：过滤阈值上（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤上阈值）
导出 过滤阈值上
段落 过滤阈值上 接收 列表, 阈值, cb_追加:
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表):
        如果 列表[i] 大于 阈值:
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
