# 积木：过滤模10零（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤10倍数）
导出 过滤模十零
段落 过滤模十零 接收 列表：
    设 结果 为 []
    设 i 为 零
    当 i 小于 长度(列表)：
        如果 列表[i] 模 10 等于 零：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果