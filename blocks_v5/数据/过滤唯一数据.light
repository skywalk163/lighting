# 积木：过滤唯一（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤唯一值）
导出 过滤唯一数据
段落 过滤唯一数据 接收 列表, p_零, cb_追加:
    设 结果 为 []
    设 i 为 p_零
    当 i 小于 长度(列表):
        如果 包含(列表, 列表[i]):
            结果.cb_追加(列表[i])
        设 i 为 i 加 1
    返回 结果
