# 段言积木组合（积木库）

把「AI 生成代码」替换为「预置积木的组合装配」——给定一句话需求，系统在**零 token、全离线**的情况下选出合适的段言积木、按契约接线、内联粘合成一个可运行的 `.duan` 文件。只有当需求真的超出当前积木能力时，才把 LLM 当作「兜底生成器」补一块新积木并永久沉淀。

核心思路：用**组合式软件生成**（compositional generation）替代**生成式软件生成**。本仓库对标隔壁 jikuai，但底层用段言的真·模块系统 + 成语式宏 + 字节码，组合更稳、更可控。

---

## 30 秒上手

```bash
# 自动组合（默认 embedding 选块，零 token）
python 积木库/组合.py "对一批数字求和再算平均" --输入 "[1,2,3,4,5]" --top 2

# 语义选块（同义改写也能命中）
python 积木库/语义选块.py "把金额写成人民币大写" --top 3

# 契约级链式接线：前一步结果自动喂后一步
python 积木库/组合.py "求和然后分页" --链式 --输入 "[1..25]" --top 2

# LLM 兜底：库内没有的能力自动生成新积木并运行
python 积木库/组合.py "计算斐波那契数列第10项" --输入 "10" --top 1
```

> 运行需经段言解释器：`python cli/duan.py run <文件>.duan`。上面的 `组合.py` 会自己调起解释器。

---

## 架构（一条零 token 链路）

| 模块 | 文件 | 职责 |
|---|---|---|
| 契约索引 | `索引.json` | 每块的名称/领域/层级/输入输出/稳定性/路径/导出名 |
| 关键词选块 | `选块.py` | v0 字符重叠打分（零 token） |
| 语义选块 | `语义选块.py` | TF-IDF(字符 n-gram)+同义词+领域先验（零 token） |
| **embedding 选块** | `embedding选块.py` | 概念图向量（默认，0.1ms）；真·句向量为 `--real` 实验选项（基准显示反而更差，见 `评估/README.md`） |
| **五把尺子** | `评估/跑分.py`、`评估/接线跑分.py`、`评估/体检.py`、`评估/冒烟.py`、`评估/扰动跑分.py` | 分别量选块（89 条）、接线（26 条）、契约自洽性、可运行率（92/92）、泛化（扰动集 40 条刻意避开块名用字的迂回说法），防止改动回退 |
| **LLM/本地校验器** | `校验器.py` | 运行前第二道闸：本地概念交集 + 可接真实 LLM 判定组合是否满足需求 |
| **类型系统** | `类型.py` | 结构化类型（`列表[T]`/`字典[K,V]`/`可选`/`联合`）+ 0~1 匹配度 |
| 契约级接线 | `接线.py` | 结构化类型匹配 + 参数名相似 + 上游优先的数据流规划；接不上上游时显式告警 |
| 内联粘合 | `粘合.py` | 把选中积木源码拼进单一 `.duan`（对应成语式宏展开） |
| 组合总入口 | `组合.py` | 需求 + 输入 → 选块 → 校验 → 接线 → 粘合 → 运行 |
| 层级生成 | `层级生成.py` | 成语式宏：把 L0+ 组合固化成 L1/L2 高级积木并注册进索引 |
| 兜底生成器 | `兜底生成器.py` | LLM 按契约生成新积木；无 key 时降级本地规则 |

数据流：`需求 → 选块(召回) → 校验(判定) → 接线(数据流) → 粘合(代码) → 运行`。

---

## 设计原则：零 token 优先

- **常态零 token**：选块、校验、接线、层级、粘合全本地。
- **LLM 只是兜底**：仅在「无候选 / 分数低于阈值 / 校验 MISMATCH / 接线不可接」且本地规则也覆盖不了时才调一次模型；生成的新积木写入 `生成/` 并注册进索引，**下次同需求零 token 复用**。
- **可解释可复现**：每个组合都能还原成一份方案 JSON（`方案` 结构见 `协议.md`），不依赖概率生成。

---

## 如何新增一块积木（L0）

1. 在对应领域目录新建 `积木库/<领域>/<名称>.duan`，格式：

   ```duan
   # 注释：一句话说明 + 契约
   导出 求和
   段落 求和 接收 序列：
       设 和 为 0
       设 i 为 0
       当 i 小于 长度(序列)：
           设 和 为 和 加 序列[i]
           设 i 为 i 加 1
       返回 和
   ```

2. 在 `索引.json` 的 `块` 数组追加一条契约（名称/领域/层级=0/描述/输入/输出/稳定性/路径/导出名）。
   **类型必须写结构化形式**：`列表[数]` / `列表[文本]` / `字典[文本,数]` / `可选[数]` / `联合[数,文本]`，
   元素类型确实任意时写 `列表[任意]`；裸 `列表` 虽向后兼容，但体检器会报 W4 警告。
3. `python 积木库/评估/体检.py` 必须 0 错误（段落名==导出名、导出名互不互为前缀、签名数一致）。
4. （可选）`python 积木库/组合.py "你的需求" --输入 "..." --top 1` 验证能被选中运行。

> 铁律一：`段落` 名必须与 `导出` 名（即「导出名」）一致，否则调用方按导出名调用会 `NameError`。
> 铁律二：导出名之间**不得互为前缀**（如 `中位` 与 `中位数`）——段言词法器全文件预扫描 +
> 最长匹配，前缀重叠会把标识符切碎。体检器 W1 专门盯这个。

---

## 如何新增一个 L1+（成语式宏）

写一个配方 JSON（见 `范围跨度配方.json` / `统计双指标配方.json`），运行：

```bash
python 积木库/层级生成.py 积木库/范围跨度配方.json --写索引
# 或自动发现可组合模式并织成 L1：
python 积木库/层级生成.py --自动
```

生成的 L1/L2 会进入索引，后续可像普通积木一样被选中或继续向上组合。

---

## 版本演进

- **v0.0** 移植 jikuai 的 `索引.json + select/glue` 思路，建立段言版选块 + 内联粘合，端到端跑通。
- **v0.13** 语义选块（TF-IDF+同义词）、链式数据流、L1+ 层级生成（成语式宏）。
- **v0.14** 契约级精确接线（解决多步同类型误接）、LLM 兜底生成器。
- **v0.15** embedding 概念图选块（根治词法盲区）、LLM/本地校验器（运行前第二道闸）、生成块自动织入 L1+。
- **v0.16** sentence_transformers 真·句向量检索（更细语义召回）；真实 LLM 校验器处理细微错配（如「中文转拼音 vs 数字转中文」）。
- **v0.18** 类型系统 v2：契约从裸标签（`列表`）升级为结构化类型（`列表[数]`/`字典[文本,数]`/`可选[T]`/`联合[A,B]`），全库 42 块一次性迁移；接线器改用 **0~1 匹配度**排序并修掉「多共享输入抢主数据」；`--链式` 接不上上游时显式告警而非静默退化；新增契约体检器（抓到 `均值` 导出名与 `中位数` 前缀冲突的真雷）。接线跑分 **1.000 vs 粗类型对照 0.769**，选块指标不变。
- **v0.19** 第四把尺子 **冒烟测试**（`评估/冒烟.py`）：前三把尺子都不执行代码，段言的坑（词法前缀吞并 / 关键字前缀 / `己`=self）只在运行期暴露。首发即抓出 7 块「契约完美、一跑就崩」的假积木。顺带修掉语言层 bug：`src/lexer.py` 的 `_scan_user_definitions` 漏收 `_`，导致 `聚合_财务_数_2项` 这类名字被切碎（词法器测试 15 passed 零回归）。
- **v0.20** 扩库 **42 → 92 块**（新增 日期 / 校验 / 格式 三个领域，及统计、数值、网络、文本补充）。基准套件同步扩到 89 条，10 条原「库外」能力转入库内。自动织补两道闸：**冒烟自检**（同输入类型 ≠ 同输入语义，跑不通不注册）与 **`选块可见` 透传**（聚合脚手架退出候选池，不遮蔽精确块）。四把尺子全绿：可运行率 **1.000 (92/92)**、体检 0 错 0 警、Hit@1 **1.0**、MRR **1.0**、库外召回 **1.0**、误兜底 **0**、校验器 FN/FP **0/0**、链路正确率 **1.0**。
- **v0.21** 混合选块 + 计划缓存。造 `评估/扰动集.json`（40 条刻意避开块名用字的「迂回说法」）后发现基准 1.0 是**过拟合**：概念图在扰动集上 Hit@1 仅 **0.375**、**18/40 空候选**，问题在**召回**不在排序。于是 `混合选块.py` 做两级：概念图为主召回，空/弱召回时 TF-IDF **二级补召回**，并列群内语义重排。扰动集 Hit@1 **0.375 → 0.85**、空候选 **18 → 0**；诚实的泛化数字看留出段 R21–40：**hybrid 0.70 vs concept 0.50**（R01–20 是调参段，1.00 含过拟合）。基准 89 条两策略均 **1.0 零回归**，库外召回仍 1.0（补召回进来的候选由校验器拦下）。`计划缓存.py`：需求指纹 + **库指纹** + 策略指纹 → 复用整套选块/校验/接线结论；库一变（兜底生成新块、契约改动）整体作废，绝不拿旧方案硬套。规划耗时 **13.2ms → 2.5ms（5.3x）**。
- **v0.22** 补泛化（自动别名扩充）：新增 `auto_alias.py` + `概念别名补充.json`（131 条别名 / 77 概念，从块**元数据**榨同义说法，无 LLM key 也能跑）；`embedding选块.py` 在 import 时合并补充别名，并新增「**具体度决胜**」——余弦完全平局时优先选查询命中别名更长的候选（压同义抢答），只重排平局不拖累主基准。修复 `auto_alias` 真 bug：块名兜底误把「取奇数」归到「取偶数」污染别名表（U05 接不到）。效果：**扰动集留出段 concept 0.70 → 1.0**（远超 0.90 目标）；主基准 Hit@1 仍 0.9881（唯一残余 D15 为复合块余弦稀释的结构性短板）。
- **v0.22.1 (D15)** 复合块覆盖度门控：查询显式带「同时/都/两个/一并」且点名 ≥2 个具体概念时，对只满足部分概念的块按覆盖比例降权，让复合块（如 `统计双指标`=均值+极差）压过单组件块（`范围跨度`=极差）；顺序型需求（算完 X 再看看 Y）不带复合标记、不触发，避免误伤 S07。主基准（89 条带校验）链路正确率 **0.9888 → 1.0**。
- **v0.23** 执行闭环：组合.py 新增 `_运行_单次` + `_成功` 判定（段言运行时静默吞错，**`rc==0 且 有非空输出`** 才算成功），`_cli` 运行段改为带重试闭环——主方案跑挂 → 自动换次优候选重跑 → 都挂触发兜底生成重跑。双评测零回归：泛化 concept 1.0 / hybrid 0.975；主基准（89 条带校验）全 **1.0**。
- **v0.17** 评测驱动：`评估/` 基准套件（62 条）把质量变成可回归的数字；概念拆分 + 最长别名优先 + 能力边界约束，把链路正确率从 **0.387 提到 0.984**；数据推翻「真向量优先」的默认（真向量 0.823/3733ms 反不如概念图 0.984/0.1ms），降级为 `--real` 实验选项；新增类型闸门与「纠正优先于生成」。详见 `协议.md`、`评估/README.md`。

---

## 接入真实大模型

复制 `llm_config.example.json` 为 `llm_config.json`，填入 `api_key` / `base_url` / `model`（OpenAI 兼容端点）。此时：
- 选块低置信或能力缺失时，由真实 LLM 生成缺失积木；
- 校验器用真实 LLM 判定「组合是否真的满足需求」，能拦下词法/语义的细微错配。

无 key 时全部降级为本地规则，系统仍完整可用。

完整协议与方案 JSON 结构见 [`协议.md`](./协议.md)。
