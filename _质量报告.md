# 积木质量扫描报告

- **扫描时间**: 2026-08-11 14:09:53
- **积木库根目录**: d:\traework\light\积木库\blocks_v5
- **扫描领域**: 物理, 化学, 生物, 天文, 地理
- **总文件数**: 903
- **有问题的文件**: 237
- **扫描耗时**: 1.45s

## 各领域统计

| 领域 | 文件数 | 有问题 | 状态 |
|------|--------|--------|------|
| 物理 | 199 | 1 | ⚠️ |
| 化学 | 171 | 21 | ⚠️ |
| 生物 | 194 | 31 | ⚠️ |
| 天文 | 171 | 66 | ⚠️ |
| 地理 | 168 | 118 | ⚠️ |

## 问题分类统计

| 问题类型 | 数量 |
|----------|------|
| SelfRecursive | 227 |
| StubBlock | 10 |

## 问题详情

### SelfRecursive（227 处）

- 物理/瞬时速度.light: 函数体调用自身: 返回 瞬时速度(输入)
- 化学/元素周期.light: 函数体调用自身: 返回 元素周期(输入)
- 化学/元素族.light: 函数体调用自身: 返回 元素族(输入)
- 化学/共价半径.light: 函数体调用自身: 返回 共价半径(输入)
- 化学/化合价.light: 函数体调用自身: 返回 化合价(输入)
- 化学/原子量.light: 函数体调用自身: 返回 原子量(输入)
- 化学/放射性.light: 函数体调用自身: 返回 放射性(输入)
- 化学/晶体结构.light: 函数体调用自身: 返回 晶体结构(输入)
- 化学/氧化态.light: 函数体调用自身: 返回 氧化态(输入)
- 化学/沸点.light: 函数体调用自身: 返回 沸点(输入)
- 化学/溶解性.light: 函数体调用自身: 返回 溶解性(输入)
- 化学/熔点.light: 函数体调用自身: 返回 熔点(输入)
- 化学/电子亲和能.light: 函数体调用自身: 返回 电子亲和能(输入)
- 化学/电负性.light: 函数体调用自身: 返回 电负性(输入)
- 化学/磁性.light: 函数体调用自身: 返回 磁性(输入)
- 化学/离子半径.light: 函数体调用自身: 返回 离子半径(输入)
- 化学/第一电离能.light: 函数体调用自身: 返回 第一电离能(输入)
- 化学/范德华半径.light: 函数体调用自身: 返回 范德华半径(输入)
- 生物/DNA长度.light: 函数体调用自身: 返回 DNA长度(输入)
- 生物/互惠利他.light: 函数体调用自身: 返回 互惠利他(输入)
- 生物/亲代投资.light: 函数体调用自身: 返回 亲代投资(输入)
- 生物/代谢率.light: 函数体调用自身: 返回 代谢率(输入)
- 生物/传导速度.light: 函数体调用自身: 返回 传导速度(输入)
- 生物/冬眠时长.light: 函数体调用自身: 返回 冬眠时长(输入)
- 生物/利他行为.light: 函数体调用自身: 返回 利他行为(输入)
- 生物/动作电位.light: 函数体调用自身: 返回 动作电位(输入)
- 生物/基因数量.light: 函数体调用自身: 返回 基因数量(输入)
- 生物/婚配制度.light: 函数体调用自身: 返回 婚配制度(输入)
- 生物/尿酸.light: 函数体调用自身: 返回 尿酸(输入)
- 生物/年龄结构.light: 函数体调用自身: 返回 年龄结构(输入)
- 生物/染色体数.light: 函数体调用自身: 返回 染色体数(输入)
- 生物/甘油三酯.light: 函数体调用自身: 返回 甘油三酯(输入)
- 生物/白细胞.light: 函数体调用自身: 返回 白细胞(输入)
- 生物/社会等级.light: 函数体调用自身: 返回 社会等级(输入)
- 生物/突触延迟.light: 函数体调用自身: 返回 突触延迟(输入)
- 生物/线粒体.light: 函数体调用自身: 返回 线粒体(输入)
- 生物/细胞大小.light: 函数体调用自身: 返回 细胞大小(输入)
- 生物/肌酐.light: 函数体调用自身: 返回 肌酐(输入)
- 生物/胆固醇.light: 函数体调用自身: 返回 胆固醇(输入)
- 生物/膜电位.light: 函数体调用自身: 返回 膜电位(输入)
- 生物/血小板.light: 函数体调用自身: 返回 血小板(输入)
- 生物/血糖.light: 函数体调用自身: 返回 血糖(输入)
- 生物/血红蛋白.light: 函数体调用自身: 返回 血红蛋白(输入)
- 生物/血细胞.light: 函数体调用自身: 返回 血细胞(输入)
- 生物/血细胞比容.light: 函数体调用自身: 返回 血细胞比容(输入)
- 生物/迁徙时间.light: 函数体调用自身: 返回 迁徙时间(输入)
- 生物/迁徙距离.light: 函数体调用自身: 返回 迁徙距离(输入)
- 生物/配偶选择.light: 函数体调用自身: 返回 配偶选择(输入)
- 生物/领域行为.light: 函数体调用自身: 返回 领域行为(输入)
- 天文/CCD采样率.light: 函数体调用自身: 返回 CCD采样率(输入)
- 天文/中子星密度.light: 函数体调用自身: 返回 中子星密度(输入)
- 天文/信噪比计算.light: 函数体调用自身: 返回 信噪比计算(输入)
- 天文/偏振测量.light: 函数体调用自身: 返回 偏振测量(输入)
- 天文/光度距离宇宙.light: 函数体调用自身: 返回 光度距离宇宙(输入)
- 天文/光谱分辨率.light: 函数体调用自身: 返回 光谱分辨率(输入)
- 天文/光谱类型.light: 函数体调用自身: 返回 光谱类型(输入)
- 天文/分光镜色散.light: 函数体调用自身: 返回 分光镜色散(输入)
- 天文/大气压力.light: 函数体调用自身: 返回 大气压力(输入)
- 天文/大气标高.light: 函数体调用自身: 返回 大气标高(输入)
- 天文/大气视宁度.light: 函数体调用自身: 返回 大气视宁度(输入)
- 天文/天体测量精度.light: 函数体调用自身: 返回 天体测量精度(输入)
- 天文/太阳常数.light: 函数体调用自身: 返回 太阳常数(输入)
- 天文/太阳活动周期.light: 函数体调用自身: 返回 太阳活动周期(输入)
- 天文/太阳黑子数.light: 函数体调用自身: 返回 太阳黑子数(输入)
- 天文/宇宙曲率.light: 函数体调用自身: 返回 宇宙曲率(输入)
- 天文/小行星直径.light: 函数体调用自身: 返回 小行星直径(输入)
- 天文/干涉仪分辨率.light: 函数体调用自身: 返回 干涉仪分辨率(输入)
- 天文/干涉仪基线.light: 函数体调用自身: 返回 干涉仪基线(输入)
- 天文/开普勒第一定律.light: 函数体调用自身: 返回 开普勒第一定律(输入)
- 天文/开普勒第二定律.light: 函数体调用自身: 返回 开普勒第二定律(输入)
- 天文/引力时间膨胀.light: 函数体调用自身: 返回 引力时间膨胀(输入)
- 天文/引力波振幅.light: 函数体调用自身: 返回 引力波振幅(输入)
- 天文/引力波频率.light: 函数体调用自身: 返回 引力波频率(输入)
- 天文/引力红移.light: 函数体调用自身: 返回 引力红移(输入)
- 天文/彗星轨道周期.light: 函数体调用自身: 返回 彗星轨道周期(输入)
- 天文/恒星半径.light: 函数体调用自身: 返回 恒星半径(输入)
- 天文/恒星温度.light: 函数体调用自身: 返回 恒星温度(输入)
- 天文/恒星绝对星等.light: 函数体调用自身: 返回 恒星绝对星等(输入)
- 天文/恒星视星等.light: 函数体调用自身: 返回 恒星视星等(输入)
- 天文/恒星质量.light: 函数体调用自身: 返回 恒星质量(输入)
- 天文/探测效率.light: 函数体调用自身: 返回 探测效率(输入)
- 天文/日地距离.light: 函数体调用自身: 返回 日地距离(输入)
- 天文/时间分辨率.light: 函数体调用自身: 返回 时间分辨率(输入)
- 天文/暗物质占比.light: 函数体调用自身: 返回 暗物质占比(输入)
- 天文/暗能量占比.light: 函数体调用自身: 返回 暗能量占比(输入)
- 天文/月地距离.light: 函数体调用自身: 返回 月地距离(输入)
- 天文/月相计算.light: 函数体调用自身: 返回 月相计算(输入)
- 天文/望远镜分辨率.light: 函数体调用自身: 返回 望远镜分辨率(输入)
- 天文/望远镜放大倍率.light: 函数体调用自身: 返回 望远镜放大倍率(输入)
- 天文/望远镜极限星等.light: 函数体调用自身: 返回 望远镜极限星等(输入)
- 天文/望远镜焦比.light: 函数体调用自身: 返回 望远镜焦比(输入)
- 天文/望远镜焦距.light: 函数体调用自身: 返回 望远镜焦距(输入)
- 天文/望远镜视场.light: 函数体调用自身: 返回 望远镜视场(输入)
- 天文/望远镜集光力.light: 函数体调用自身: 返回 望远镜集光力(输入)
- 天文/流星体速度.light: 函数体调用自身: 返回 流星体速度(输入)
- 天文/测光精度.light: 函数体调用自身: 返回 测光精度(输入)
- 天文/红移计算.light: 函数体调用自身: 返回 红移计算(输入)
- 天文/能量分辨率.light: 函数体调用自身: 返回 能量分辨率(输入)
- 天文/行星反照率.light: 函数体调用自身: 返回 行星反照率(输入)
- 天文/行星密度.light: 函数体调用自身: 返回 行星密度(输入)
- 天文/行星平衡温度.light: 函数体调用自身: 返回 行星平衡温度(输入)
- 天文/行星环半径.light: 函数体调用自身: 返回 行星环半径(输入)
- 天文/行星自转速度.light: 函数体调用自身: 返回 行星自转速度(输入)
- 天文/行星表面重力.light: 函数体调用自身: 返回 行星表面重力(输入)
- 天文/行星轴倾角.light: 函数体调用自身: 返回 行星轴倾角(输入)
- 天文/行星逃逸速度.light: 函数体调用自身: 返回 行星逃逸速度(输入)
- 天文/角直径距离.light: 函数体调用自身: 返回 角直径距离(输入)
- 天文/赫罗图位置.light: 函数体调用自身: 返回 赫罗图位置(输入)
- 天文/轨道半长轴.light: 函数体调用自身: 返回 轨道半长轴(输入)
- 天文/近地点距离.light: 函数体调用自身: 返回 近地点距离(输入)
- 天文/远地点距离.light: 函数体调用自身: 返回 远地点距离(输入)
- 天文/陨石类型.light: 函数体调用自身: 返回 陨石类型(输入)
- 天文/黑洞事件视界.light: 函数体调用自身: 返回 黑洞事件视界(输入)
- 天文/黑洞温度.light: 函数体调用自身: 返回 黑洞温度(输入)
- 天文/黑洞蒸发时间.light: 函数体调用自身: 返回 黑洞蒸发时间(输入)
- 地理/Delaunay三角剖分.light: 函数体调用自身: 返回 Delaunay三角剖分(输入)
- 地理/EVI计算.light: 函数体调用自身: 返回 EVI计算(输入)
- 地理/MGRS坐标.light: 函数体调用自身: 返回 MGRS坐标(输入)
- 地理/MGRS转经纬度.light: 函数体调用自身: 返回 MGRS转经纬度(输入)
- 地理/MNDWI计算.light: 函数体调用自身: 返回 MNDWI计算(输入)
- 地理/NDBI计算.light: 函数体调用自身: 返回 NDBI计算(输入)
- 地理/NDVI计算.light: 函数体调用自身: 返回 NDVI计算(输入)
- 地理/NDWI计算.light: 函数体调用自身: 返回 NDWI计算(输入)
- 地理/SAVI计算.light: 函数体调用自身: 返回 SAVI计算(输入)
- 地理/UTM坐标.light: 函数体调用自身: 返回 UTM坐标(输入)
- 地理/UTM转经纬度.light: 函数体调用自身: 返回 UTM转经纬度(输入)
- 地理/中点坐标.light: 函数体调用自身: 返回 中点坐标(输入)
- 地理/亮度温度.light: 函数体调用自身: 返回 亮度温度(输入)
- 地理/人口增长率.light: 函数体调用自身: 返回 人口增长率(输入)
- 地理/人口密度分级.light: 函数体调用自身: 返回 人口密度分级(输入)
- 地理/人口性别比.light: 函数体调用自身: 返回 人口性别比(输入)
- 地理/人口抚养比.light: 函数体调用自身: 返回 人口抚养比(输入)
- 地理/兰勃特投影.light: 函数体调用自身: 返回 兰勃特投影(输入)
- 地理/冰川面积.light: 函数体调用自身: 返回 冰川面积(输入)
- 地理/几何校正.light: 函数体调用自身: 返回 几何校正(输入)
- 地理/可视域分析.light: 函数体调用自身: 返回 可视域分析(输入)
- 地理/可达性.light: 函数体调用自身: 返回 可达性(甲, 乙, 丙)
- 地理/含沙量.light: 函数体调用自身: 返回 含沙量(输入)
- 地理/国家代码.light: 函数体调用自身: 返回 国家代码(输入)
- 地理/土地利用类型.light: 函数体调用自身: 返回 土地利用类型(输入)
- 地理/土壤类型.light: 函数体调用自身: 返回 土壤类型(甲, 乙)
- 地理/地址解析.light: 函数体调用自身: 返回 地址解析(输入)
- 地理/地形剖面.light: 函数体调用自身: 返回 地形剖面(输入)
- 地理/地形坡向.light: 函数体调用自身: 返回 地形坡向(输入)
- 地理/地形坡度.light: 函数体调用自身: 返回 地形坡度(输入)
- 地理/地形曲率.light: 函数体调用自身: 返回 地形曲率(输入)
- 地理/地形粗糙度.light: 函数体调用自身: 返回 地形粗糙度(输入)
- 地理/地形起伏.light: 函数体调用自身: 返回 地形起伏(输入)
- 地理/地形阴影.light: 函数体调用自身: 返回 地形阴影(输入)
- 地理/地心坐标.light: 函数体调用自身: 返回 地心坐标(输入)
- 地理/地理区域.light: 函数体调用自身: 返回 地理区域(输入)
- 地理/地表温度.light: 函数体调用自身: 返回 地表温度(输入)
- 地理/地震烈度.light: 函数体调用自身: 返回 地震烈度(输入)
- 地理/地震能量.light: 函数体调用自身: 返回 地震能量(输入)
- 地理/地震震级.light: 函数体调用自身: 返回 地震震级(输入)
- 地理/坡向.light: 函数体调用自身: 返回 坡向(甲, 乙)
- 地理/基尼系数.light: 函数体调用自身: 返回 基尼系数(输入)
- 地理/墨卡托反投影.light: 函数体调用自身: 返回 墨卡托反投影(输入)
- 地理/墨卡托投影.light: 函数体调用自身: 返回 墨卡托投影(输入)
- 地理/多边形周长.light: 函数体调用自身: 返回 多边形周长(输入)
- 地理/多边形面积.light: 函数体调用自身: 返回 多边形面积(输入)
- 地理/大气校正.light: 函数体调用自身: 返回 大气校正(输入)
- 地理/大陆面积.light: 函数体调用自身: 返回 大陆面积(输入)
- 地理/太阳方位角.light: 函数体调用自身: 返回 太阳方位角(输入)
- 地理/太阳高度角.light: 函数体调用自身: 返回 太阳高度角(输入)
- 地理/岛屿面积.light: 函数体调用自身: 返回 岛屿面积(输入)
- 地理/径流量.light: 函数体调用自身: 返回 径流量(输入)
- 地理/投影带号.light: 函数体调用自身: 返回 投影带号(输入)
- 地理/日出时间.light: 函数体调用自身: 返回 日出时间(甲, 乙, 丙)
- 地理/日照时长.light: 函数体调用自身: 返回 日照时长(输入)
- 地理/日落时间.light: 函数体调用自身: 返回 日落时间(甲, 乙, 丙)
- 地理/时区识别.light: 函数体调用自身: 返回 时区识别(输入)
- 地理/时差.light: 函数体调用自身: 返回 时差(输入)
- 地理/昼夜平分点.light: 函数体调用自身: 返回 昼夜平分点(输入)
- 地理/昼夜长短.light: 函数体调用自身: 返回 昼夜长短(甲, 乙)
- 地理/最近邻.light: 函数体调用自身: 返回 最近邻(输入)
- 地理/最近邻分析.light: 函数体调用自身: 返回 最近邻分析(输入)
- 地理/本地平均时.light: 函数体调用自身: 返回 本地平均时(输入)
- 地理/极球面投影.light: 函数体调用自身: 返回 极球面投影(输入)
- 地理/标准差椭圆.light: 函数体调用自身: 返回 标准差椭圆(输入)
- 地理/核密度.light: 函数体调用自身: 返回 核密度(甲, 乙, 丙)
- 地理/植被类型.light: 函数体调用自身: 返回 植被类型(甲, 乙)
- 地理/植被覆盖率.light: 函数体调用自身: 返回 植被覆盖率(输入)
- 地理/正射投影.light: 函数体调用自身: 返回 正射投影(输入)
- 地理/气候带.light: 函数体调用自身: 返回 气候带(输入)
- 地理/气候类型.light: 函数体调用自身: 返回 气候类型(甲, 乙, 丙)
- 地理/气压带.light: 函数体调用自身: 返回 气压带(输入)
- 地理/气温.light: 函数体调用自身: 返回 气温(输入)
- 地理/水域覆盖率.light: 函数体调用自身: 返回 水域覆盖率(输入)
- 地理/沙漠面积.light: 函数体调用自身: 返回 沙漠面积(输入)
- 地理/河流长度.light: 函数体调用自身: 返回 河流长度(输入)
- 地理/泥石流危险.light: 函数体调用自身: 返回 泥石流危险(输入)
- 地理/泰森多边形.light: 函数体调用自身: 返回 泰森多边形(输入)
- 地理/洪水水位.light: 函数体调用自身: 返回 洪水水位(输入)
- 地理/洪水频率.light: 函数体调用自身: 返回 洪水频率(输入)
- 地理/流域面积.light: 函数体调用自身: 返回 流域面积(输入)
- 地理/海啸到达时间.light: 函数体调用自身: 返回 海啸到达时间(输入)
- 地理/海啸波高.light: 函数体调用自身: 返回 海啸波高(输入)
- 地理/海岸线长度.light: 函数体调用自身: 返回 海岸线长度(输入)
- 地理/海拔.light: 函数体调用自身: 返回 海拔(输入)
- 地理/海拔分区.light: 函数体调用自身: 返回 海拔分区(输入)
- 地理/海拔高度.light: 函数体调用自身: 返回 海拔高度(输入)
- 地理/海洋面积.light: 函数体调用自身: 返回 海洋面积(输入)
- 地理/湖泊面积.light: 函数体调用自身: 返回 湖泊面积(输入)
- 地理/滑坡风险.light: 函数体调用自身: 返回 滑坡风险(输入)
- 地理/火灾危险指数.light: 函数体调用自身: 返回 火灾危险指数(输入)
- 地理/点到多边形距离.light: 函数体调用自身: 返回 点到多边形距离(输入)
- 地理/点在多边形内.light: 函数体调用自身: 返回 点在多边形内(输入)
- 地理/电话区号.light: 函数体调用自身: 返回 电话区号(输入)
- 地理/空间插值IDW.light: 函数体调用自身: 返回 空间插值IDW(输入)
- 地理/空间插值克里金.light: 函数体调用自身: 返回 空间插值克里金(输入)
- 地理/等距方位投影.light: 函数体调用自身: 返回 等距方位投影(输入)
- 地理/等高线间距.light: 函数体调用自身: 返回 等高线间距(输入)
- 地理/纬度.light: 函数体调用自身: 返回 纬度(输入)
- 地理/终点坐标.light: 函数体调用自身: 返回 终点坐标(输入)
- 地理/经度.light: 函数体调用自身: 返回 经度(输入)
- 地理/经纬度格式化.light: 函数体调用自身: 返回 经纬度格式化(输入)
- 地理/缓冲区分析.light: 函数体调用自身: 返回 缓冲区分析(输入)
- 地理/至日.light: 函数体调用自身: 返回 至日(输入)
- 地理/行政区划.light: 函数体调用自身: 返回 行政区划(输入)
- 地理/逆地址解析.light: 函数体调用自身: 返回 逆地址解析(输入)
- 地理/邮政编码.light: 函数体调用自身: 返回 邮政编码(输入)
- 地理/阿尔伯斯投影.light: 函数体调用自身: 返回 阿尔伯斯投影(输入)
- 地理/陆地面积.light: 函数体调用自身: 返回 陆地面积(输入)
- 地理/风带.light: 函数体调用自身: 返回 风带(输入)
- 地理/高斯克吕格反投影.light: 函数体调用自身: 返回 高斯克吕格反投影(输入)
- 地理/高斯克吕格投影.light: 函数体调用自身: 返回 高斯克吕格投影(输入)

### StubBlock（10 处）

- 化学/密度化学.light: 仅一行调用: 返回 密度(输入)
- 化学/沉淀平衡.light: 仅一行调用: 返回 平方根(输入)
- 化学/配位数化学.light: 仅一行调用: 返回 配位数(输入)
- 化学/颜色化学.light: 仅一行调用: 返回 颜色(输入)
- 地理/时区地理.light: 仅一行调用: 返回 时区(甲, 乙)
- 地理/海拔地理.light: 仅一行调用: 返回 海拔(输入)
- 地理/热点分析.light: 仅一行调用: 返回 热点(输入)
- 地理/莫兰指数.light: 仅一行调用: 返回 莫兰(输入)
- 地理/赫芬达尔.light: 仅一行调用: 返回 HHI(输入)
- 地理/降水量地理.light: 仅一行调用: 返回 降水量(输入)

