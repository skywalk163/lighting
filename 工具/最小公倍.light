# 积木：最小公倍（工具领域 L0）
# 契约：输入 数, 数 → 输出 数（最小公倍数）
导出 最小公倍
段落 约数辅 接收 甲入, 乙入：
    设 前项 为 整数(绝对值(甲入))
    设 后项 为 整数(绝对值(乙入))
    当 后项 大于 0：
        设 余项 为 前项 模 后项
        设 前项 为 后项
        设 后项 为 余项
    返回 前项

段落 最小公倍 接收 左数, 右数：
    设 左量 为 整数(绝对值(左数))
    设 右量 为 整数(绝对值(右数))
    如果 左量 等于 0：
        返回 0
    如果 右量 等于 0：
        返回 0
    返回 整数(左量 乘 右量 除 约数辅(左量, 右量))
