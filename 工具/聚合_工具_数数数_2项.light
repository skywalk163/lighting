# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_数数数_2项
# 描述：自动织成：对数输入并行执行 生成范围、数值限幅，返回结果列表

# ── 积木：生成范围（工具）──
段落 区间 接收 起, 止, 步：
    设 结果 为 []
    如果 步 等于 0：
        返回 结果
    设 i 为 起
    如果 步 大于 0：
        当 i 小于 止：
            结果.追加(i)
            设 i 为 i 加 步
    否则：
        当 i 大于 止：
            结果.追加(i)
            设 i 为 i 加 步
    返回 结果

# ── 积木：数值限幅（工具）──
段落 数值限幅 接收 数入, 下界, 上界：
    如果 数入 小于 下界：
        返回 下界
    如果 数入 大于 上界：
        返回 上界
    返回 数入

段落 聚合_工具_数数数_2项 接收 起, 止, 步：
    设 赵果1 为 区间(起, 止, 步)。
    设 赵果2 为 数值限幅(起, 止, 步)。
    返回 [赵果1, 赵果2]。

导出 聚合_工具_数数数_2项
