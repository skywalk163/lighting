# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_列表_3项
# 描述：自动织成：对列表[任意]输入并行执行 排序列表、取唯一、聚合_工具_列表_2项，返回结果列表

# ── 积木：排序列表（工具）──
段落 排列 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(列表[i])
        设 i 为 i 加 1
    结果.排序()
    返回 结果

# ── 积木：取唯一（工具）──
段落 去重 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        设 已有 为 0
        设 j 为 0
        当 j 小于 长度(结果)：
            如果 结果[j] 等于 列表[i]：
                设 已有 为 1
            设 j 为 j 加 1
        如果 已有 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果

# ── 积木：聚合_工具_列表_2项（工具）──
段落 偶筛 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 (列表[i] 模 2) 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果

段落 奇筛 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 (列表[i] 模 2) 不等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果

段落 聚合_工具_列表_2项 接收 列表：
    设 赵果1 为 偶筛(列表)。
    设 赵果2 为 奇筛(列表)。
    返回 [赵果1, 赵果2]。

段落 聚合_工具_列表_3项 接收 列表：
    设 赵果1 为 排列(列表)。
    设 赵果2 为 去重(列表)。
    设 赵果3 为 聚合_工具_列表_2项(列表)。
    返回 [赵果1, 赵果2, 赵果3]。

导出 聚合_工具_列表_3项
