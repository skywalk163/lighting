# 积木：生成范围（工具领域 L0）
# 契约：输入 起, 止, 步 → 输出 列表（左闭右开的等差数列，步可为负）
导出 区间
段落 区间 接收 起, 止, 步：
    设 结果 为 []
    如果 步 等于 0：
        返回 结果
    设 i 为 起
    如果 步 大于 0：
        当 i 小于 止：
            结果.追加(i)
            设 i 为 i 加 步
    否则：
        当 i 大于 止：
            结果.追加(i)
            设 i 为 i 加 步
    返回 结果
