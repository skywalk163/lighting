# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_3项
# 描述：自动织成：对数输入并行执行 分页参数、取余数、求幂，返回结果列表

# ── 积木：分页参数（网络）──
段落 分页 接收 总数, 每页：
    设 结果 为 []
    设 i 为 0
    当 (i 乘 每页) 小于 总数：
        结果.追加(i 乘 每页)
        设 i 为 i 加 1
    返回 结果

# ── 积木：取余数（工具）──
段落 余 接收 甲, 乙：
    返回 甲 模 乙

# ── 积木：求幂（工具）──
段落 乘方 接收 底, 指：
    返回 底 幂 指

段落 聚合_工具_3项 接收 总数：
    设 赵果1 为 分页(总数)。
    设 赵果2 为 余(总数)。
    设 赵果3 为 乘方(总数)。
    返回 [赵果1, 赵果2, 赵果3]。

导出 聚合_工具_3项
