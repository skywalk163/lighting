# 积木：取唯一（工具领域 L0）
# 契约：输入 列表 → 输出 列表（去掉重复元素，保留首次出现的顺序）
导出 去重
段落 去重 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        设 已有 为 0
        设 j 为 0
        当 j 小于 长度(结果)：
            如果 结果[j] 等于 列表[i]：
                设 已有 为 1
            设 j 为 j 加 1
        如果 已有 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
