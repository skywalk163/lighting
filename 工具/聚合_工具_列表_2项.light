# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_列表_2项
# 描述：自动织成：对列表[数]输入并行执行 取偶数、取奇数，返回结果列表

# ── 积木：取偶数（工具）──
段落 偶筛 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 (列表[i] 模 2) 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果

# ── 积木：取奇数（工具）──
段落 奇筛 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 (列表[i] 模 2) 不等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果

段落 聚合_工具_列表_2项 接收 列表：
    设 赵果1 为 偶筛(列表)。
    设 赵果2 为 奇筛(列表)。
    返回 [赵果1, 赵果2]。

导出 聚合_工具_列表_2项
