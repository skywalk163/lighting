# 积木：奇偶互换（工具领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（工具：奇偶互换）
导出 奇偶互换
段落 奇偶互换 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 加 1 小于 长度(列表)：
        结果.追加(列表[i 加 1])
        结果.追加(列表[i])
        设 i 为 i 加 2
    如果 长度(列表) 模 2 不等于 0：
        结果.追加(列表[长度(列表) 减 1])
    返回 结果
