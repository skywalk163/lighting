# 积木：取偶数（工具领域 L0）
# 契约：输入 列表 → 输出 列表（只保留能被 2 整除的元素）
导出 偶筛
段落 偶筛 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 (列表[i] 模 2) 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
