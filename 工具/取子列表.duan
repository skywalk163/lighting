# 积木：取子列表（工具领域 L0）
# 契约：输入 列表, 起, 止 → 输出 列表（索引 起..止-1 的元素，越界自动裁剪）
导出 切片
段落 切片 接收 列表, 起, 止：
    设 结果 为 []
    设 总长 为 长度(列表)
    设 头 为 起
    设 尾 为 止
    如果 头 小于 0：
        设 头 为 0
    如果 尾 大于 总长：
        设 尾 为 总长
    设 i 为 头
    当 i 小于 尾：
        结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
