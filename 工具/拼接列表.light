# 积木：拼接列表（工具领域 L0）
# 契约：输入 表甲, 表乙 → 输出 列表（首尾相接的新表，不改动原表）
导出 并表
段落 并表 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲)：
        结果.追加(表甲[i])
        设 i 为 i 加 1
    设 j 为 0
    当 j 小于 长度(表乙)：
        结果.追加(表乙[j])
        设 j 为 j 加 1
    返回 结果
