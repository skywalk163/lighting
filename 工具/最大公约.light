# 积木：最大公约（工具领域 L0）
# 契约：输入 数, 数 → 输出 数（辗转相除求最大公约数）
导出 最大公约
段落 最大公约 接收 甲入, 乙入：
    设 前项 为 整数(绝对值(甲入))
    设 后项 为 整数(绝对值(乙入))
    当 后项 大于 0：
        设 余项 为 前项 模 后项
        设 前项 为 后项
        设 后项 为 余项
    返回 前项
