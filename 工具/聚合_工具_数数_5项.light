# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_数数_5项
# 描述：自动织成：对数输入并行执行 取余数、求幂、聚合_工具_数数_2项、最大公约、最小公倍，返回结果列表

# ── 积木：取余数（工具）──
段落 余 接收 甲, 乙：
    返回 甲 模 乙

# ── 积木：求幂（工具）──
段落 乘方 接收 底, 指：
    返回 底 幂 指

# ── 积木：聚合_工具_数数_2项（工具）──
段落 余 接收 甲, 乙：
    返回 甲 模 乙

段落 乘方 接收 底, 指：
    返回 底 幂 指

段落 聚合_工具_数数_2项 接收 甲, 乙：
    设 赵果1 为 余(甲, 乙)。
    设 赵果2 为 乘方(甲, 乙)。
    返回 [赵果1, 赵果2]。

# ── 积木：最大公约（工具）──
段落 最大公约 接收 甲入, 乙入：
    设 前项 为 整数(绝对值(甲入))
    设 后项 为 整数(绝对值(乙入))
    当 后项 大于 0：
        设 余项 为 前项 模 后项
        设 前项 为 后项
        设 后项 为 余项
    返回 前项

# ── 积木：最小公倍（工具）──
段落 约数辅 接收 甲入, 乙入：
    设 前项 为 整数(绝对值(甲入))
    设 后项 为 整数(绝对值(乙入))
    当 后项 大于 0：
        设 余项 为 前项 模 后项
        设 前项 为 后项
        设 后项 为 余项
    返回 前项

段落 最小公倍 接收 左数, 右数：
    设 左量 为 整数(绝对值(左数))
    设 右量 为 整数(绝对值(右数))
    如果 左量 等于 0：
        返回 0
    如果 右量 等于 0：
        返回 0
    返回 整数(左量 乘 右量 除 约数辅(左量, 右量))

段落 聚合_工具_数数_5项 接收 甲, 乙：
    设 赵果1 为 余(甲, 乙)。
    设 赵果2 为 乘方(甲, 乙)。
    设 赵果3 为 聚合_工具_数数_2项(甲, 乙)。
    设 赵果4 为 最大公约(甲, 乙)。
    设 赵果5 为 最小公倍(甲, 乙)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4, 赵果5]。

导出 聚合_工具_数数_5项
