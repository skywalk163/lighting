# -*- coding: utf-8 -*-
"""光明积木 · 函数领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 恒等函数 ──
# 输入: ['数'] → 输出: 数
# 积木：恒等函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：恒等函数）
def 恒等(输入):
    return 输入

# ── 常值函数 ──
# 输入: ['数'] → 输出: 数
# 积木：常值函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：常值函数）
def 常值(输入):
    return 1

# ── 递增函数 ──
# 输入: ['数'] → 输出: 数
# 积木：递增函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：递增函数）
def 递增(输入):
    return 输入 + 1

# ── 递减函数 ──
# 输入: ['数'] → 输出: 数
# 积木：递减函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：递减函数）
def 递减(输入):
    return 输入 - 1

# ── 平方函数 ──
# 输入: ['数'] → 输出: 数
# 积木：平方函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：平方函数）
def 平方(输入):
    return 输入 * 输入

# ── 立方函数 ──
# 输入: ['数'] → 输出: 数
# 积木：立方函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：立方函数）
def 立方(输入):
    return 输入 * 输入 * 输入

# ── 双倍函数 ──
# 输入: ['数'] → 输出: 数
# 积木：双倍函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：双倍函数）
def 双倍(输入):
    return 输入 * 2

# ── 半数函数 ──
# 输入: ['数'] → 输出: 数
# 积木：半数函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：半数函数）
def 半数(输入):
    return 输入 / 2

# ── 取反函数 ──
# 输入: ['数'] → 输出: 数
# 积木：取反函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：取反函数）
def 取反(输入):
    return 0 - 输入

# ── 补数函数 ──
# 输入: ['数'] → 输出: 数
# 积木：补数函数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数操作：补数函数）
def 补数(输入):
    return 100 - 输入

# ── 函数_恒等 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_恒等（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_恒等）
def 恒等(输入):
    return 输入

# ── 函数_常值 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_常值（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_常值）
def 常值(输入):
    return 1

# ── 函数_递增 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_递增（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_递增）
def 递增(输入):
    return 输入 + 1

# ── 函数_递减 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_递减（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_递减）
def 递减(输入):
    return 输入 - 1

# ── 函数_平方 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_平方（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_平方）
def 平方(输入):
    return 输入 * 输入

# ── 函数_立方 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_立方（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_立方）
def 立方(输入):
    return 输入 * 输入 * 输入

# ── 函数_双倍 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_双倍（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_双倍）
def 双倍(输入):
    return 输入 * 2

# ── 函数_半数 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_半数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_半数）
def 半数(输入):
    return 输入 / 2

# ── 函数_取反 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_取反（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_取反）
def 取反(输入):
    return 0 - 输入

# ── 函数_补数 ──
# 输入: ['数'] → 输出: 数
# 积木：函数_补数（函数领域，自动生成）
# 契约：输入 [数] → 输出 数（函数高级操作：函数_补数）
def 补数(输入):
    return 100 - 输入
