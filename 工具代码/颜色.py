# -*- coding: utf-8 -*-
"""光明积木 · 颜色领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── RGB转灰度 ──
# 输入: ['数'] → 输出: 数
# 积木：RGB转灰度（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：RGB转灰度）
def 转灰度(输入):
    return (红 * 0.299 + 绿 * 0.587 + 蓝 * 0.114)

# ── 亮度计算 ──
# 输入: ['数'] → 输出: 数
# 积木：亮度计算（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：亮度计算）
def 亮度(输入):
    return 输入 * 0.299

# ── 对比度调整 ──
# 输入: ['数'] → 输出: 数
# 积木：对比度调整（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：对比度调整）
def 对比度(输入):
    return 输入 * 1.5

# ── 饱和度调整 ──
# 输入: ['数'] → 输出: 数
# 积木：饱和度调整（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：饱和度调整）
def 饱和度(输入):
    return 输入 * 1.2

# ── 色相偏移 ──
# 输入: ['数'] → 输出: 数
# 积木：色相偏移（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：色相偏移）
def 色相(输入):
    return 输入 + 30

# ── 颜色反转 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色反转（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：颜色反转）
def 反色(输入):
    return 255 - 输入

# ── 颜色增强 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色增强（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：颜色增强）
def 增强(输入):
    return 输入 * 1.2

# ── 颜色减弱 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色减弱（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：颜色减弱）
def 减弱(输入):
    return 输入 * 0.8

# ── 暖色滤镜 ──
# 输入: ['数'] → 输出: 数
# 积木：暖色滤镜（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：暖色滤镜）
def 暖色(输入):
    return 输入 * 1.1

# ── 冷色滤镜 ──
# 输入: ['数'] → 输出: 数
# 积木：冷色滤镜（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：冷色滤镜）
def 冷色(输入):
    return 输入 * 0.9

# ── 红色通道 ──
# 输入: ['数'] → 输出: 数
# 积木：红色通道（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：红色通道）
def 红色通道(输入):
    return 输入 / 65536 % 256

# ── 绿色通道 ──
# 输入: ['数'] → 输出: 数
# 积木：绿色通道（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：绿色通道）
def 绿色通道(输入):
    return 输入 / 256 % 256

# ── 蓝色通道 ──
# 输入: ['数'] → 输出: 数
# 积木：蓝色通道（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：蓝色通道）
def 蓝色通道(输入):
    return 输入 % 256

# ── 透明度通道 ──
# 输入: ['数'] → 输出: 数
# 积木：透明度通道（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：透明度通道）
def 透明度通道(输入):
    return 输入 / 16777216

# ── RGB合成 ──
# 输入: ['数'] → 输出: 数
# 积木：RGB合成（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：RGB合成）
def RGB合成(输入):
    return 红 * 65536 + 绿 * 256 + 蓝

# ── 亮度调整 ──
# 输入: ['数'] → 输出: 数
# 积木：亮度调整（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：亮度调整）
def 亮度调整(输入):
    return 输入 * 0.299 + 绿 * 0.587 + 蓝 * 0.114

# ── 对比度增强 ──
# 输入: ['数'] → 输出: 数
# 积木：对比度增强（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：对比度增强）
def 对比度增强(输入):
    return (输入 - 128) * 1.2 + 128

# ── 对比度减弱 ──
# 输入: ['数'] → 输出: 数
# 积木：对比度减弱（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：对比度减弱）
def 对比度减弱(输入):
    return (输入 - 128) * 0.8 + 128

# ── 灰度化 ──
# 输入: ['数'] → 输出: 数
# 积木：灰度化（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：灰度化）
def 灰度化(输入):
    return int(输入 * 0.299 + 绿 * 0.587 + 蓝 * 0.114)

# ── 二值化 ──
# 输入: ['数'] → 输出: 数
# 积木：二值化（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：二值化）
def 二值化(输入):
    return 255 if 输入 > 128 else 0

# ── 颜色加深 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色加深（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：颜色加深）
def 颜色加深(输入):
    return int(输入 * 0.8)

# ── 颜色减淡 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色减淡（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：颜色减淡）
def 颜色减淡(输入):
    return int(输入 * 1.2)

# ── 暖色调 ──
# 输入: ['数'] → 输出: 数
# 积木：暖色调（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：暖色调）
def 暖色调(输入):
    return int(输入 * 1.1)

# ── 冷色调 ──
# 输入: ['数'] → 输出: 数
# 积木：冷色调（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：冷色调）
def 冷色调(输入):
    return int(输入 * 0.9)

# ── 复古色 ──
# 输入: ['数'] → 输出: 数
# 积木：复古色（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：复古色）
def 复古色(输入):
    return int(输入 * 0.85 + 30)

# ── 褪色 ──
# 输入: ['数'] → 输出: 数
# 积木：褪色（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：褪色）
def 褪色(输入):
    return int(输入 * 0.7 + 50)

# ── 饱和度增强 ──
# 输入: ['数'] → 输出: 数
# 积木：饱和度增强（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：饱和度增强）
def 饱和度增强(输入):
    return 输入 * 1.3

# ── 饱和度减弱 ──
# 输入: ['数'] → 输出: 数
# 积木：饱和度减弱（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：饱和度减弱）
def 饱和度减弱(输入):
    return 输入 * 0.7

# ── 色调分离 ──
# 输入: ['数'] → 输出: 数
# 积木：色调分离（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色操作：色调分离）
def 色调分离(输入):
    return int(输入 / 64) * 64

# ── 颜色_HEX转RGB ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_HEX转RGB（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_HEX转RGB）
def HEX转RGB(输入):
    return int(输入 / 65536)

# ── 颜色_RGB转HEX ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_RGB转HEX（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_RGB转HEX）
def RGB转HEX(输入):
    return 红 * 65536 + 绿 * 256 + 蓝

# ── 颜色_HSL转RGB ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_HSL转RGB（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_HSL转RGB）
def HSL转RGB(输入):
    return 输入

# ── 颜色_RGB转HSL ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_RGB转HSL（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_RGB转HSL）
def RGB转HSL(输入):
    return 输入

# ── 颜色_CMYK转RGB ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_CMYK转RGB（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_CMYK转RGB）
def CMYK转RGB(输入):
    return 输入

# ── 颜色_RGB转CMYK ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_RGB转CMYK（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_RGB转CMYK）
def RGB转CMYK(输入):
    return 输入

# ── 颜色_色温转换 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_色温转换（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_色温转换）
def 色温转换(输入):
    return 输入

# ── 颜色_亮度标准化 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_亮度标准化（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_亮度标准化）
def 亮度标准化(输入):
    return 输入 / 255

# ── 颜色_对比度标准化 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_对比度标准化（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_对比度标准化）
def 对比度标准化(输入):
    return (输入 - 128) / 128

# ── 颜色_伽马校正 ──
# 输入: ['数'] → 输出: 数
# 积木：颜色_伽马校正（颜色领域，自动生成）
# 契约：输入 [数] → 输出 数（颜色高级操作：颜色_伽马校正）
def 伽马校正(输入):
    return 输入 ** (1 / 2.2)
