# -*- coding: utf-8 -*-
"""光明积木 · 文件领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 文件路径拼接 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件路径拼接（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：文件路径拼接）
def 路径拼(文本):
    return 文本 + '/' + 子

# ── 文件扩展名 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件扩展名（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：文件扩展名）
def 扩展名(文本):
    return 文本.split('.')[-1]

# ── 文件名去扩展 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件名去扩展（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：文件名去扩展）
def 去扩展(文本):
    return 文本.split('.')[0]

# ── 文件大小格式化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件大小格式化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：文件大小格式化）
def 大小格(文本):
    return 文本(输入 / 1024) + 'KB'

# ── 目录路径 ──
# 输入: ['文本'] → 输出: 文本
# 积木：目录路径（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：目录路径）
def 目录(文本):
    return 文本.split('/')[0:-1].join('/')

# ── 文件名提取 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件名提取（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：文件名提取）
def 文件名(文本):
    return 文本.split('/')[-1]

# ── 路径规范化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：路径规范化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件操作：路径规范化）
def 路径规(文本):
    return 文本.replace('\\', '/')

# ── 文件计数 ──
# 输入: ['文本'] → 输出: 数
# 积木：文件计数（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件操作：文件计数）
def 文件计(文本):
    return 输入

# ── 目录计数 ──
# 输入: ['文本'] → 输出: 数
# 积木：目录计数（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件操作：目录计数）
def 目录计(文本):
    return 输入

# ── 文件大小计算 ──
# 输入: ['文本'] → 输出: 数
# 积木：文件大小计算（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件操作：文件大小计算）
def 大小计(文本):
    return 输入 * 1024

# ── 文件_文件名提取 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_文件名提取（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_文件名提取）
def 文件名提取(文本):
    return 文本.split('/')[-1]

# ── 文件_目录提取 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_目录提取（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_目录提取）
def 目录提取(文本):
    return 文本.split('/')[0:-1].join('/')

# ── 文件_扩展名提取 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_扩展名提取（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_扩展名提取）
def 扩展名提取(文本):
    return 文本.split('.')[-1]

# ── 文件_路径规范化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_路径规范化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_路径规范化）
def 路径规范化(文本):
    return 文本.replace('\\', '/')

# ── 文件_路径绝对化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_路径绝对化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_路径绝对化）
def 路径绝对化(文本):
    return 文本

# ── 文件_路径相对化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_路径相对化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_路径相对化）
def 路径相对化(文本):
    return 文本

# ── 文件_文件大小格式化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：文件_文件大小格式化（文件领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文件高级操作：文件_文件大小格式化）
def 文件大小格式化(文本):
    return 文本(输入 / 1024) + 'KB'

# ── 文件_文件大小转字节 ──
# 输入: ['文本'] → 输出: 数
# 积木：文件_文件大小转字节（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件高级操作：文件_文件大小转字节）
def 文件大小转字节(文本):
    return 输入 * 1024

# ── 文件_文件计数 ──
# 输入: ['文本'] → 输出: 数
# 积木：文件_文件计数（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件高级操作：文件_文件计数）
def 文件计数(文本):
    return 输入

# ── 文件_目录计数 ──
# 输入: ['文本'] → 输出: 数
# 积木：文件_目录计数（文件领域，自动生成）
# 契约：输入 [文本] → 输出 数（文件高级操作：文件_目录计数）
def 目录计数(文本):
    return 输入
