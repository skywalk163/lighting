# -*- coding: utf-8 -*-
"""光明积木 · 时间领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 秒转分钟 ──
# 输入: ['数'] → 输出: 数
# 积木：秒转分钟（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：秒转分钟）
def 秒转分(输入):
    return 输入 / 60

# ── 分钟转小时 ──
# 输入: ['数'] → 输出: 数
# 积木：分钟转小时（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：分钟转小时）
def 分转时(输入):
    return 输入 / 60

# ── 小时转天 ──
# 输入: ['数'] → 输出: 数
# 积木：小时转天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：小时转天）
def 时转天(输入):
    return 输入 / 24

# ── 天转小时 ──
# 输入: ['数'] → 输出: 数
# 积木：天转小时（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转小时）
def 天转时(输入):
    return 输入 * 24

# ── 小时转分钟 ──
# 输入: ['数'] → 输出: 数
# 积木：小时转分钟（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：小时转分钟）
def 时转分(输入):
    return 输入 * 60

# ── 分钟转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：分钟转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：分钟转秒）
def 分转秒(输入):
    return 输入 * 60

# ── 天转周 ──
# 输入: ['数'] → 输出: 数
# 积木：天转周（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转周）
def 天转周(输入):
    return 输入 / 7

# ── 周转天 ──
# 输入: ['数'] → 输出: 数
# 积木：周转天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：周转天）
def 周转天(输入):
    return 输入 * 7

# ── 年转月 ──
# 输入: ['数'] → 输出: 数
# 积木：年转月（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：年转月）
def 年转月(输入):
    return 输入 * 12

# ── 月转年 ──
# 输入: ['数'] → 输出: 数
# 积木：月转年（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：月转年）
def 月转年(输入):
    return 输入 / 12

# ── 月转天 ──
# 输入: ['数'] → 输出: 数
# 积木：月转天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：月转天）
def 月转天(输入):
    return 输入 * 30

# ── 天转月 ──
# 输入: ['数'] → 输出: 数
# 积木：天转月（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转月）
def 天转月(输入):
    return 输入 / 30

# ── 年转天 ──
# 输入: ['数'] → 输出: 数
# 积木：年转天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：年转天）
def 年转天(输入):
    return 输入 * 365

# ── 天转年 ──
# 输入: ['数'] → 输出: 数
# 积木：天转年（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转年）
def 天转年(输入):
    return 输入 / 365

# ── 秒转毫秒 ──
# 输入: ['数'] → 输出: 数
# 积木：秒转毫秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：秒转毫秒）
def 秒转毫(输入):
    return 输入 * 1000

# ── 毫秒转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：毫秒转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：毫秒转秒）
def 毫转秒(输入):
    return 输入 / 1000

# ── 小时转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：小时转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：小时转秒）
def 时转秒(输入):
    return 输入 * 3600

# ── 天转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：天转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转秒）
def 天转秒(输入):
    return 输入 * 86400

# ── 周转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：周转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：周转秒）
def 周转秒(输入):
    return 输入 * 604800

# ── 月转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：月转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：月转秒）
def 月转秒(输入):
    return 输入 * 2592000

# ── 年转秒 ──
# 输入: ['数'] → 输出: 数
# 积木：年转秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：年转秒）
def 年转秒(输入):
    return 输入 * 31536000

# ── 秒转分钟取整 ──
# 输入: ['数'] → 输出: 数
# 积木：秒转分钟取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：秒转分钟取整）
def 秒转整分(输入):
    return int(输入 / 60)

# ── 分钟转小时取整 ──
# 输入: ['数'] → 输出: 数
# 积木：分钟转小时取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：分钟转小时取整）
def 分转整时(输入):
    return int(输入 / 60)

# ── 小时转天取整 ──
# 输入: ['数'] → 输出: 数
# 积木：小时转天取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：小时转天取整）
def 时转整天(输入):
    return int(输入 / 24)

# ── 天转周取整 ──
# 输入: ['数'] → 输出: 数
# 积木：天转周取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天转周取整）
def 天转整周(输入):
    return int(输入 / 7)

# ── 月转年取整 ──
# 输入: ['数'] → 输出: 数
# 积木：月转年取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：月转年取整）
def 月转整年(输入):
    return int(输入 / 12)

# ── 秒取余 ──
# 输入: ['数'] → 输出: 数
# 积木：秒取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：秒取余）
def 秒余(输入):
    return 输入 % 60

# ── 分钟取余 ──
# 输入: ['数'] → 输出: 数
# 积木：分钟取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：分钟取余）
def 分余(输入):
    return 输入 % 60

# ── 小时取余 ──
# 输入: ['数'] → 输出: 数
# 积木：小时取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：小时取余）
def 时余(输入):
    return 输入 % 24

# ── 天取余 ──
# 输入: ['数'] → 输出: 数
# 积木：天取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：天取余）
def 天余(输入):
    return 输入 % 7

# ── 月取余 ──
# 输入: ['数'] → 输出: 数
# 积木：月取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：月取余）
def 月余(输入):
    return 输入 % 12

# ── 时间戳转日期 ──
# 输入: ['数'] → 输出: 数
# 积木：时间戳转日期（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：时间戳转日期）
def 时间戳转日期(输入):
    return 输入

# ── 日期转时间戳 ──
# 输入: ['数'] → 输出: 数
# 积木：日期转时间戳（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间转换：日期转时间戳）
def 日期转时间戳(输入):
    return 输入

# ── 时间_秒转时分秒 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_秒转时分秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（秒数转为时分秒格式）
def 秒转时分秒(输入):
    return 文本(int(输入 / 3600)) + ':' + 文本(int((输入 % 3600) / 60)) + ':' + 文本(输入 % 60)

# ── 时间_分钟转时分 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_分钟转时分（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（分钟转为小时分钟）
def 分钟转时分(输入):
    return 文本(int(输入 / 60)) + '时' + 文本(输入 % 60) + '分'

# ── 时间_小时转天时 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_小时转天时（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（小时转为天小时）
def 小时转天时(输入):
    return 文本(int(输入 / 24)) + '天' + 文本(输入 % 24) + '时'

# ── 时间_天转周天 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_天转周天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（天数转为周天）
def 天转周天(输入):
    return 文本(int(输入 / 7)) + '周' + 文本(输入 % 7) + '天'

# ── 时间_年转年月 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_年转年月（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（年数转为年月）
def 年转年月(输入):
    return 文本(int(输入)) + '年' + 文本(int((输入 - int(输入)) * 12)) + '月'

# ── 时间_时间差 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间差（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（两时间差值）
def 时间差(输入):
    return 甲 - 乙

# ── 时间_时间比较 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间比较（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（比较两时间大小）
def 时间比较(输入):
    return 1 if 甲 > 乙 else ((0 - 1) if 甲 < 乙 else 0)

# ── 时间_时间排序 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间排序（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（多个时间排序）
def 时间排序(输入):
    return 列表.sort()

# ── 时间_时间取整 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间取整到天）
def 时间取整(输入):
    return int(输入 / 86400) * 86400

# ── 时间_时间取余 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（取时间余数）
def 时间取余(输入):
    return 输入 % 86400

# ── 时间_时间戳比较 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳比较（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（比较两个时间戳）
def 时间戳比较(输入):
    return '>' if 甲 > 乙 else ('<' if 甲 < 乙 else '==')

# ── 时间_时间戳排序 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳排序（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（排序时间戳列表）
def 时间戳排序(输入):
    return 列表.sort()

# ── 时间_时间戳范围 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳范围（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（判断时间戳是否在范围内）
def 时间戳范围(输入):
    return '在范围内' if 输入 >= 起 and 输入 <= 止 else '超出范围'

# ── 时间_时间戳差值 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳差值（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（计算时间戳差值）
def 时间戳差值(输入):
    return 甲 - 乙

# ── 时间_时间戳取整 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取整（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（取整到小时）
def 时间戳取整(输入):
    return int(输入 / 3600) * 3600

# ── 时间_时间戳取余 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取余（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（取余到秒）
def 时间戳取余(输入):
    return 输入 % 60

# ── 时间_时间戳分组 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳分组（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（按时间分组）
def 时间戳分组(输入):
    return 输入 / 86400

# ── 时间_时间戳窗口 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳窗口（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（滑动时间窗口）
def 时间戳窗口(输入):
    return 输入 / 3600

# ── 时间_时间戳聚合 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳聚合（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（聚合时间戳）
def 时间戳聚合(输入):
    return 输入 / 86400

# ── 时间_时间戳过滤 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳过滤（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（过滤时间戳）
def 时间戳过滤(输入):
    return 输入 > 阈值

# ── 时间_时间戳格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：时间_时间戳格式化（时间领域，自动生成）
# 契约：输入 [数] → 输出 文本（时间高级操作：时间_时间戳格式化）
def 时间戳格式化(输入):
    return 文本(输入)

# ── 时间_时间戳解析 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳解析（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳解析）
def 时间戳解析(输入):
    return int(输入)

# ── 时间_时间戳加法 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳加法（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳加法）
def 时间戳加法(输入):
    return 甲 + 乙

# ── 时间_时间戳减法 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳减法（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳减法）
def 时间戳减法(输入):
    return 甲 - 乙

# ── 时间_时间戳乘法 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳乘法（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳乘法）
def 时间戳乘法(输入):
    return 输入 * 倍数

# ── 时间_时间戳除法 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳除法（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳除法）
def 时间戳除法(输入):
    return 输入 / 除数

# ── 时间_时间戳取整到分 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取整到分（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳取整到分）
def 时间戳取整到分(输入):
    return int(输入 / 60) * 60

# ── 时间_时间戳取整到时 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取整到时（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳取整到时）
def 时间戳取整到时(输入):
    return int(输入 / 3600) * 3600

# ── 时间_时间戳取整到天 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取整到天（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳取整到天）
def 时间戳取整到天(输入):
    return int(输入 / 86400) * 86400

# ── 时间_时间戳取整到周 ──
# 输入: ['数'] → 输出: 数
# 积木：时间_时间戳取整到周（时间领域，自动生成）
# 契约：输入 [数] → 输出 数（时间高级操作：时间_时间戳取整到周）
def 时间戳取整到周(输入):
    return int(输入 / 604800) * 604800
