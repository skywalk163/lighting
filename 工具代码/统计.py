# -*- coding: utf-8 -*-
"""光明积木 · 统计领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 求积 ──
# 输入: ['列表'] → 输出: 数
# 积木：求积（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表求积）
def 积(列表):
    结果 = 1
    i = 0
    while i < len(列表):
        结果 = 结果 * 列表[i]
        i = i + 1
    return 结果

# ── 计数非零 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数非零（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表计数非零）
def 非零数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] != 0 else 结果
        i = i + 1
    return 结果

# ── 计数正数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数正数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表计数正数）
def 正数数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] > 0 else 结果
        i = i + 1
    return 结果

# ── 计数负数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数负数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表计数负数）
def 负数数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] < 0 else 结果
        i = i + 1
    return 结果

# ── 计数偶数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数偶数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数偶数）
def 偶数计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] % 2 == 0 else 结果
        i = i + 1
    return 结果

# ── 计数奇数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数奇数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数奇数）
def 奇数计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] % 2 != 0 else 结果
        i = i + 1
    return 结果

# ── 计数零 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数零（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数零）
def 零计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] == 0 else 结果
        i = i + 1
    return 结果

# ── 计数大数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数大数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数大数）
def 大数计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] > 10 else 结果
        i = i + 1
    return 结果

# ── 计数小数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数小数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数小数）
def 小数计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] < 1 else 结果
        i = i + 1
    return 结果

# ── 计数范围 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数范围（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计操作：计数范围）
def 范围计数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 + 1 if 列表[i] > 0 and 列表[i] < 100 else 结果
        i = i + 1
    return 结果

# ── 统计_偏度 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_偏度（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_偏度）
def 偏度(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 统计_峰度 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_峰度（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_峰度）
def 峰度(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i] * 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 统计_协方差 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_协方差（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_协方差）
def 协方差(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + (列表[i] - 均值) * (列表2[i] - 均值2)
        i = i + 1
    return 结果

# ── 统计_相关系数 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_相关系数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_相关系数）
def 相关系数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表2[i]
        i = i + 1
    return 结果

# ── 统计_决定系数 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_决定系数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_决定系数）
def 决定系数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 统计_调整R方 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_调整R方（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_调整R方）
def 调整R方(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 统计_F统计量 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_F统计量（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_F统计量）
def F统计量(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 统计_t统计量 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_t统计量（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_t统计量）
def t统计量(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 统计_卡方统计量 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_卡方统计量（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_卡方统计量）
def 卡方统计量(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 统计_自由度 ──
# 输入: ['列表'] → 输出: 数
# 积木：统计_自由度（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（统计高级操作：统计_自由度）
def 自由度(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + (len(列表) - 1)
        i = i + 1
    return 结果
