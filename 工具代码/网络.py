# -*- coding: utf-8 -*-
"""光明积木 · 网络领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 分页参数 ──
# 输入: ['数', '数'] → 输出: 列表
# 积木：分页参数（网络领域 L0）
# 契约：输入 数(总数), 数(每页) → 输出 [数]（各页 offset 列表，offset/limit 分页）
def 分页(总数, 每页):
    结果 = []
    i = 0
    while (i * 每页) < 总数:
        结果.append(i * 每页)
        i = i + 1
    return 结果

# ── URL编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL编码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL编码）
def URL编码(文本):
    return 文本.encode(输入)

# ── URL解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL解码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL解码）
def URL解码(文本):
    return 文本.decode(输入)

# ── Base64编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base64编码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：Base64编码）
def B64编码(文本):
    return 文本.encode('base64')

# ── Base64解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base64解码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：Base64解码）
def B64解码(文本):
    return 文本.decode('base64')

# ── HTML转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：HTML转义（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：HTML转义）
def HTML转(文本):
    return 文本.replace('<', '&lt;').replace('>', '&gt;')

# ── HTML反转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：HTML反转义（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：HTML反转义）
def HTML反(文本):
    return 文本.replace('&lt;', '<').replace('&gt;', '>')

# ── URL参数化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL参数化（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL参数化）
def URL参数(文本):
    return 文本.replace(' ', '%20')

# ── URL去参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL去参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL去参数）
def URL去参(文本):
    return 文本.split('?')[0]

# ── 域名提取 ──
# 输入: ['文本'] → 输出: 文本
# 积木：域名提取（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：域名提取）
def 域名(文本):
    return 文本.split('/')[2]

# ── 端口提取 ──
# 输入: ['文本'] → 输出: 数
# 积木：端口提取（网络领域，自动生成）
# 契约：输入 [文本] → 输出 数（网络操作：端口提取）
def 端口(文本):
    return 80

# ── URL拼接 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL拼接（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL拼接）
def URL拼接(输入):
    return 基础 + '/' + 路径

# ── URL添加参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL添加参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL添加参数）
def URL加参(输入):
    return 基础 + '?' + 参数

# ── URL去掉参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL去掉参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL去掉参数）
def URL去参(输入):
    return 基础.split('?')[0]

# ── 提取协议 ──
# 输入: ['文本'] → 输出: 文本
# 积木：提取协议（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：提取协议）
def 提取协议(输入):
    return 输入.split('://')[0]

# ── 提取域名 ──
# 输入: ['文本'] → 输出: 文本
# 积木：提取域名（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：提取域名）
def 提取域名(输入):
    return 输入.split('/')[2]

# ── 提取路径 ──
# 输入: ['文本'] → 输出: 文本
# 积木：提取路径（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：提取路径）
def 提取路径(输入):
    return '/' + 输入.split('/')[3:].join('/')

# ── 提取查询参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：提取查询参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：提取查询参数）
def 提取查询(输入):
    return 输入.split('?')[1]

# ── URL标准化 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL标准化（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL标准化）
def URL标准化(输入):
    return 输入.lower()

# ── URL验证 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL验证（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL验证）
def URL验证(输入):
    return 'http' in 输入

# ── 端口默认值 ──
# 输入: ['文本'] → 输出: 文本
# 积木：端口默认值（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：端口默认值）
def 端口默认(输入):
    return '80' if 输入 == '' else 输入

# ── HTTP状态码分类 ──
# 输入: ['文本'] → 输出: 文本
# 积木：HTTP状态码分类（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：HTTP状态码分类）
def 状态码分类(输入):
    return '信息' if 输入 < 200 else ('成功' if 输入 < 300 else ('重定向' if 输入 < 400 else ('客户端错误' if 输入 < 500 else '服务端错误')))

# ── IP地址验证 ──
# 输入: ['文本'] → 输出: 文本
# 积木：IP地址验证（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：IP地址验证）
def IP验证(输入):
    return len(输入.split('.')) == 4

# ── 端口号验证 ──
# 输入: ['文本'] → 输出: 文本
# 积木：端口号验证（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：端口号验证）
def 端口验证(输入):
    return 输入 > 0 and 输入 < 65536

# ── URL编码空格 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL编码空格（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL编码空格）
def URL编码空格(输入):
    return 输入.replace(' ', '%20')

# ── URL解码空格 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL解码空格（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL解码空格）
def URL解码空格(输入):
    return 输入.replace('%20', ' ')

# ── MIME类型 ──
# 输入: ['文本'] → 输出: 文本
# 积木：MIME类型（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：MIME类型）
def MIME类型(输入):
    return 'text/html' if '.html' in 输入 else ('application/json' if '.json' in 输入 else 'application/octet-stream')

# ── Cookie解析 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Cookie解析（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：Cookie解析）
def Cookie解析(输入):
    return 输入.split(';')

# ── Header解析 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Header解析（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：Header解析）
def Header解析(输入):
    return 输入.split(':')

# ── URL路径层级 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL路径层级（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL路径层级）
def URL层级(输入):
    return len(输入.split('/')) - 1

# ── URL短链接 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL短链接（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络操作：URL短链接）
def URL短链接(输入):
    return 输入[0:30]

# ── 网络_URL解析 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL解析（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（解析URL组成部分）
def URL解析(输入):
    return 文本.split('/')

# ── 网络_URL拼接路径 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL拼接路径（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（拼接URL路径）
def URL拼接路径(输入):
    return 基础 + '/' + 路径

# ── 网络_URL添加查询 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL添加查询（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（添加查询参数）
def URL添加查询(输入):
    return URL + '?' + 键 + '=' + 值

# ── 网络_URL提取域名 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL提取域名（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（从URL提取域名）
def URL提取域名(输入):
    return 文本.split('/')[2]

# ── 网络_URL提取路径 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL提取路径（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（从URL提取路径）
def URL提取路径(输入):
    return 文本.split('/')[3:].join('/')

# ── 网络_URL提取协议 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL提取协议（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（从URL提取协议）
def URL提取协议(输入):
    return 文本.split('://')[0]

# ── 网络_URL提取端口 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL提取端口（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（从URL提取端口）
def URL提取端口(输入):
    return 80

# ── 网络_HTTP状态码判断 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_HTTP状态码判断（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（判断HTTP状态码）
def HTTP状态码判断(输入):
    return 'OK' if 输入 == 200 else ('Not Found' if 输入 == 404 else 'Other')

# ── 网络_HTTP方法判断 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_HTTP方法判断（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（判断HTTP方法）
def HTTP方法判断(输入):
    return '读' if 输入 == 'GET' else ('写' if 输入 == 'POST' else '其他')

# ── 网络_IP地址判断 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_IP地址判断（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（判断是否为IP地址）
def IP地址判断(输入):
    return len(文本.split('.')) == 4

# ── 网络_域名判断 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_域名判断（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（判断是否为域名）
def 域名判断(输入):
    return '.' in 文本

# ── 网络_URL编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL编码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（URL编码）
def URL编码(输入):
    return 文本.encode('url')

# ── 网络_URL解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL解码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（URL解码）
def URL解码(输入):
    return 文本.decode('url')

# ── 网络_Base64编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_Base64编码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Base64编码）
def Base64编码(输入):
    return 文本.encode('base64')

# ── 网络_Base64解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_Base64解码（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Base64解码）
def Base64解码(输入):
    return 文本.decode('base64')

# ── 网络_HTML转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_HTML转义（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（HTML特殊字符转义）
def HTML转义(输入):
    return 文本.replace('<', '&lt;').replace('>', '&gt;')

# ── 网络_HTML反转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_HTML反转义（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（HTML反转义）
def HTML反转义(输入):
    return 文本.replace('&lt;', '<').replace('&gt;', '>')

# ── 网络_Cookie解析 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_Cookie解析（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（解析Cookie字符串）
def Cookie解析(输入):
    return 文本.split(';')

# ── 网络_Header解析 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_Header解析（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（解析HTTP头）
def Header解析(输入):
    return 文本.split(':')

# ── 网络_URL路径拼接 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL路径拼接（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL路径拼接）
def URL路径拼接(输入):
    return 基础 + '/' + 路径

# ── 网络_URL查询参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL查询参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL查询参数）
def URL查询参数(输入):
    return 键 + '=' + 值

# ── 网络_URL锚点 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL锚点（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL锚点）
def URL锚点(输入):
    return URL + '#' + 锚点

# ── 网络_URL片段 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL片段（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL片段）
def URL片段(输入):
    return 输入.split('#')[1]

# ── 网络_URL主机名 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL主机名（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL主机名）
def URL主机名(输入):
    return 输入.split('/')[2]

# ── 网络_URL端口号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL端口号（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL端口号）
def URL端口号(输入):
    return 输入.split(':')[2] if ':' in 输入 else '80'

# ── 网络_URL路径 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL路径（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL路径）
def URL路径(输入):
    return 输入.split('/')[3:]

# ── 网络_URL参数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL参数（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL参数）
def URL参数(输入):
    return 输入.split('?')[1]

# ── 网络_URL编码全 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL编码全（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL编码全）
def URL编码全(输入):
    return 文本.encode('url')

# ── 网络_URL解码全 ──
# 输入: ['文本'] → 输出: 文本
# 积木：网络_URL解码全（网络领域，自动生成）
# 契约：输入 [文本] → 输出 文本（网络高级操作：网络_URL解码全）
def URL解码全(输入):
    return 文本.decode('url')
