# -*- coding: utf-8 -*-
"""光明积木 · 迭代领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 列表范围 ──
# 输入: ['列表'] → 输出: 列表
# 积木：列表范围（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：列表范围）
def 列范围(输入):
    return 1

# ── 重复列表 ──
# 输入: ['列表'] → 输出: 列表
# 积木：重复列表（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：重复列表）
def 重复表(输入):
    return 列表 + 列表 + 列表

# ── 交错列表 ──
# 输入: ['列表'] → 输出: 列表
# 积木：交错列表（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：交错列表）
def 交错表(输入):
    return 表甲 + 表乙

# ── 循环移位 ──
# 输入: ['列表'] → 输出: 列表
# 积木：循环移位（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：循环移位）
def 循环(输入):
    return 列表[1:] + [列表[0]]

# ── 反向迭代 ──
# 输入: ['列表'] → 输出: 列表
# 积木：反向迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：反向迭代）
def 反向(输入):
    return 列表.reverse()

# ── 跳跃采样 ──
# 输入: ['列表'] → 输出: 列表
# 积木：跳跃采样（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：跳跃采样）
def 跳跃(输入):
    return 列表[0::2]

# ── 间隔采样 ──
# 输入: ['列表'] → 输出: 列表
# 积木：间隔采样（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：间隔采样）
def 间隔(输入):
    return 列表[1::2]

# ── 累积映射 ──
# 输入: ['列表'] → 输出: 列表
# 积木：累积映射（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：累积映射）
def 累积(输入):
    return 列表[i] + 列表[i]

# ── 分块迭代 ──
# 输入: ['列表'] → 输出: 列表
# 积木：分块迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代操作：分块迭代）
def 分块(输入):
    return 列表[0:3]

# ── 归约迭代 ──
# 输入: ['列表'] → 输出: 数
# 积木：归约迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 数（迭代操作：归约迭代）
def 归约(输入):
    return 列表[i] + 列表[i]

# ── 迭代_范围生成 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_范围生成（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_范围生成）
def 范围生成(输入):
    return 1

# ── 迭代_重复元素 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_重复元素（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_重复元素）
def 重复元素(输入):
    return 元素 + 元素 + 元素

# ── 迭代_交错合并 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_交错合并（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_交错合并）
def 交错合并(输入):
    return 甲 + 乙

# ── 迭代_循环移位 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_循环移位（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_循环移位）
def 循环移位(输入):
    return 列表[1:] + [列表[0]]

# ── 迭代_反向迭代 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_反向迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_反向迭代）
def 反向迭代(输入):
    return 列表.reverse()

# ── 迭代_跳跃采样 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_跳跃采样（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_跳跃采样）
def 跳跃采样(输入):
    return 列表[0::2]

# ── 迭代_间隔采样 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_间隔采样（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_间隔采样）
def 间隔采样(输入):
    return 列表[1::2]

# ── 迭代_累积映射 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_累积映射（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_累积映射）
def 累积映射(输入):
    return 列表[i] + 列表[i]

# ── 迭代_分块迭代 ──
# 输入: ['列表'] → 输出: 列表
# 积木：迭代_分块迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 列表（迭代高级操作：迭代_分块迭代）
def 分块迭代(输入):
    return 列表[0:3]

# ── 迭代_归约迭代 ──
# 输入: ['列表'] → 输出: 数
# 积木：迭代_归约迭代（迭代领域，自动生成）
# 契约：输入 [列表] → 输出 数（迭代高级操作：迭代_归约迭代）
def 归约迭代(输入):
    return 列表[i] + 列表[i]
