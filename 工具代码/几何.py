# -*- coding: utf-8 -*-
"""光明积木 · 几何领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 圆面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆面积）
def 圆面积(输入):
    return 3.14159 * 输入 * 输入

# ── 圆周长 ──
# 输入: ['数'] → 输出: 数
# 积木：圆周长（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆周长）
def 圆周长(输入):
    return 2 * 3.14159 * 输入

# ── 矩形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：矩形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：矩形面积）
def 矩形面积(输入):
    return 甲 * 乙

# ── 矩形周长 ──
# 输入: ['数'] → 输出: 数
# 积木：矩形周长（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：矩形周长）
def 矩形周长(输入):
    return 2 * (甲 + 乙)

# ── 三角形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：三角形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：三角形面积）
def 三角面积(输入):
    return 甲 * 乙 / 2

# ── 球体积 ──
# 输入: ['数'] → 输出: 数
# 积木：球体积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：球体积）
def 球体积(输入):
    return 4 * 3.14159 * 输入 * 输入 * 输入 / 3

# ── 圆柱体积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆柱体积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆柱体积）
def 圆柱体积(输入):
    return 3.14159 * 半径 * 半径 * 高

# ── 圆锥体积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆锥体积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆锥体积）
def 圆锥体积(输入):
    return 3.14159 * 半径 * 半径 * 高 / 3

# ── 正方形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：正方形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正方形面积）
def 正方面积(输入):
    return 输入 * 输入

# ── 正方形周长 ──
# 输入: ['数'] → 输出: 数
# 积木：正方形周长（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正方形周长）
def 正方周长(输入):
    return 输入 * 4

# ── 梯形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：梯形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：梯形面积）
def 梯形面积(输入):
    return (上底 + 下底) * 高 / 2

# ── 扇形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：扇形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：扇形面积）
def 扇形面积(输入):
    return 半径 * 半径 * 3.14159 * 角度 / 360

# ── 环形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：环形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：环形面积）
def 环形面积(输入):
    return 3.14159 * (外径 * 外径 - 内径 * 内径)

# ── 椭圆面积 ──
# 输入: ['数'] → 输出: 数
# 积木：椭圆面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：椭圆面积）
def 椭圆面积(输入):
    return 3.14159 * 长轴 * 短轴

# ── 长方体体积 ──
# 输入: ['数'] → 输出: 数
# 积木：长方体体积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：长方体体积）
def 长方体体积(输入):
    return 长 * 宽 * 高

# ── 正方体体积 ──
# 输入: ['数'] → 输出: 数
# 积木：正方体体积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正方体体积）
def 正方体体积(输入):
    return 边长 * 边长 * 边长

# ── 球表面积 ──
# 输入: ['数'] → 输出: 数
# 积木：球表面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：球表面积）
def 球表面积(输入):
    return 4 * 3.14159 * 半径 * 半径

# ── 圆柱侧面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆柱侧面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆柱侧面积）
def 圆柱侧面积(输入):
    return 2 * 3.14159 * 半径 * 高

# ── 圆锥侧面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆锥侧面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆锥侧面积）
def 圆锥侧面积(输入):
    return 3.14159 * 半径 * (半径 * 半径 + 高 * math.sqrt(高))

# ── 正六边形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：正六边形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正六边形面积）
def 正六边形面积(输入):
    return 3 * 1.732 * 边长 * 边长 / 2

# ── 正八边形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：正八边形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正八边形面积）
def 正八边形面积(输入):
    return 2 * (1 + 1.414) * 边长 * 边长

# ── 平行四边形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：平行四边形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：平行四边形面积）
def 平行四边形面积(输入):
    return 底 * 高

# ── 菱形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：菱形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：菱形面积）
def 菱形面积(输入):
    return 对角线甲 * 对角线乙 / 2

# ── 圆环面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆环面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆环面积）
def 圆环面积(输入):
    return 3.14159 * (外径 * 外径 - 内径 * 内径)

# ── 弓形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：弓形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：弓形面积）
def 弓形面积(输入):
    return 半径 * 半径 * (弧度 - 弧度.正弦()) / 2

# ── 正多边形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：正多边形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：正多边形面积）
def 正多边形面积(输入):
    return 边数 * 边长 * 边心距 / 2

# ── 立方体表面积 ──
# 输入: ['数'] → 输出: 数
# 积木：立方体表面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：立方体表面积）
def 立方体表面积(输入):
    return 6 * 边长 * 边长

# ── 圆柱全面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆柱全面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆柱全面积）
def 圆柱全面积(输入):
    return 2 * 3.14159 * 半径 * (半径 + 高)

# ── 圆锥全面积 ──
# 输入: ['数'] → 输出: 数
# 积木：圆锥全面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：圆锥全面积）
def 圆锥全面积(输入):
    return 3.14159 * 半径 * (半径 + (半径 * 半径 + 高 * math.sqrt(高)))

# ── 球体积公式 ──
# 输入: ['数'] → 输出: 数
# 积木：球体积公式（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何计算：球体积公式）
def 球体积公式(输入):
    return 4 / 3 * 3.14159 * 半径 * 半径 * 半径

# ── 几何_两点距离 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_两点距离（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_两点距离）
def 两点距离(输入):
    return 平方根((甲减乙) * (甲减乙) + (丙减丁) * (丙减丁))

# ── 几何_中点计算 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_中点计算（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_中点计算）
def 中点计算(输入):
    return (甲 + 乙) / 2

# ── 几何_斜率计算 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_斜率计算（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_斜率计算）
def 斜率计算(输入):
    return (乙减甲) / (丁减丙)

# ── 几何_角度计算 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_角度计算（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_角度计算）
def 角度计算(输入):
    return 反正切(斜率)

# ── 几何_向量长度 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_向量长度（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_向量长度）
def 向量长度(输入):
    return 平方根(甲 * 甲 + 乙 * 乙)

# ── 几何_向量点积 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_向量点积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_向量点积）
def 向量点积(输入):
    return 甲 * 丙 + 乙 * 丁

# ── 几何_向量叉积 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_向量叉积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_向量叉积）
def 向量叉积(输入):
    return 甲 * 丁 - 乙 * 丙

# ── 几何_三角形周长 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_三角形周长（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_三角形周长）
def 三角形周长(输入):
    return 甲 + 乙 + 丙

# ── 几何_四边形周长 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_四边形周长（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_四边形周长）
def 四边形周长(输入):
    return 甲 + 乙 + 丙 + 丁

# ── 几何_多边形面积 ──
# 输入: ['数'] → 输出: 数
# 积木：几何_多边形面积（几何领域，自动生成）
# 契约：输入 [数] → 输出 数（几何高级计算：几何_多边形面积）
def 多边形面积(输入):
    return 输入
