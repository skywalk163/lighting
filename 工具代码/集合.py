# -*- coding: utf-8 -*-
"""光明积木 · 集合领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 取正数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取正数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（从列表中筛选出取正数）
def 正数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取负数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取负数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（从列表中筛选出取负数）
def 负数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] < 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取非零 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取非零（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（从列表中筛选出取非零）
def 非零(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] != 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取大于均 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取大于均（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（从列表中筛选出取大于均）
def 大于均(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > (列表求和 / len(列表)):
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取大于阈值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取大于阈值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取大于阈值）
def 大于阈值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 阈值:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取小于阈值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取小于阈值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取小于阈值）
def 小于阈值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] < 阈值:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取等于阈值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取等于阈值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取等于阈值）
def 等于阈值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] == 阈值:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取在范围内 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取在范围内（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取在范围内）
def 在范围内(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 甲 and 列表[i] < 乙:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取不在范围 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取不在范围（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取不在范围）
def 不在范围(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] < 甲 or 列表[i] > 乙:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取整数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取整数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取整数）
def 整数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] == int(列表[i]):
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取小数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取小数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取小数）
def 小数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] != int(列表[i]):
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取大于均值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取大于均值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取大于均值）
def 大于均值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取小于均值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取小于均值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取小于均值）
def 小于均值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] < 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取前N个 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取前N个（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取前N个）
def 前N个(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if i < N:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取后N个 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取后N个（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取后N个）
def 后N个(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if i >= len(列表) - N:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取中间段 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取中间段（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取中间段）
def 中间段(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if i >= 甲 and i < 乙:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取每隔N个 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取每隔N个（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取每隔N个）
def 每隔N(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if i % N == 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取每隔N个偏移 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取每隔N个偏移（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取每隔N个偏移）
def 每隔N偏移(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if i % N == 偏移:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取随机子集 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取随机子集（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合操作：取随机子集）
def 随机子集(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if random.random() > 0.5:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取质数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取质数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取质数）
def 质数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] == 2 or 列表[i] == 3 or 列表[i] == 5 or 列表[i] == 7 or 列表[i] == 11:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取完全平方 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取完全平方（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取完全平方）
def 完全平方(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if int(平方根(列表[i])) * int(平方根(列表[i])) == 列表[i]:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取完全立方 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取完全立方（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取完全立方）
def 完全立方(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if int(立方根(列表[i])) * int(立方根(列表[i])) * int(立方根(列表[i])) == 列表[i]:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取回文数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取回文数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取回文数）
def 回文数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 文本(列表[i]) == 文本(列表[i]).reverse():
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取素数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取素数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取素数）
def 素数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 1:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取合数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取合数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取合数）
def 合数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 1:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取斐波那契 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取斐波那契（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取斐波那契）
def 斐波那契(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] == 0 or 列表[i] == 1 or 列表[i] == 2 or 列表[i] == 3 or 列表[i] == 5 or 列表[i] == 8:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取倍数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取倍数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取倍数）
def 倍数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] % 倍数 == 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取约数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取约数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取约数）
def 约数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 输入 % 列表[i] == 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 集合_取对称差 ──
# 输入: ['列表'] → 输出: 列表
# 积木：集合_取对称差（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取对称差）
def 对称差(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if 列表[i] > 0:
            结果.append(列表[i])
        i = i + 1
    return 结果
