# -*- coding: utf-8 -*-
"""光明积木 · 格式领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 数字补零 ──
# 输入: ['数'] → 输出: 文本
# 积木：数字补零（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：数字补零）
def 补零(输入):
    return 文本(输入).zfill('0', 5)

# ── 数字千分位 ──
# 输入: ['数'] → 输出: 文本
# 积木：数字千分位（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：数字千分位）
def 千分位(输入):
    return 文本(输入)

# ── 百分比格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：百分比格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：百分比格式化）
def 百分比(输入):
    return 文本(输入 * 100) + '%'

# ── 货币格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：货币格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：货币格式化）
def 货币(输入):
    return '¥' + 文本(输入)

# ── 科学计数法 ──
# 输入: ['数'] → 输出: 文本
# 积木：科学计数法（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：科学计数法）
def 科学(输入):
    return 文本(输入)

# ── 右对齐 ──
# 输入: ['数'] → 输出: 文本
# 积木：右对齐（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：右对齐）
def 右对齐(输入):
    return 文本(输入).zfill(' ', 10)

# ── 左对齐 ──
# 输入: ['数'] → 输出: 文本
# 积木：左对齐（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：左对齐）
def 左对齐(输入):
    return 文本(输入).zfill(' ', 10)

# ── 居中对齐 ──
# 输入: ['数'] → 输出: 文本
# 积木：居中对齐（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：居中对齐）
def 居中(输入):
    return 文本(输入).zfill(' ', 10)

# ── 截断小数 ──
# 输入: ['数'] → 输出: 数
# 积木：截断小数（格式领域，自动生成）
# 契约：输入 [数] → 输出 数（格式化操作：截断小数）
def 截小数(输入):
    return int(输入 * 100) / 100

# ── 四舍五入 ──
# 输入: ['数'] → 输出: 数
# 积木：四舍五入（格式领域，自动生成）
# 契约：输入 [数] → 输出 数（格式化操作：四舍五入）
def 四舍五入(输入):
    return int(输入 + 0.5)

# ── 整数格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：整数格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：整数格式化）
def 整数格式化(输入):
    return int(输入)

# ── 小数格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：小数格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：小数格式化）
def 小数格式化(输入):
    return 小数(输入)

# ── 百分比格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：百分比格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：百分比格式）
def 百分比格式(输入):
    return 文本(输入 * 100) + '%'

# ── 货币格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：货币格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：货币格式）
def 货币格式(输入):
    return '¥' + 文本(输入)

# ── 科学格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：科学格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：科学格式）
def 科学格式(输入):
    return 文本(输入)

# ── 千分位格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：千分位格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：千分位格式）
def 千分位格式(输入):
    return 文本(输入)

# ── 补零对齐 ──
# 输入: ['数'] → 输出: 文本
# 积木：补零对齐（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：补零对齐）
def 补零对齐(输入):
    return 文本(输入).zfill('0', 5)

# ── 补空格对齐 ──
# 输入: ['数'] → 输出: 文本
# 积木：补空格对齐（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：补空格对齐）
def 补空格对齐(输入):
    return 文本(输入).zfill(' ', 5)

# ── 截断小数两位 ──
# 输入: ['数'] → 输出: 数
# 积木：截断小数两位（格式领域，自动生成）
# 契约：输入 [数] → 输出 数（格式化操作：截断小数两位）
def 截断小数两位(输入):
    return int(输入 * 100) / 100

# ── 四舍五入两位 ──
# 输入: ['数'] → 输出: 数
# 积木：四舍五入两位（格式领域，自动生成）
# 契约：输入 [数] → 输出 数（格式化操作：四舍五入两位）
def 四舍五入两位(输入):
    return int(输入 * 100 + 0.5) / 100

# ── 科学计数法格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：科学计数法格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：科学计数法格式）
def 科学计数法格式(输入):
    return 文本(输入)

# ── 工程计数法 ──
# 输入: ['数'] → 输出: 文本
# 积木：工程计数法（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：工程计数法）
def 工程计数法(输入):
    return 文本(输入)

# ── 分数格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：分数格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：分数格式）
def 分数格式(输入):
    return 文本(int(输入)) + '/' + 文本(int(1 / (输入 - int(输入))))

# ── 带符号格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：带符号格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：带符号格式）
def 带符号格式(输入):
    return '+' + 文本(输入) if 输入 > 0 else 文本(输入)

# ── 括号格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：括号格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：括号格式）
def 括号格式(输入):
    return '(' + 文本(输入) + ')'

# ── 绝对值格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：绝对值格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：绝对值格式）
def 绝对值格式(输入):
    return 文本(输入) if 输入 > 0 else 文本(0 - 输入)

# ── 范围格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：范围格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：范围格式）
def 范围格式(输入):
    return 文本(甲) + '~' + 文本(乙)

# ── 列表格式 ──
# 输入: ['数'] → 输出: 文本
# 积木：列表格式（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式化操作：列表格式）
def 列表格式(输入):
    return 文本(输入) + ',' + 文本(输入)

# ── 格式_科学计数法格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_科学计数法格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_科学计数法格式化）
def 科学计数法格式化(输入):
    return 文本(输入)

# ── 格式_工程计数法格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_工程计数法格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_工程计数法格式化）
def 工程计数法格式化(输入):
    return 文本(输入)

# ── 格式_分数格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_分数格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_分数格式化）
def 分数格式化(输入):
    return 文本(int(输入)) + '/' + 文本(1)

# ── 格式_带符号格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_带符号格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_带符号格式化）
def 带符号格式化(输入):
    return '+' + 文本(输入) if 输入 > 0 else 文本(输入)

# ── 格式_括号格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_括号格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_括号格式化）
def 括号格式化(输入):
    return '(' + 文本(输入) + ')'

# ── 格式_绝对值格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_绝对值格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_绝对值格式化）
def 绝对值格式化(输入):
    return 文本(输入) if 输入 > 0 else 文本(0 - 输入)

# ── 格式_范围格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_范围格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_范围格式化）
def 范围格式化(输入):
    return 文本(甲) + '~' + 文本(乙)

# ── 格式_列表格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_列表格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_列表格式化）
def 列表格式化(输入):
    return 文本(输入) + ',' + 文本(输入)

# ── 格式_JSON格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_JSON格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_JSON格式化）
def JSON格式化(输入):
    return 文本(输入)

# ── 格式_XML格式化 ──
# 输入: ['数'] → 输出: 文本
# 积木：格式_XML格式化（格式领域，自动生成）
# 契约：输入 [数] → 输出 文本（格式高级操作：格式_XML格式化）
def XML格式化(输入):
    return 文本(输入)
