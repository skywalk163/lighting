# -*- coding: utf-8 -*-
"""光明积木 · 逻辑领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 逻辑与 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：逻辑与（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：逻辑与）
def 且(甲, 乙):
    return 甲 and 乙

# ── 逻辑或 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：逻辑或（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：逻辑或）
def 或(甲, 乙):
    return 甲 or 乙

# ── 逻辑非 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：逻辑非（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：逻辑非）
def 非(甲, 乙):
    return not 甲

# ── 等于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：等于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：等于）
def 等于(甲, 乙):
    return 甲 == 乙

# ── 不等于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：不等于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：不等于）
def 不等(甲, 乙):
    return 甲 != 乙

# ── 大于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：大于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：大于）
def 大于(甲, 乙):
    return 甲 > 乙

# ── 小于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：小于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：小于）
def 小于(甲, 乙):
    return 甲 < 乙

# ── 大于等于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：大于等于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：大于等于）
def 大于等(甲, 乙):
    return 甲 >= 乙

# ── 小于等于 ──
# 输入: ['逻辑', '逻辑'] → 输出: 逻辑
# 积木：小于等于（逻辑领域，自动生成）
# 契约：输入 [逻辑, 逻辑] → 输出 逻辑（逻辑运算：小于等于）
def 小于等(甲, 乙):
    return 甲 <= 乙

# ── 与非 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：与非（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：与非）
def 与非(输入):
    return not (甲 and 乙)

# ── 或非 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：或非（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：或非）
def 或非(输入):
    return not (甲 or 乙)

# ── 异或 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：异或（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：异或）
def 异或(输入):
    return (甲 or 乙) and (not (甲 and 乙))

# ── 同或 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：同或（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：同或）
def 同或(输入):
    return (甲 and 乙) or (not (甲 or 乙))

# ── 蕴含 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：蕴含（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：蕴含）
def 蕴含(输入):
    return not 甲 or 乙

# ── 双向蕴含 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：双向蕴含（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：双向蕴含）
def 双向蕴含(输入):
    return (甲 and 乙) or (not (甲 or 乙))

# ── 真值判断 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：真值判断（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：真值判断）
def 真值判断(输入):
    return 1 if 输入 else 0

# ── 假值判断 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：假值判断（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：假值判断）
def 假值判断(输入):
    return 0 if 输入 else 1

# ── 逻辑反转 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑反转（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：逻辑反转）
def 逻辑反转(输入):
    return not 输入

# ── 双条件 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：双条件（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑运算：双条件）
def 双条件(输入):
    return (甲 and 乙) or (not 甲 and not 乙)

# ── 逻辑_三值逻辑 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_三值逻辑（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_三值逻辑）
def 三值逻辑(输入):
    return 乙 if 甲 else 丙

# ── 逻辑_多数表决 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_多数表决（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_多数表决）
def 多数表决(输入):
    return (甲 and 乙) or (甲 and 丙) or (乙 and 丙)

# ── 逻辑_一致性 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_一致性（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_一致性）
def 一致性(输入):
    return 甲 == 乙 and 乙 == 丙

# ── 逻辑_排他性 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_排他性（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_排他性）
def 排他性(输入):
    return (甲 or 乙) and not (甲 and 乙)

# ── 逻辑_必要性 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_必要性（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_必要性）
def 必要性(输入):
    return not 甲 or 乙

# ── 逻辑_充分性 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_充分性（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_充分性）
def 充分性(输入):
    return 甲 or not 乙

# ── 逻辑_等价 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_等价（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_等价）
def 等价(输入):
    return (甲 and 乙) or (not 甲 and not 乙)

# ── 逻辑_矛盾 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_矛盾（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_矛盾）
def 矛盾(输入):
    return 甲 and not 甲

# ── 逻辑_重言式 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_重言式（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_重言式）
def 重言式(输入):
    return 甲 or not 甲

# ── 逻辑_条件否定 ──
# 输入: ['逻辑'] → 输出: 逻辑
# 积木：逻辑_条件否定（逻辑领域，自动生成）
# 契约：输入 [逻辑] → 输出 逻辑（逻辑高级运算：逻辑_条件否定）
def 条件否定(输入):
    return 甲 and not 乙
