# -*- coding: utf-8 -*-
"""光明积木 · 单位领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 米转千米 ──
# 输入: ['数'] → 输出: 数
# 积木：米转千米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：米转千米）
def 米转千(输入):
    return 输入 / 1000

# ── 千米转米 ──
# 输入: ['数'] → 输出: 数
# 积木：千米转米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：千米转米）
def 千转米(输入):
    return 输入 * 1000

# ── 厘米转米 ──
# 输入: ['数'] → 输出: 数
# 积木：厘米转米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：厘米转米）
def 厘转米(输入):
    return 输入 / 100

# ── 米转厘米 ──
# 输入: ['数'] → 输出: 数
# 积木：米转厘米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：米转厘米）
def 米转厘(输入):
    return 输入 * 100

# ── 毫米转米 ──
# 输入: ['数'] → 输出: 数
# 积木：毫米转米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：毫米转米）
def 毫转米(输入):
    return 输入 / 1000

# ── 米转毫米 ──
# 输入: ['数'] → 输出: 数
# 积木：米转毫米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：米转毫米）
def 米转毫(输入):
    return 输入 * 1000

# ── 千克转克 ──
# 输入: ['数'] → 输出: 数
# 积木：千克转克（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：千克转克）
def 千转克(输入):
    return 输入 * 1000

# ── 克转千克 ──
# 输入: ['数'] → 输出: 数
# 积木：克转千克（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：克转千克）
def 克转千(输入):
    return 输入 / 1000

# ── 升转毫升 ──
# 输入: ['数'] → 输出: 数
# 积木：升转毫升（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：升转毫升）
def 升转毫(输入):
    return 输入 * 1000

# ── 毫升转升 ──
# 输入: ['数'] → 输出: 数
# 积木：毫升转升（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：毫升转升）
def 毫转升(输入):
    return 输入 / 1000

# ── 摄氏度转华氏度 ──
# 输入: ['数'] → 输出: 数
# 积木：摄氏度转华氏度（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：摄氏度转华氏度）
def 摄转华(输入):
    return (输入 * 9 / 5) + 32

# ── 华氏度转摄氏度 ──
# 输入: ['数'] → 输出: 数
# 积木：华氏度转摄氏度（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：华氏度转摄氏度）
def 华转摄(输入):
    return (输入 - 32) * 5 / 9

# ── 公里转英里 ──
# 输入: ['数'] → 输出: 数
# 积木：公里转英里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：公里转英里）
def 公转英(输入):
    return 输入 * 0.6214

# ── 英里转公里 ──
# 输入: ['数'] → 输出: 数
# 积木：英里转公里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：英里转公里）
def 英转公(输入):
    return 输入 / 0.6214

# ── 公斤转磅 ──
# 输入: ['数'] → 输出: 数
# 积木：公斤转磅（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：公斤转磅）
def 公转磅(输入):
    return 输入 * 2.2046

# ── 磅转公斤 ──
# 输入: ['数'] → 输出: 数
# 积木：磅转公斤（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：磅转公斤）
def 磅转公(输入):
    return 输入 / 2.2046

# ── 平方米转亩 ──
# 输入: ['数'] → 输出: 数
# 积木：平方米转亩（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：平方米转亩）
def 平转亩(输入):
    return 输入 * 0.0015

# ── 亩转平方米 ──
# 输入: ['数'] → 输出: 数
# 积木：亩转平方米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：亩转平方米）
def 亩转平(输入):
    return 输入 / 0.0015

# ── 海里转公里 ──
# 输入: ['数'] → 输出: 数
# 积木：海里转公里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：海里转公里）
def 海转公(输入):
    return 输入 * 1.852

# ── 公里转海里 ──
# 输入: ['数'] → 输出: 数
# 积木：公里转海里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：公里转海里）
def 公转海(输入):
    return 输入 / 1.852

# ── 英寸转厘米 ──
# 输入: ['数'] → 输出: 数
# 积木：英寸转厘米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：英寸转厘米）
def 英寸转厘米(输入):
    return 输入 * 2.54

# ── 厘米转英寸 ──
# 输入: ['数'] → 输出: 数
# 积木：厘米转英寸（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：厘米转英寸）
def 厘米转英寸(输入):
    return 输入 / 2.54

# ── 英尺转米 ──
# 输入: ['数'] → 输出: 数
# 积木：英尺转米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：英尺转米）
def 英尺转米(输入):
    return 输入 * 0.3048

# ── 米转英尺 ──
# 输入: ['数'] → 输出: 数
# 积木：米转英尺（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：米转英尺）
def 米转英尺(输入):
    return 输入 / 0.3048

# ── 码转米 ──
# 输入: ['数'] → 输出: 数
# 积木：码转米（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：码转米）
def 码转米(输入):
    return 输入 * 0.9144

# ── 米转码 ──
# 输入: ['数'] → 输出: 数
# 积木：米转码（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：米转码）
def 米转码(输入):
    return 输入 / 0.9144

# ── 盎司转克 ──
# 输入: ['数'] → 输出: 数
# 积木：盎司转克（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：盎司转克）
def 盎司转克(输入):
    return 输入 * 28.3495

# ── 克转盎司 ──
# 输入: ['数'] → 输出: 数
# 积木：克转盎司（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：克转盎司）
def 克转盎司(输入):
    return 输入 / 28.3495

# ── 磅转千克 ──
# 输入: ['数'] → 输出: 数
# 积木：磅转千克（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：磅转千克）
def 磅转千克(输入):
    return 输入 * 0.453592

# ── 千克转磅 ──
# 输入: ['数'] → 输出: 数
# 积木：千克转磅（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：千克转磅）
def 千克转磅(输入):
    return 输入 / 0.453592

# ── 加仑转升 ──
# 输入: ['数'] → 输出: 数
# 积木：加仑转升（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：加仑转升）
def 加仑转升(输入):
    return 输入 * 3.78541

# ── 升转加仑 ──
# 输入: ['数'] → 输出: 数
# 积木：升转加仑（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：升转加仑）
def 升转加仑(输入):
    return 输入 / 3.78541

# ── 华氏度转开尔文 ──
# 输入: ['数'] → 输出: 数
# 积木：华氏度转开尔文（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：华氏度转开尔文）
def 华氏转开尔文(输入):
    return (输入 - 32) * 5 / 9 + 273.15

# ── 摄氏度转开尔文 ──
# 输入: ['数'] → 输出: 数
# 积木：摄氏度转开尔文（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：摄氏度转开尔文）
def 摄氏转开尔文(输入):
    return 输入 + 273.15

# ── 开尔文转摄氏度 ──
# 输入: ['数'] → 输出: 数
# 积木：开尔文转摄氏度（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：开尔文转摄氏度）
def 开尔文转摄氏(输入):
    return 输入 - 273.15

# ── 开尔文转华氏度 ──
# 输入: ['数'] → 输出: 数
# 积木：开尔文转华氏度（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位换算：开尔文转华氏度）
def 开尔文转华氏(输入):
    return (输入 - 273.15) * 9 / 5 + 32

# ── 单位_速度换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_速度换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（km/h转m/s）
def 速度换算(输入):
    return 输入 / 3.6

# ── 单位_速度换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_速度换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（m/s转km/h）
def 速度换算2(输入):
    return 输入 * 3.6

# ── 单位_面积换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_面积换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（平方米转公顷）
def 面积换算(输入):
    return 输入 / 10000

# ── 单位_面积换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_面积换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（公顷转平方米）
def 面积换算2(输入):
    return 输入 * 10000

# ── 单位_体积换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_体积换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（立方米转升）
def 体积换算(输入):
    return 输入 * 1000

# ── 单位_体积换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_体积换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（升转立方米）
def 体积换算2(输入):
    return 输入 / 1000

# ── 单位_压力换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_压力换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（Pa转kPa）
def 压力换算(输入):
    return 输入 / 1000

# ── 单位_压力换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_压力换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kPa转Pa）
def 压力换算2(输入):
    return 输入 * 1000

# ── 单位_能量换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_能量换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（J转kJ）
def 能量换算(输入):
    return 输入 / 1000

# ── 单位_能量换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_能量换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kJ转J）
def 能量换算2(输入):
    return 输入 * 1000

# ── 单位_功率换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_功率换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（W转kW）
def 功率换算(输入):
    return 输入 / 1000

# ── 单位_功率换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_功率换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kW转W）
def 功率换算2(输入):
    return 输入 * 1000

# ── 单位_电压换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电压换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（V转kV）
def 电压换算(输入):
    return 输入 / 1000

# ── 单位_电压换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电压换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kV转V）
def 电压换算2(输入):
    return 输入 * 1000

# ── 单位_电流换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电流换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（A转mA）
def 电流换算(输入):
    return 输入 * 1000

# ── 单位_电流换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电流换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（mA转A）
def 电流换算2(输入):
    return 输入 / 1000

# ── 单位_电阻换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电阻换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（Ω转kΩ）
def 电阻换算(输入):
    return 输入 / 1000

# ── 单位_电阻换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_电阻换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kΩ转Ω）
def 电阻换算2(输入):
    return 输入 * 1000

# ── 单位_频率换算 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_频率换算（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（Hz转kHz）
def 频率换算(输入):
    return 输入 / 1000

# ── 单位_频率换算2 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_频率换算2（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（kHz转Hz）
def 频率换算2(输入):
    return 输入 * 1000

# ── 单位_光年转公里 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_光年转公里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_光年转公里）
def 光年转公里(输入):
    return 输入 * 9460730472580

# ── 单位_公里转光年 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_公里转光年（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_公里转光年）
def 公里转光年(输入):
    return 输入 / 9460730472580

# ── 单位_天文单位转公里 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_天文单位转公里（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_天文单位转公里）
def 天文单位转公里(输入):
    return 输入 * 149597871

# ── 单位_公里转天文单位 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_公里转天文单位（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_公里转天文单位）
def 公里转天文单位(输入):
    return 输入 / 149597871

# ── 单位_秒差距转光年 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_秒差距转光年（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_秒差距转光年）
def 秒差距转光年(输入):
    return 输入 * 3.26156

# ── 单位_光年转秒差距 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_光年转秒差距（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_光年转秒差距）
def 光年转秒差距(输入):
    return 输入 / 3.26156

# ── 单位_节转公里每小时 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_节转公里每小时（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_节转公里每小时）
def 节转公里每小时(输入):
    return 输入 * 1.852

# ── 单位_公里每小时转节 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_公里每小时转节（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_公里每小时转节）
def 公里每小时转节(输入):
    return 输入 / 1.852

# ── 单位_马赫转公里每小时 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_马赫转公里每小时（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_马赫转公里每小时）
def 马赫转公里每小时(输入):
    return 输入 * 1225

# ── 单位_公里每小时转马赫 ──
# 输入: ['数'] → 输出: 数
# 积木：单位_公里每小时转马赫（单位领域，自动生成）
# 契约：输入 [数] → 输出 数（单位高级换算：单位_公里每小时转马赫）
def 公里每小时转马赫(输入):
    return 输入 / 1225
