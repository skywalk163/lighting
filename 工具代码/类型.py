# -*- coding: utf-8 -*-
"""光明积木 · 类型领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 转整数 ──
# 输入: ['数'] → 输出: 数
# 积木：转整数（类型领域，自动生成）
# 契约：输入 [数] → 输出 数（类型转换：转整数）
def 转整数(输入):
    return int(输入)

# ── 转小数 ──
# 输入: ['数'] → 输出: 小数
# 积木：转小数（类型领域，自动生成）
# 契约：输入 [数] → 输出 小数（类型转换：转小数）
def 转小数(输入):
    return 小数(输入)

# ── 转文本 ──
# 输入: ['数'] → 输出: 文本
# 积木：转文本（类型领域，自动生成）
# 契约：输入 [数] → 输出 文本（类型转换：转文本）
def 转文本(输入):
    return 文本(输入)

# ── 转逻辑 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：转逻辑（类型领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（类型转换：转逻辑）
def 转逻辑(输入):
    return 逻辑(输入)

# ── 转列表 ──
# 输入: ['数'] → 输出: 列表
# 积木：转列表（类型领域，自动生成）
# 契约：输入 [数] → 输出 列表（类型转换：转列表）
def 转列表(输入):
    return 列表(输入)

# ── 类型_转整数 ──
# 输入: ['数'] → 输出: 数
# 积木：类型_转整数（类型领域，自动生成）
# 契约：输入 [数] → 输出 数（类型转换高级操作：类型_转整数）
def 转整数(输入):
    return int(输入)

# ── 类型_转小数 ──
# 输入: ['数'] → 输出: 小数
# 积木：类型_转小数（类型领域，自动生成）
# 契约：输入 [数] → 输出 小数（类型转换高级操作：类型_转小数）
def 转小数(输入):
    return 小数(输入)

# ── 类型_转文本 ──
# 输入: ['数'] → 输出: 文本
# 积木：类型_转文本（类型领域，自动生成）
# 契约：输入 [数] → 输出 文本（类型转换高级操作：类型_转文本）
def 转文本(输入):
    return 文本(输入)

# ── 类型_转逻辑 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：类型_转逻辑（类型领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（类型转换高级操作：类型_转逻辑）
def 转逻辑(输入):
    return 逻辑(输入)

# ── 类型_转列表 ──
# 输入: ['数'] → 输出: 列表
# 积木：类型_转列表（类型领域，自动生成）
# 契约：输入 [数] → 输出 列表（类型转换高级操作：类型_转列表）
def 转列表(输入):
    return 列表(输入)

# ── 类型_转字典 ──
# 输入: ['数'] → 输出: 字典
# 积木：类型_转字典（类型领域，自动生成）
# 契约：输入 [数] → 输出 字典（类型转换高级操作：类型_转字典）
def 转字典(输入):
    return 字典(输入)

# ── 类型_转集合 ──
# 输入: ['数'] → 输出: 集合
# 积木：类型_转集合（类型领域，自动生成）
# 契约：输入 [数] → 输出 集合（类型转换高级操作：类型_转集合）
def 转集合(输入):
    return 集合(输入)

# ── 类型_转字节 ──
# 输入: ['数'] → 输出: 字节
# 积木：类型_转字节（类型领域，自动生成）
# 契约：输入 [数] → 输出 字节（类型转换高级操作：类型_转字节）
def 转字节(输入):
    return 字节(输入)

# ── 类型_转元组 ──
# 输入: ['数'] → 输出: 元组
# 积木：类型_转元组（类型领域，自动生成）
# 契约：输入 [数] → 输出 元组（类型转换高级操作：类型_转元组）
def 转元组(输入):
    return 元组(输入)

# ── 类型_转数组 ──
# 输入: ['数'] → 输出: 数组
# 积木：类型_转数组（类型领域，自动生成）
# 契约：输入 [数] → 输出 数组（类型转换高级操作：类型_转数组）
def 转数组(输入):
    return 数组(输入)
