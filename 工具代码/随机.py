# -*- coding: utf-8 -*-
"""光明积木 · 随机领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 随机范围 ──
# 输入: ['数'] → 输出: 数
# 积木：随机范围（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机操作：随机范围）
def 随机范围(输入):
    return random.random(起, 止)

# ── 随机列表 ──
# 输入: ['数'] → 输出: 列表
# 积木：随机列表（随机领域，自动生成）
# 契约：输入 [数] → 输出 列表（随机操作：随机列表）
def 随机列表(输入):
    return 随机列表(长度)

# ── 随机洗牌 ──
# 输入: ['数'] → 输出: 列表
# 积木：随机洗牌（随机领域，自动生成）
# 契约：输入 [数] → 输出 列表（随机操作：随机洗牌）
def 洗牌(输入):
    return 洗牌(列表)

# ── 随机采样 ──
# 输入: ['数'] → 输出: 列表
# 积木：随机采样（随机领域，自动生成）
# 契约：输入 [数] → 输出 列表（随机操作：随机采样）
def 采样(输入):
    return 采样(列表, 数量)

# ── 随机布尔 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：随机布尔（随机领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（随机操作：随机布尔）
def 随机布尔(输入):
    return random.random() > 0.5

# ── 随机_正态分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_正态分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_正态分布）
def 正态分布(输入):
    return random.random() + random.random() + random.random() + random.random() + random.random() - 2.5

# ── 随机_均匀分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_均匀分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_均匀分布）
def 均匀分布(输入):
    return 甲 + random.random() * (乙 - 甲)

# ── 随机_指数分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_指数分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_指数分布）
def 指数分布(输入):
    return 0 - 对数(1 - random.random()) / 速率

# ── 随机_泊松分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_泊松分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_泊松分布）
def 泊松分布(输入):
    return 0

# ── 随机_二项分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_二项分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_二项分布）
def 二项分布(输入):
    return 0

# ── 随机_几何分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_几何分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_几何分布）
def 几何分布(输入):
    return 0

# ── 随机_超几何分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_超几何分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_超几何分布）
def 超几何分布(输入):
    return 0

# ── 随机_贝塔分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_贝塔分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_贝塔分布）
def 贝塔分布(输入):
    return 0

# ── 随机_伽马分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_伽马分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_伽马分布）
def 伽马分布(输入):
    return 0

# ── 随机_韦伯分布 ──
# 输入: ['数'] → 输出: 数
# 积木：随机_韦伯分布（随机领域，自动生成）
# 契约：输入 [数] → 输出 数（随机高级操作：随机_韦伯分布）
def 韦伯分布(输入):
    return 0
