# -*- coding: utf-8 -*-
"""光明积木 · 工具领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 生成范围 ──
# 输入: ['数', '数', '数'] → 输出: 列表
# 积木：生成范围（工具领域 L0）
# 契约：输入 起, 止, 步 → 输出 列表（左闭右开的等差数列，步可为负）
def 区间(起, 止, 步):
    结果 = []
    if 步 == 0:
        return 结果
    i = 起
    if 步 > 0:
        while i < 止:
            结果.append(i)
            i = i + 步
    else:
        while i > 止:
            结果.append(i)
            i = i + 步
    return 结果

# ── 排序列表 ──
# 输入: ['列表'] → 输出: 列表
# 积木：排序列表（工具领域 L0）
# 契约：输入 列表 → 输出 列表（升序排序的新表，不改动原表）
def 排列(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i])
        i = i + 1
    结果.sort()
    return 结果

# ── 取唯一 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取唯一（工具领域 L0）
# 契约：输入 列表 → 输出 列表（去掉重复元素，保留首次出现的顺序）
def 去重(列表):
    结果 = []
    i = 0
    while i < len(列表):
        已有 = 0
        j = 0
        while j < len(结果):
            if 结果[j] == 列表[i]:
                已有 = 1
            j = j + 1
        if 已有 == 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取偶数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取偶数（工具领域 L0）
# 契约：输入 列表 → 输出 列表（只保留能被 2 整除的元素）
def 偶筛(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if (列表[i] % 2) == 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取奇数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：取奇数（工具领域 L0）
# 契约：输入 列表 → 输出 列表（只保留不能被 2 整除的元素）
def 奇筛(列表):
    结果 = []
    i = 0
    while i < len(列表):
        if (列表[i] % 2) != 0:
            结果.append(列表[i])
        i = i + 1
    return 结果

# ── 取余数 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：取余数（工具领域 L0）
# 契约：输入 甲, 乙 → 输出 数（甲 除以 乙 的余数）
def 余(甲, 乙):
    return 甲 % 乙

# ── 求幂 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：求幂（工具领域 L0）
# 契约：输入 底, 指 → 输出 数（底的指次方）
def 乘方(底, 指):
    return 底 ** 指

# ── 拼接列表 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：拼接列表（工具领域 L0）
# 契约：输入 表甲, 表乙 → 输出 列表（首尾相接的新表，不改动原表）
def 并表(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲):
        结果.append(表甲[i])
        i = i + 1
    j = 0
    while j < len(表乙):
        结果.append(表乙[j])
        j = j + 1
    return 结果

# ── 取子列表 ──
# 输入: ['列表', '数', '数'] → 输出: 列表
# 积木：取子列表（工具领域 L0）
# 契约：输入 列表, 起, 止 → 输出 列表（索引 起..止-1 的元素，越界自动裁剪）
def 切片(列表, 起, 止):
    结果 = []
    总长 = len(列表)
    头 = 起
    尾 = 止
    if 头 < 0:
        头 = 0
    if 尾 > 总长:
        尾 = 总长
    i = 头
    while i < 尾:
        结果.append(列表[i])
        i = i + 1
    return 结果

# ── 聚合_工具_3项 ──
# 输入: ['数'] → 输出: 列表
# 由 光明积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：工具 · 名称：聚合_工具_3项
# 描述：自动织成：对数输入并行执行 分页参数、取余数、求幂，返回结果列表
# ── 积木：分页参数（网络）──
def 分页(总数, 每页):
    结果 = []
    i = 0
    while (i * 每页) < 总数:
        结果.append(i * 每页)
        i = i + 1
    return 结果
    # ── 积木：取余数（工具）──
def 余(甲, 乙):
    return 甲 % 乙
    # ── 积木：求幂（工具）──
def 乘方(底, 指):
    return 底 ** 指
def 聚合_工具_3项(总数):
    赵果1 = 分页(总数, 10)
    赵果2 = 余(总数, 2)
    赵果3 = 乘方(总数, 2)
    return [赵果1, 赵果2, 赵果3]

# ── 正负判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：正负判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：正负判断）
def 正负(输入):
    if 输入 > 0:
        return '正数'
    else:
        return '非正数'

# ── 奇偶判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：奇偶判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：奇偶判断）
def 奇偶(输入):
    if 输入 % 2 == 0:
        return '偶数'
    else:
        return '奇数'

# ── 零判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：零判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：零判断）
def 零判(输入):
    if 输入 == 0:
        return '零'
    else:
        return '非零'

# ── 大小判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：大小判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：大小判断）
def 大小判(输入):
    if 输入 > 10:
        return '大'
    else:
        return '小'

# ── 范围判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：范围判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：范围判断）
def 范围判(输入):
    if 输入 > 0 and 输入 < 100:
        return '在范围内'
    else:
        return '超出范围'

# ── 相邻交换 ──
# 输入: ['列表'] → 输出: 列表
# 积木：相邻交换（工具领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（工具：相邻交换）
def 相邻交换(列表):
    结果 = []
    i = 0
    while i + 1 < len(列表):
        结果.append(列表[i + 1])
        结果.append(列表[i])
        i = i + 2
    if len(列表) % 2 != 0:
        结果.append(列表[len(列表) - 1])
    return 结果

# ── 奇偶互换 ──
# 输入: ['列表'] → 输出: 列表
# 积木：奇偶互换（工具领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（工具：奇偶互换）
def 奇偶互换(列表):
    结果 = []
    i = 0
    while i + 1 < len(列表):
        结果.append(列表[i + 1])
        结果.append(列表[i])
        i = i + 2
    if len(列表) % 2 != 0:
        结果.append(列表[len(列表) - 1])
    return 结果

# ── 字典取值 ──
# 输入: ['列表'] → 输出: 数
# 积木：字典取值（工具领域，自动生成）
# 契约：输入 [列表] → 输出 数（工具：字典取值）
def 取值(输入):
    return 列表[0]

# ── 字典取键 ──
# 输入: ['列表'] → 输出: 文本
# 积木：字典取键（工具领域，自动生成）
# 契约：输入 [列表] → 输出 文本（工具：字典取键）
def 取键(输入):
    return 列表[0]

# ── 字典长度 ──
# 输入: ['列表'] → 输出: 数
# 积木：字典长度（工具领域，自动生成）
# 契约：输入 [列表] → 输出 数（工具：字典长度）
def 长(输入):
    return len(列表)

# ── 字典合并 ──
# 输入: ['列表'] → 输出: 列表
# 积木：字典合并（工具领域，自动生成）
# 契约：输入 [列表] → 输出 列表（工具：字典合并）
def 合并(输入):
    return 列表 + 列表

# ── 字典反转 ──
# 输入: ['列表'] → 输出: 列表
# 积木：字典反转（工具领域，自动生成）
# 契约：输入 [列表] → 输出 列表（工具：字典反转）
def 反转(输入):
    return 列表.reverse()

# ── 空值判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：空值判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：空值判断）
def 空值判断(输入):
    if '空' if 输入 == 0 else '非空':
        return '空'
    else:
        return '非空'

# ── 大小比较 ──
# 输入: ['数'] → 输出: 文本
# 积木：大小比较（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：大小比较）
def 大小比较(输入):
    if 甲 > 乙:
        return '大'
    else:
        return '小'

# ── 相等判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：相等判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：相等判断）
def 相等判断(输入):
    if 甲 == 乙:
        return '相等'
    else:
        return '不等'

# ── 范围包含 ──
# 输入: ['数'] → 输出: 文本
# 积木：范围包含（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：范围包含）
def 范围包含(输入):
    if 输入 >= 甲 and 输入 <= 乙:
        return '在范围内'
    else:
        return '超出范围'

# ── 素数判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：素数判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：素数判断）
def 素数判断(输入):
    if 输入 == 2 or 输入 == 3 or 输入 == 5 or 输入 == 7:
        return '素数'
    else:
        return '合数'

# ── 正负号 ──
# 输入: ['数'] → 输出: 文本
# 积木：正负号（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：正负号）
def 正负号(输入):
    if 输入 > 0:
        return '+'
    else:
        return '-'

# ── 零值判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：零值判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：零值判断）
def 零值判断(输入):
    if 输入 == 0:
        return '零'
    else:
        return '非零'

# ── 边界判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：边界判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具：边界判断）
def 边界判断(输入):
    if 输入 == 甲 or 输入 == 乙:
        return '边界'
    else:
        return '内部'

# ── 工具_版本比较 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_版本比较（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_版本比较）
def 版本比较(输入):
    if '新' if 甲 > 乙 else ('旧' if 甲 < 乙 else '相同'):
        return '新'
    else:
        return '旧'

# ── 工具_数值分段 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_数值分段（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_数值分段）
def 数值分段(输入):
    if 输入 > 0 and 输入 <= 10:
        return 'A段'
    else:
        return 'B段'

# ── 工具_状态转换 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_状态转换（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_状态转换）
def 状态转换(输入):
    if 输入 == 0:
        return '初始'
    else:
        return '激活'

# ── 工具_优先级判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_优先级判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_优先级判断）
def 优先级判断(输入):
    if 输入 > 8:
        return '高'
    else:
        return '低'

# ── 工具_等级判断 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_等级判断（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_等级判断）
def 等级判断(输入):
    if 输入 >= 90:
        return '优秀'
    else:
        return '一般'

# ── 工具_风险等级 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_风险等级（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_风险等级）
def 风险等级(输入):
    if 输入 > 70:
        return '高风险'
    else:
        return '低风险'

# ── 工具_温度等级 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_温度等级（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_温度等级）
def 温度等级(输入):
    if 输入 > 30:
        return '高温'
    else:
        return '常温'

# ── 工具_速度等级 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_速度等级（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_速度等级）
def 速度等级(输入):
    if 输入 > 100:
        return '高速'
    else:
        return '低速'

# ── 工具_距离等级 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_距离等级（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_距离等级）
def 距离等级(输入):
    if 输入 > 1000:
        return '远'
    else:
        return '近'

# ── 工具_大小等级 ──
# 输入: ['数'] → 输出: 文本
# 积木：工具_大小等级（工具领域，自动生成）
# 契约：输入 [文本] → 输出 文本（工具高级操作：工具_大小等级）
def 大小等级(输入):
    if 输入 > 100:
        return '大'
    else:
        return '小'
