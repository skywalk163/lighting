# -*- coding: utf-8 -*-
"""光明积木 · 财务领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 个税 ──
# 输入: ['数'] → 输出: 数
# 积木：个税（财务领域 L0）
# 契约：输入 数(月收) → 输出 数（本月应缴个税）
# 中国大陆个税月度速算：起征点 5000，七级超额累进（2019 起适用）
def 税(月收):
    应纳 = 月收 - 5000
    if 应纳 <= 0:
        return 0
    elif 应纳 <= 3000:
        return 应纳 * 0.03
    elif 应纳 <= 12000:
        return 应纳 * 0.1 - 210
    elif 应纳 <= 25000:
        return 应纳 * 0.2 - 1410
    elif 应纳 <= 35000:
        return 应纳 * 0.25 - 2660
    elif 应纳 <= 55000:
        return 应纳 * 0.3 - 4410
    elif 应纳 <= 80000:
        return 应纳 * 0.35 - 7160
    else:
        return 应纳 * 0.45 - 15160

# ── 保留分 ──
# 输入: ['数'] → 输出: 数
# 积木：保留分（财务领域 L0）
# 契约：输入 数(数值) → 输出 数（四舍五入到分，两位小数）
def 留分(数值):
    return 四舍五入(数值, 2)

# ── 计算利息 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：计算利息（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：计算利息）
def 利息(输入):
    return 本金 * 利率

# ── 计算复利 ──
# 输入: ['数', '数', '数'] → 输出: 数
# 积木：计算复利（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：计算复利）
def 复利(输入):
    return 本金 * (1 + 利率) ** 期数

# ── 含税金额 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：含税金额（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：含税金额）
def 含税(输入):
    return 金额 * (1 + 税率)

# ── 不含税金额 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：不含税金额（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：不含税金额）
def 不含税(输入):
    return 金额 / (1 + 税率)

# ── 税额计算 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：税额计算（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：税额计算）
def 税额(输入):
    return 金额 * 税率

# ── 折扣后金额 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：折扣后金额（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：折扣后金额）
def 折后(输入):
    return 金额 * (1 - 折扣)

# ── 利润率 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：利润率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：利润率）
def 利润率(输入):
    return (收入 - 成本) / 成本

# ── 毛利率 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：毛利率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：毛利率）
def 毛利率(输入):
    return (收入 - 成本) / 收入

# ── 月供计算 ──
# 输入: ['数', '数', '数'] → 输出: 数
# 积木：月供计算（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：月供计算）
def 月供(输入):
    return 贷款 * 利率 / (1 - (1 + 利率) ** (0 - 期数))

# ── 折旧计算 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：折旧计算（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：折旧计算）
def 折旧(输入):
    return 原值 / 年限

# ── 净现值 ──
# 输入: ['数'] → 输出: 数
# 积木：净现值（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：净现值）
def 净现值(输入):
    return 现金流 / ((1 + 折现率) ** 期数)

# ── 内部收益率 ──
# 输入: ['数'] → 输出: 数
# 积木：内部收益率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：内部收益率）
def IRR(输入):
    return 0

# ── 投资回收期 ──
# 输入: ['数'] → 输出: 数
# 积木：投资回收期（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：投资回收期）
def 回收期(输入):
    return 投资 / 年收益

# ── 盈亏平衡点 ──
# 输入: ['数'] → 输出: 数
# 积木：盈亏平衡点（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：盈亏平衡点）
def 盈亏平衡(输入):
    return 固定成本 / (单价 - 变动成本)

# ── 资产回报率 ──
# 输入: ['数'] → 输出: 数
# 积木：资产回报率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：资产回报率）
def ROA(输入):
    return 净利润 / 总资产

# ── 净资产收益率 ──
# 输入: ['数'] → 输出: 数
# 积木：净资产收益率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：净资产收益率）
def ROE(输入):
    return 净利润 / 净资产

# ── 净利率 ──
# 输入: ['数'] → 输出: 数
# 积木：净利率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：净利率）
def 净利率(输入):
    return 净利润 / 收入

# ── 市盈率 ──
# 输入: ['数'] → 输出: 数
# 积木：市盈率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：市盈率）
def 市盈率(输入):
    return 股价 / 每股收益

# ── 市净率 ──
# 输入: ['数'] → 输出: 数
# 积木：市净率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：市净率）
def 市净率(输入):
    return 股价 / 每股净资产

# ── 股息率 ──
# 输入: ['数'] → 输出: 数
# 积木：股息率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：股息率）
def 股息率(输入):
    return 股息 / 股价

# ── 增长率 ──
# 输入: ['数'] → 输出: 数
# 积木：增长率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：增长率）
def 增长率(输入):
    return (本期 - 上期) / 上期

# ── 复合增长率 ──
# 输入: ['数'] → 输出: 数
# 积木：复合增长率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：复合增长率）
def CAGR(终值, 初值, 年数):
    return (终值 / 初值) ** (1 / 年数) - 1

# ── 折现因子 ──
# 输入: ['数'] → 输出: 数
# 积木：折现因子（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：折现因子）
def 折现因子(输入):
    return 1 / (1 + 利率) ** 期数

# ── 终值计算 ──
# 输入: ['数'] → 输出: 数
# 积木：终值计算（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：终值计算）
def 终值(输入):
    return 现值 * (1 + 利率) ** 期数

# ── 现值计算 ──
# 输入: ['数'] → 输出: 数
# 积木：现值计算（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：现值计算）
def 现值(输入):
    return 终值 / (1 + 利率) ** 期数

# ── 年金现值 ──
# 输入: ['数'] → 输出: 数
# 积木：年金现值（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：年金现值）
def 年金现值(输入):
    return 年金 * (1 - (1 / (1 + 利率) ** 期数)) / 利率

# ── 年金终值 ──
# 输入: ['数'] → 输出: 数
# 积木：年金终值（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：年金终值）
def 年金终值(输入):
    return 年金 * ((1 + 利率) ** 期数 - 1) / 利率

# ── 等额本息 ──
# 输入: ['数'] → 输出: 数
# 积木：等额本息（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：等额本息）
def 等额本息(输入):
    return 贷款 * 月利率 * (1 + 月利率) ** 月数 / ((1 + 月利率) ** 月数 - 1)

# ── 等额本金 ──
# 输入: ['数'] → 输出: 数
# 积木：等额本金（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务计算：等额本金）
def 等额本金(输入):
    return 贷款 / 月数 + (贷款 - 已还) * 月利率

# ── 财务_总资产 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_总资产（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（总资产=负债+权益）
def 总资产(输入):
    return 负债 + 权益

# ── 财务_流动比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_流动比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（流动资产/流动负债）
def 流动比率(输入):
    return 流动资产 / 流动负债

# ── 财务_速动比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_速动比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（(流动资产-存货)/流动负债）
def 速动比率(输入):
    return (流动资产 - 存货) / 流动负债

# ── 财务_资产负债率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_资产负债率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（总负债/总资产）
def 资产负债率(输入):
    return 总负债 / 总资产

# ── 财务_权益乘数 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_权益乘数（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（总资产/净资产）
def 权益乘数(输入):
    return 总资产 / 净资产

# ── 财务_存货周转率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_存货周转率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（营业成本/平均存货）
def 存货周转率(输入):
    return 成本 / 存货

# ── 财务_应收账款周转率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_应收账款周转率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（收入/应收）
def 应收账款周转率(输入):
    return 收入 / 应收

# ── 财务_总资产周转率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_总资产周转率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（收入/总资产）
def 总资产周转率(输入):
    return 收入 / 总资产

# ── 财务_销售净利率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_销售净利率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（净利润/收入）
def 销售净利率(输入):
    return 净利润 / 收入

# ── 财务_资产净利率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_资产净利率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（净利润/总资产）
def 资产净利率(输入):
    return 净利润 / 总资产

# ── 财务_每股收益 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_每股收益（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（净利润/股数）
def 每股收益(输入):
    return 净利润 / 股数

# ── 财务_每股净资产 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_每股净资产（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（净资产/股数）
def 每股净资产(输入):
    return 净资产 / 股数

# ── 财务_股息支付率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_股息支付率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（股息/净利润）
def 股息支付率(输入):
    return 股息 / 净利润

# ── 财务_留存收益率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_留存收益率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（1-股息支付率）
def 留存收益率(输入):
    return 1 - (股息 / 净利润)

# ── 财务_财务杠杆 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_财务杠杆（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（EBIT/税前利润）
def 财务杠杆(输入):
    return EBIT / (EBIT - 利息)

# ── 财务_经营杠杆 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_经营杠杆（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（(收入-变动成本)/(收入-变动成本-固定成本)）
def 经营杠杆(输入):
    return (收入 - 变动) / (收入 - 变动 - 固定)

# ── 财务_总杠杆 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_总杠杆（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（经营杠杆*财务杠杆）
def 总杠杆(输入):
    return (收入 - 变动) / (EBIT - 利息)

# ── 财务_资本成本 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_资本成本（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（加权平均资本成本）
def 资本成本(输入):
    return 权益占比 * 权益成本 + 负债占比 * 负债成本

# ── 财务_经济增加值 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_经济增加值（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（EVA）
def 经济增加值(输入):
    return 净利润 - 资本 * 资本成本

# ── 财务_市场增加值 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_市场增加值（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（MVA）
def 市场增加值(输入):
    return 市值 - 资本

# ── 财务_夏普比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_夏普比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_夏普比率）
def 夏普比率(输入):
    return (收益 - 无风险) / 波动

# ── 财务_贝塔系数 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_贝塔系数（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_贝塔系数）
def 贝塔系数(输入):
    return 协方差(收益, 市场) / 方差(市场)

# ── 财务_阿尔法 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_阿尔法（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_阿尔法）
def 阿尔法(输入):
    return 收益 - 无风险 - 贝塔 * (市场 - 无风险)

# ── 财务_信息比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_信息比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_信息比率）
def 信息比率(输入):
    return (收益 - 基准) / 跟踪误差

# ── 财务_特雷诺比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_特雷诺比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_特雷诺比率）
def 特雷诺比率(输入):
    return (收益 - 无风险) / 贝塔

# ── 财务_索提诺比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_索提诺比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_索提诺比率）
def 索提诺比率(输入):
    return (收益 - 无风险) / 下行波动

# ── 财务_卡尔玛比率 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_卡尔玛比率（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_卡尔玛比率）
def 卡尔玛比率(输入):
    return (收益 - 无风险) / 最大回撤

# ── 财务_最大回撤 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_最大回撤（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_最大回撤）
def 最大回撤(输入):
    return min(收益)

# ── 财务_上涨捕获 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_上涨捕获（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_上涨捕获）
def 上涨捕获(输入):
    return 收益 / 市场

# ── 财务_下跌捕获 ──
# 输入: ['数'] → 输出: 数
# 积木：财务_下跌捕获（财务领域，自动生成）
# 契约：输入 [数] → 输出 数（财务高级指标：财务_下跌捕获）
def 下跌捕获(输入):
    return 收益 / 市场
