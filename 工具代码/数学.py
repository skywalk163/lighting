# -*- coding: utf-8 -*-
"""光明积木 · 数学领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 加法 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：加法（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做加法运算）
def 加法(甲, 乙):
    return 甲 + 乙

# ── 减法 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：减法（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做减法运算）
def 减法(甲, 乙):
    return 甲 - 乙

# ── 乘法 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：乘法（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做乘法运算）
def 乘法(甲, 乙):
    return 甲 * 乙

# ── 除法 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：除法（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做除法运算）
def 除法(甲, 乙):
    return 甲 * 乙

# ── 取模 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：取模（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做取模运算）
def 模(甲, 乙):
    return 甲 / 乙

# ── 最大值 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：最大值（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做最大值运算）
def 最大(甲, 乙):
    return 甲 * 乙

# ── 最小值 ──
# 输入: ['数', '数'] → 输出: 数
# 积木：最小值（数学领域，自动生成）
# 契约：输入 [数, 数] → 输出 数（对两个数做最小值运算）
def 最小(甲, 乙):
    return 甲 * 乙

# ── 绝对值 ──
# 输入: ['数'] → 输出: 数
# 积木：绝对值（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做绝对值运算）
def 绝对值(输入):
    return 输入 if 输入 > 0 else (0 - 输入)

# ── 取整 ──
# 输入: ['数'] → 输出: 数
# 积木：取整（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做取整运算）
def 取整(输入):
    return int(输入)

# ── 倒数 ──
# 输入: ['数'] → 输出: 数
# 积木：倒数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做倒数运算）
def 倒数(输入):
    return 1 / 输入

# ── 平方 ──
# 输入: ['数'] → 输出: 数
# 积木：平方（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做平方运算）
def 平方(输入):
    return 输入 * 输入

# ── 立方 ──
# 输入: ['数'] → 输出: 数
# 积木：立方（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做立方运算）
def 立方(输入):
    return 输入 * 输入 * 输入

# ── 加一 ──
# 输入: ['数'] → 输出: 数
# 积木：加一（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做加一运算）
def 加一(输入):
    return 输入 + 1

# ── 减一 ──
# 输入: ['数'] → 输出: 数
# 积木：减一（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做减一运算）
def 减一(输入):
    return 输入 - 1

# ── 翻倍 ──
# 输入: ['数'] → 输出: 数
# 积木：翻倍（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做翻倍运算）
def 翻倍(输入):
    return 输入 * 2

# ── 减半 ──
# 输入: ['数'] → 输出: 数
# 积木：减半（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做减半运算）
def 减半(输入):
    return 输入 / 2

# ── 取符号 ──
# 输入: ['数'] → 输出: 数
# 积木：取符号（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（对数值做取符号运算）
def 取符号(输入):
    return 1 if 输入 > 0 else ((0 - 1) if 输入 < 0 else 0)

# ── 正弦 ──
# 输入: ['数'] → 输出: 数
# 积木：正弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：正弦）
def 正弦(输入):
    return 输入.正弦()

# ── 余弦 ──
# 输入: ['数'] → 输出: 数
# 积木：余弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：余弦）
def 余弦(输入):
    return 输入.余弦()

# ── 正切 ──
# 输入: ['数'] → 输出: 数
# 积木：正切（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：正切）
def 正切(输入):
    return 输入.正切()

# ── 反正弦 ──
# 输入: ['数'] → 输出: 数
# 积木：反正弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：反正弦）
def 反正弦(输入):
    return 输入.反正弦()

# ── 反余弦 ──
# 输入: ['数'] → 输出: 数
# 积木：反余弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：反余弦）
def 反余弦(输入):
    return 输入.反余弦()

# ── 反正切 ──
# 输入: ['数'] → 输出: 数
# 积木：反正切（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：反正切）
def 反正切(输入):
    return 输入.反正切()

# ── 自然对数 ──
# 输入: ['数'] → 输出: 数
# 积木：自然对数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：自然对数）
def 自然对数(输入):
    return 输入.对数()

# ── 常用对数 ──
# 输入: ['数'] → 输出: 数
# 积木：常用对数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：常用对数）
def 常用对数(输入):
    return 输入.对数(10)

# ── 指数 ──
# 输入: ['数'] → 输出: 数
# 积木：指数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：指数）
def 指数(输入):
    return 输入.指数()

# ── 平方根 ──
# 输入: ['数'] → 输出: 数
# 积木：平方根（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：平方根）
def 平方根(输入):
    return 输入.平方根()

# ── 立方根 ──
# 输入: ['数'] → 输出: 数
# 积木：立方根（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：立方根）
def 立方根(输入):
    return 输入 * 输入 * math.cbrt(输入)

# ── 向上取整 ──
# 输入: ['数'] → 输出: 数
# 积木：向上取整（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：向上取整）
def 向上取整(输入):
    return int(输入) + 1 if 输入 != int(输入) else 输入

# ── 向下取整 ──
# 输入: ['数'] → 输出: 数
# 积木：向下取整（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：向下取整）
def 向下取整(输入):
    return int(输入)

# ── 四舍五入到整数 ──
# 输入: ['数'] → 输出: 数
# 积木：四舍五入到整数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：四舍五入到整数）
def 四舍五入到整数(输入):
    return int(输入 + 0.5)

# ── 取小数部分 ──
# 输入: ['数'] → 输出: 数
# 积木：取小数部分（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：取小数部分）
def 取小数部分(输入):
    return 输入 - int(输入)

# ── 取整数部分 ──
# 输入: ['数'] → 输出: 数
# 积木：取整数部分（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：取整数部分）
def 整数部分(输入):
    return int(输入)

# ── 取分数部分 ──
# 输入: ['数'] → 输出: 数
# 积木：取分数部分（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：取分数部分）
def 分数部分(输入):
    return 输入 - int(输入)

# ── 双曲正弦 ──
# 输入: ['数'] → 输出: 数
# 积木：双曲正弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：双曲正弦）
def 双曲正弦(输入):
    return (输入.指数() - (0 - 输入).指数()) / 2

# ── 双曲余弦 ──
# 输入: ['数'] → 输出: 数
# 积木：双曲余弦（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：双曲余弦）
def 双曲余弦(输入):
    return (输入.指数() + (0 - 输入).指数()) / 2

# ── 双曲正切 ──
# 输入: ['数'] → 输出: 数
# 积木：双曲正切（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：双曲正切）
def 双曲正切(输入):
    return (输入.指数() - (0 - 输入).指数()) / (输入.指数() + (0 - 输入).指数())

# ── 反正切2 ──
# 输入: ['数'] → 输出: 数
# 积木：反正切2（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：反正切2）
def 反正切2(输入):
    return 甲.反正切2(乙)

# ── 角度转弧度 ──
# 输入: ['数'] → 输出: 数
# 积木：角度转弧度（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：角度转弧度）
def 角度转弧度(输入):
    return 输入 * 3.14159 / 180

# ── 弧度转角度 ──
# 输入: ['数'] → 输出: 数
# 积木：弧度转角度（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：弧度转角度）
def 弧度转角度(输入):
    return 输入 * 180 / 3.14159

# ── 百分数 ──
# 输入: ['数'] → 输出: 数
# 积木：百分数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：百分数）
def 百分数(输入):
    return 输入 * 100

# ── 千分数 ──
# 输入: ['数'] → 输出: 数
# 积木：千分数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：千分数）
def 千分数(输入):
    return 输入 * 1000

# ── 归一化 ──
# 输入: ['数'] → 输出: 数
# 积木：归一化（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：归一化）
def 归一化(输入):
    return 输入 / 100

# ── 标准化 ──
# 输入: ['数'] → 输出: 数
# 积木：标准化（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：标准化）
def 标准化(输入):
    return (输入 - 50) / 10

# ── 线性映射 ──
# 输入: ['数'] → 输出: 数
# 积木：线性映射（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：线性映射）
def 线性映射(输入):
    return 甲 * 比例 + 偏移

# ── 截断范围 ──
# 输入: ['数'] → 输出: 数
# 积木：截断范围（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：截断范围）
def 截断范围(输入):
    return 0 if 输入 < 0 else (100 if 输入 > 100 else 输入)

# ── 限制范围 ──
# 输入: ['数'] → 输出: 数
# 积木：限制范围（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：限制范围）
def 限制范围(输入):
    return 甲 if 输入 < 甲 else (乙 if 输入 > 乙 else 输入)

# ── 取较大值 ──
# 输入: ['数'] → 输出: 数
# 积木：取较大值（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：取较大值）
def 取较大值(输入):
    return 甲 if 甲 > 乙 else 乙

# ── 取较小值 ──
# 输入: ['数'] → 输出: 数
# 积木：取较小值（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：取较小值）
def 取较小值(输入):
    return 甲 if 甲 < 乙 else 乙

# ── 两数之差 ──
# 输入: ['数'] → 输出: 数
# 积木：两数之差（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：两数之差）
def 两数之差(输入):
    return 甲 - 乙 if 甲 > 乙 else 乙 - 甲

# ── 两数之和 ──
# 输入: ['数'] → 输出: 数
# 积木：两数之和（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：两数之和）
def 两数之和(输入):
    return 甲 + 乙

# ── 两数之积 ──
# 输入: ['数'] → 输出: 数
# 积木：两数之积（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：两数之积）
def 两数之积(输入):
    return 甲 * 乙

# ── 两数之商 ──
# 输入: ['数'] → 输出: 数
# 积木：两数之商（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：两数之商）
def 两数之商(输入):
    return 甲 / 乙

# ── 两数平均值 ──
# 输入: ['数'] → 输出: 数
# 积木：两数平均值（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学运算：两数平均值）
def 两数平均值(输入):
    return (甲 + 乙) / 2

# ── 数学_线性函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_线性函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=ax+b）
def 线性函数(输入):
    return 甲 * 系数 + 偏移

# ── 数学_二次函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_二次函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=ax^2+bx+c）
def 二次函数(输入):
    return 甲 * 甲 * 系数 + 甲 * 系数 + 常数

# ── 数学_指数函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_指数函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=a*e^x）
def 指数函数(输入):
    return 系数 * 输入.指数()

# ── 数学_对数函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_对数函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=log_a(x)）
def 对数函数(输入):
    return 输入.对数(底)

# ── 数学_幂函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_幂函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=x^n）
def 幂函数(输入):
    return 输入 ** 指数

# ── 数学_三角函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_三角函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=sin(x)）
def 三角函数(输入):
    return 输入.正弦()

# ── 数学_反三角函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_反三角函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=arcsin(x)）
def 反三角函数(输入):
    return 输入.反正弦()

# ── 数学_双曲函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_双曲函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（y=sinh(x)）
def 双曲函数(输入):
    return 输入.双曲正弦()

# ── 数学_取整函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_取整函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（四舍五入到整数）
def 取整函数(输入):
    return int(输入 + 0.5)

# ── 数学_取小数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_取小数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（提取小数部分）
def 取小数(输入):
    return 输入 - int(输入)

# ── 数学_限制范围 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_限制范围（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（限制在[a,b]内）
def 限制范围(输入):
    return 甲 if 输入 < 甲 else (乙 if 输入 > 乙 else 输入)

# ── 数学_映射范围 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_映射范围（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（从[a,b]映射到[c,d]）
def 映射范围(输入):
    return 甲 + (输入 - 甲) * (乙 - 甲) / (乙 - 甲)

# ── 数学_阶跃函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_阶跃函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（阈值触发）
def 阶跃函数(输入):
    return 1 if 输入 > 阈值 else 0

# ── 数学_符号函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_符号函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（返回符号）
def 符号函数(输入):
    return 1 if 输入 > 0 else ((0 - 1) if 输入 < 0 else 0)

# ── 数学_矩形脉冲 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_矩形脉冲（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（矩形波形）
def 矩形脉冲(输入):
    return 1 if 输入 > 甲 and 输入 < 乙 else 0

# ── 数学_锯齿波 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_锯齿波（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（锯齿波形）
def 锯齿波(输入):
    return 输入 % 周期

# ── 数学_三角波 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_三角波（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（三角波形）
def 三角波(输入):
    return 输入 % 周期

# ── 数学_高斯函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_高斯函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（高斯分布）
def 高斯函数(输入):
    return 指数(0 - (输入 * 输入) / (2 * 方差))

# ── 数学_S形函数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_S形函数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（Sigmoid函数）
def S形函数(输入):
    return 1 / (1 + 指数(0 - 输入))

# ── 数学_阶乘 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_阶乘（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_阶乘）
def 阶乘(输入):
    return 1 if 输入 == 0 else 输入 * 阶乘(输入 - 1)

# ── 数学_斐波那契 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_斐波那契（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_斐波那契）
def 斐波那契(输入):
    return 输入 if 输入 <= 1 else 斐波那契(输入 - 1) + 斐波那契(输入 - 2)

# ── 数学_最大公约数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_最大公约数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_最大公约数）
def 最大公约数(输入):
    return 甲 if 乙 == 0 else 最大公约数(乙, 甲 % 乙)

# ── 数学_最小公倍数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_最小公倍数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_最小公倍数）
def 最小公倍数(输入):
    return (甲 * 乙) / 最大公约数(甲, 乙)

# ── 数学_质数判断 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_质数判断（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_质数判断）
def 质数判断(输入):
    return 假 if 输入 < 2 else (输入 == 2 if 输入 % 2 == 0 else 真)

# ── 数学_近似值 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_近似值（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_近似值）
def 近似值(输入):
    return int(输入 * 100) / 100

# ── 数学_误差计算 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_误差计算（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_误差计算）
def 误差计算(输入):
    return abs(甲 - 乙)

# ── 数学_相对误差 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_相对误差（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_相对误差）
def 相对误差(输入):
    return abs(甲 - 乙) / 乙

# ── 数学_百分比误差 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_百分比误差（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_百分比误差）
def 百分比误差(输入):
    return abs(甲 - 乙) / 乙 * 100

# ── 数学_舍入到倍数 ──
# 输入: ['数'] → 输出: 数
# 积木：数学_舍入到倍数（数学领域，自动生成）
# 契约：输入 [数] → 输出 数（数学高级运算：数学_舍入到倍数）
def 舍入到倍数(输入):
    return int(输入 / 倍数) * 倍数
