# -*- coding: utf-8 -*-
"""光明积木 · 验证领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 非空验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：非空验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：非空验证）
def 非空(输入):
    return len(文本) > 0

# ── 数字验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：数字验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：数字验证）
def 是数(输入):
    return int(文本)

# ── 长度范围 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：长度范围（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：长度范围）
def 长范围(输入):
    return len(文本) >= 甲 and len(文本) <= 乙

# ── 等于验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：等于验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：等于验证）
def 等于验(输入):
    return 甲 == 乙

# ── 大于验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：大于验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：大于验证）
def 大于验(输入):
    return 甲 > 乙

# ── 小于验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：小于验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：小于验证）
def 小于验(输入):
    return 甲 < 乙

# ── 范围验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：范围验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：范围验证）
def 范围验(输入):
    return 输入 >= 甲 and 输入 <= 乙

# ── 正数验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：正数验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：正数验证）
def 正数验(输入):
    return 输入 > 0

# ── 负数验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：负数验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：负数验证）
def 负数验(输入):
    return 输入 < 0

# ── 偶数验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：偶数验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：偶数验证）
def 偶数验(输入):
    return 输入 % 2 == 0

# ── 长度验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：长度验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：长度验证）
def 长度验证(输入):
    return len(文本) >= 甲 and len(文本) <= 乙

# ── 不等于验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：不等于验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：不等于验证）
def 不等于验证(输入):
    return 输入 != 预期

# ── 包含验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：包含验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：包含验证）
def 包含验证(输入):
    return 子 in 文本

# ── 正则验证 ──
# 输入: ['数'] → 输出: 逻辑
# 积木：正则验证（验证领域，自动生成）
# 契约：输入 [数] → 输出 逻辑（验证操作：正则验证）
def 正则验证(输入):
    return '^' in 文本
