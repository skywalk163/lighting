# -*- coding: utf-8 -*-
"""光明积木 · 数据领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 求和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求和（数据领域 L0）
# 契约：输入 [数] → 输出 数（所有元素之和）
def 汇总(列表):
    合计 = 0
    i = 0
    while i < len(列表):
        合计 = 合计 + 列表[i]
        i = i + 1
    return 合计

# ── 均值 ──
# 输入: ['列表'] → 输出: 数
# 积木：均值（数据领域 L0）
# 契约：输入 [数] → 输出 数（算术平均数）
def 中位(列表):
    合计 = 0
    i = 0
    while i < len(列表):
        合计 = 合计 + 列表[i]
        i = i + 1
    return 合计 / len(列表)

# ── 最大 ──
# 输入: ['列表'] → 输出: 数
# 积木：最大（数据领域 L0）
# 契约：输入 [数] → 输出 数（列表中的最大值）
def 极大(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] > 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前

# ── 最小 ──
# 输入: ['列表'] → 输出: 数
# 积木：最小（数据领域 L0）
# 契约：输入 [数] → 输出 数（列表中的最小值）
def 极小(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] < 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前

# ── 计数 ──
# 输入: ['列表'] → 输出: 数
# 积木：计数（数据领域 L0）
# 契约：输入 [数] → 输出 数（列表元素个数）
def 计数(列表):
    return len(列表)

# ── 范围跨度 ──
# 输入: ['列表'] → 输出: 数
# 由 光明积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：数据 · 名称：范围跨度
# 描述：列表最大值减最小值，即数据的极差/跨度，衡量离散程度
# ── 积木：最大（数据）──
def 极大(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] > 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
    # ── 积木：最小（数据）──
def 极小(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] < 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
def 跨度(序列):
    # 取最大值
    赵果1 = 极大(序列)
    # 取最小值
    赵果2 = 极小(序列)
    return 赵果1 - 赵果2

# ── 统计双指标 ──
# 输入: ['列表'] → 输出: 列表
# 由 光明积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：2 · 领域：数据 · 名称：统计双指标
# 描述：对数值列表同时给出算术均值与极差跨度，作为基础描述统计组合（L2 = L0 均值 + L1 范围跨度）
# ── 积木：均值（数据）──
def 中位(列表):
    合计 = 0
    i = 0
    while i < len(列表):
        合计 = 合计 + 列表[i]
        i = i + 1
    return 合计 / len(列表)
    # ── 积木：范围跨度（数据）──
def 极大(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] > 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
def 极小(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] < 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
def 跨度(序列):
    赵果1 = 极大(序列)
    赵果2 = 极小(序列)
    return 赵果1 - 赵果2
def 双标(序列):
    赵果1 = 中位(序列)
    赵果2 = 跨度(序列)
    return [赵果1, 赵果2]

# ── 描述统计 ──
# 输入: ['列表'] → 输出: 列表
# 由 光明积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：数据 · 名称：描述统计
# 描述：基础描述统计：对列表并行给出 均值/最大/最小/计数/方差/中位数
# ── 积木：均值（数据）──
def 中位(列表):
    合计 = 0
    i = 0
    while i < len(列表):
        合计 = 合计 + 列表[i]
        i = i + 1
    return 合计 / len(列表)
    # ── 积木：最大（数据）──
def 极大(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] > 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
    # ── 积木：最小（数据）──
def 极小(列表):
    当前 = 列表[0]
    i = 1
    while i < len(列表):
        if 列表[i] < 当前:
            当前 = 列表[i]
        i = i + 1
    return 当前
    # ── 积木：计数（数据）──
def 计数(列表):
    return len(列表)
    # ── 积木：方差（生成）──
def 方差(序列):
    计数 = len(序列)
    和 = 0
    i = 0
    while i < 计数:
        和 = 和 + 序列[i]
        i = i + 1
    均值 = 和 / 计数
    平方和 = 0
    j = 0
    while j < 计数:
        差 = 序列[j] - 均值
        平方和 = 平方和 + (差 * 差)
        j = j + 1
    return (平方和 / 计数)
    # ── 积木：中位数（生成）──
def 中位数(序列):
    排序表 = []
    i = 0
    while i < len(序列):
        排序表.append(序列[i])
        i = i + 1
    排序表.sort()
    计数 = len(排序表)
    中 = int(计数 / 2)
    if 计数 % 2 == 0:
        return ((排序表[中 - 1] + 排序表[中]) / 2)
    else:
        return 排序表[中]
def 描述统计(序列):
    赵果1 = 中位(序列)
    赵果2 = 极大(序列)
    赵果3 = 极小(序列)
    赵果4 = 计数(序列)
    赵果5 = 方差(序列)
    赵果6 = 中位数(序列)
    return [赵果1, 赵果2, 赵果3, 赵果4, 赵果5, 赵果6]

# ── 累积积 ──
# 输入: ['列表'] → 输出: 数
# 积木：累积积（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表元素做累积积操作）
def 累积(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 * 列表[i]
        i = i + 1
    return 结果

# ── 累积和 ──
# 输入: ['列表'] → 输出: 数
# 积木：累积和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表元素做累积和操作）
def 累积和(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 * 列表[i]
        i = i + 1
    return 结果

# ── 累积差 ──
# 输入: ['列表'] → 输出: 数
# 积木：累积差（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表元素做累积差操作）
def 累积差(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 - 列表[i]
        i = i + 1
    return 结果

# ── 各元素平方 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素平方（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（对列表每个元素各元素平方）
def 平方(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 列表[i])
        i = i + 1
    return 结果

# ── 各元素立方 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素立方（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（对列表每个元素各元素立方）
def 立方(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 列表[i] * 列表[i])
        i = i + 1
    return 结果

# ── 各元素取绝对值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取绝对值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（对列表每个元素各元素取绝对值）
def 取绝对值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] if 列表[i] > 0 else (0 - 列表[i]))
        i = i + 1
    return 结果

# ── 各元素取倒数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取倒数（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [小数]（对列表每个元素各元素取倒数）
def 取倒数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(1 / 列表[i])
        i = i + 1
    return 结果

# ── 求累加和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求累加和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求累加和）
def 累加和(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求累积积 ──
# 输入: ['列表'] → 输出: 数
# 积木：求累积积（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求累积积）
def 累积积(列表):
    结果 = 1
    i = 0
    while i < len(列表):
        结果 = 结果 * 列表[i]
        i = i + 1
    return 结果

# ── 求几何平均 ──
# 输入: ['列表'] → 输出: 数
# 积木：求几何平均（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求几何平均）
def 几何平均(列表):
    结果 = 1
    i = 0
    while i < len(列表):
        结果 = 结果 * 列表[i]
        i = i + 1
    return 结果

# ── 求调和平均 ──
# 输入: ['列表'] → 输出: 数
# 积木：求调和平均（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求调和平均）
def 调和平均(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 1 / 列表[i]
        i = i + 1
    return 结果

# ── 求离差平方和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求离差平方和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求离差平方和）
def 离差平方和(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 求前缀和 ──
# 输入: ['列表'] → 输出: 列表
# 积木：求前缀和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 列表（数据统计：求前缀和）
def 前缀和(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i])
        i = i + 1
    return 结果

# ── 求后缀和 ──
# 输入: ['列表'] → 输出: 列表
# 积木：求后缀和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 列表（数据统计：求后缀和）
def 后缀和(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i])
        i = i + 1
    return 结果

# ── 求最大值索引 ──
# 输入: ['列表'] → 输出: 数
# 积木：求最大值索引（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求最大值索引）
def 最大索引(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = i if 列表[i] > 列表[结果] else 结果
        i = i + 1
    return 结果

# ── 求最小值索引 ──
# 输入: ['列表'] → 输出: 数
# 积木：求最小值索引（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求最小值索引）
def 最小索引(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = i if 列表[i] < 列表[结果] else 结果
        i = i + 1
    return 结果

# ── 求绝对值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：求绝对值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 列表（数据统计：求绝对值）
def 绝对(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求加权和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求加权和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求加权和）
def 加权和(列表, 权重):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 权重[i]
        i = i + 1
    return 结果

# ── 求平方和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求平方和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求平方和）
def 平方和(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 求立方和 ──
# 输入: ['列表'] → 输出: 数
# 积木：求立方和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求立方和）
def 立方和(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i] * 列表[i] * 列表[i]
        i = i + 1
    return 结果

# ── 求均值 ──
# 输入: ['列表'] → 输出: 数
# 积木：求均值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求均值）
def 均值(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求标准差 ──
# 输入: ['列表'] → 输出: 数
# 积木：求标准差（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求标准差）
def 标准差(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求方差 ──
# 输入: ['列表'] → 输出: 数
# 积木：求方差（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求方差）
def 方差(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求中位数 ──
# 输入: ['列表'] → 输出: 数
# 积木：求中位数（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求中位数）
def 中位数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求众数 ──
# 输入: ['列表'] → 输出: 数
# 积木：求众数（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求众数）
def 众数(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 求四分位 ──
# 输入: ['列表'] → 输出: 数
# 积木：求四分位（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（数据统计：求四分位）
def 四分位(列表):
    结果 = 0
    i = 0
    while i < len(列表):
        结果 = 结果 + 列表[i]
        i = i + 1
    return 结果

# ── 数据_加权平均 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_加权平均（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（按权重计算加权平均值）
def 加权平均(输入):
    return sum(列表[i] * 权重[i]) / sum(权重)

# ── 数据_移动平均 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_移动平均（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表做3期移动平均）
def 移动平均(输入):
    return (列表[i] + 列表[i加1] + 列表[i加2]) / 3

# ── 数据_累积和 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_累积和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（从列表首到尾累加）
def 累积和(输入):
    return 结果 + 列表[i]

# ── 数据_累积积 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_累积积（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（从列表首到尾累积）
def 累积积(输入):
    return 结果 * 列表[i]

# ── 数据_差分 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_差分（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（后项减前项）
def 差分(输入):
    return 列表[i加1] - 列表[i]

# ── 数据_百分比变化 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_百分比变化（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（前后项变化百分比）
def 百分比变化(输入):
    return (列表[i] - 列表[i减1]) / 列表[i减1]

# ── 数据_归一化 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_归一化（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（缩放到0~1之间）
def 归一化(输入):
    return (列表[i] - 最小) / (最大 - 最小)

# ── 数据_标准化 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_标准化（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（中心化到均值0标准差1）
def 标准化(输入):
    return (列表[i] - 均值) / 标准差

# ── 数据_对数变换 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_对数变换（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对每个元素取自然对数）
def 对数变换(输入):
    return 列表[i].对数()

# ── 数据_平方根变换 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_平方根变换（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对每个元素取平方根）
def 平方根变换(输入):
    return 列表[i].平方根()

# ── 数据_排序 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_排序（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（对列表升序排序）
def 排序(输入):
    return 列表.sort()

# ── 数据_反转 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_反转（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（反转列表顺序）
def 反转(输入):
    return 列表.reverse()

# ── 数据_去重 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_去重（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（去掉重复元素）
def 去重(输入):
    return 列表.去重()

# ── 数据_填充 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_填充（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（用默认值填充空位）
def 填充(输入):
    return 列表

# ── 数据_线性插值 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_线性插值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（线性插值填充空位）
def 线性插值(输入):
    return 列表

# ── 数据_分箱 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_分箱（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（将数值分到bin中）
def 分箱(输入):
    return int(列表[i] / 10)

# ── 数据_排名 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_排名（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（给每个元素排名）
def 排名(输入):
    return 列表[i]

# ── 数据_分位数 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_分位数（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（计算分位数）
def 分位数(输入):
    return 列表

# ── 数据_截断 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_截断（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（将超出范围的值截断）
def 截断(输入):
    return 0 if 列表[i] < 0 else (100 if 列表[i] > 100 else 列表[i])

# ── 数据_符号 ──
# 输入: ['列表'] → 输出: 数
# 积木：数据_符号（数据领域，自动生成）
# 契约：输入 [列表] → 输出 数（每个元素的符号）
def 符号(输入):
    return 1 if 列表[i] > 0 else ((0 - 1) if 列表[i] < 0 else 0)

# ── 数据_滚动求和 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_滚动求和（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滚动求和）
def 滚动求和(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] + 列表[i加1] + 列表[i加2])
        i = i + 1
    return 结果

# ── 数据_滚动均值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_滚动均值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滚动均值）
def 滚动均值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append((列表[i] + 列表[i加1] + 列表[i加2]) / 3)
        i = i + 1
    return 结果

# ── 数据_滚动最大值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_滚动最大值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滚动最大值）
def 滚动最大值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append((列表[i] if 列表[i] > 列表[i加1] else 列表[i加2]) if 列表[i] > 列表[i加2] else (列表[i加1] if 列表[i加1] > 列表[i加2] else 列表[i加2]))
        i = i + 1
    return 结果

# ── 数据_滚动最小值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_滚动最小值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滚动最小值）
def 滚动最小值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append((列表[i] if 列表[i] < 列表[i加1] else 列表[i加2]) if 列表[i] < 列表[i加2] else (列表[i加1] if 列表[i加1] < 列表[i加2] else 列表[i加2]))
        i = i + 1
    return 结果

# ── 数据_滞后移位 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_滞后移位（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_滞后移位）
def 滞后移位(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i])
        i = i + 1
    return 结果

# ── 数据_超前移位 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_超前移位（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_超前移位）
def 超前移位(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i加1])
        i = i + 1
    return 结果

# ── 数据_累积最大值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_累积最大值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_累积最大值）
def 累积最大值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] if 列表[i] > 结果 else 结果)
        i = i + 1
    return 结果

# ── 数据_累积最小值 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_累积最小值（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_累积最小值）
def 累积最小值(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] if 列表[i] < 结果 else 结果)
        i = i + 1
    return 结果

# ── 数据_阈值裁剪 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_阈值裁剪（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_阈值裁剪）
def 阈值裁剪(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(甲 if 列表[i] < 甲 else (乙 if 列表[i] > 乙 else 列表[i]))
        i = i + 1
    return 结果

# ── 数据_平滑滤波 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数据_平滑滤波（数据领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数据高级操作：数据_平滑滤波）
def 平滑滤波(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append((列表[i] + 列表[i加1] + 列表[i加2]) / 3)
        i = i + 1
    return 结果
