# -*- coding: utf-8 -*-
"""光明积木 · 数组领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 成对相加 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对相加（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对相加）
def 对加(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] + 表乙[i])
        i = i + 1
    return 结果

# ── 成对相减 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对相减（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对相减）
def 对减(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] - 表乙[i])
        i = i + 1
    return 结果

# ── 成对相乘 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对相乘（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对相乘）
def 对乘(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] * 表乙[i])
        i = i + 1
    return 结果

# ── 成对相除 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对相除（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对相除）
def 对除(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] / 表乙[i])
        i = i + 1
    return 结果

# ── 成对取大 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对取大（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对取大）
def 对最大(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] if 表甲[i] > 表乙[i] else 表乙[i])
        i = i + 1
    return 结果

# ── 成对取小 ──
# 输入: ['列表', '列表'] → 输出: 列表
# 积木：成对取小（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（数组操作：成对取小）
def 对最小(表甲, 表乙):
    结果 = []
    i = 0
    while i < len(表甲) and i < len(表乙):
        结果.append(表甲[i] if 表甲[i] < 表乙[i] else 表乙[i])
        i = i + 1
    return 结果

# ── 滑动平均 ──
# 输入: ['列表', '数'] → 输出: 列表
# 积木：滑动平均（数组领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（对列表做滑动平均）
def 滑动平均(列表, 窗宽):
    结果 = []
    i = 0
    while i + 窗宽 <= len(列表):
        窗口 = []
        j = 0
        while j < 窗宽:
            窗口.append(列表[i + j])
            j = j + 1
        结果.append(0)
        i = i + 1
    return 结果

# ── 滑动最大 ──
# 输入: ['列表', '数'] → 输出: 列表
# 积木：滑动最大（数组领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（对列表做滑动最大）
def 滑动最大(列表, 窗宽):
    结果 = []
    i = 0
    while i + 窗宽 <= len(列表):
        窗口 = []
        j = 0
        while j < 窗宽:
            窗口.append(列表[i + j])
            j = j + 1
        结果.append(0)
        i = i + 1
    return 结果

# ── 滑动最小 ──
# 输入: ['列表', '数'] → 输出: 列表
# 积木：滑动最小（数组领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（对列表做滑动最小）
def 滑动最小(列表, 窗宽):
    结果 = []
    i = 0
    while i + 窗宽 <= len(列表):
        窗口 = []
        j = 0
        while j < 窗宽:
            窗口.append(列表[i + j])
            j = j + 1
        结果.append(0)
        i = i + 1
    return 结果

# ── 各元素加常数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素加常数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素加常数）
def 各加常数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] + 常数)
        i = i + 1
    return 结果

# ── 各元素乘常数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素乘常数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素乘常数）
def 各乘常数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 常数)
        i = i + 1
    return 结果

# ── 各元素取整 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取整（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取整）
def 各取整(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(int(列表[i]))
        i = i + 1
    return 结果

# ── 各元素取小数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取小数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取小数）
def 各取小数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] - int(列表[i]))
        i = i + 1
    return 结果

# ── 各元素取绝对 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取绝对（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取绝对）
def 各绝对(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] if 列表[i] > 0 else (0 - 列表[i]))
        i = i + 1
    return 结果

# ── 各元素取对数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取对数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取对数）
def 各对数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i].对数())
        i = i + 1
    return 结果

# ── 各元素取指数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取指数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取指数）
def 各指数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i].指数())
        i = i + 1
    return 结果

# ── 各元素取平方根 ──
# 输入: ['列表'] → 输出: 列表
# 积木：各元素取平方根（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组操作：各元素取平方根）
def 各平方根(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i].平方根())
        i = i + 1
    return 结果

# ── 数组_各元素加一 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素加一（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素加一）
def 各加一(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] + 1)
        i = i + 1
    return 结果

# ── 数组_各元素减一 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素减一（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素减一）
def 各减一(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] - 1)
        i = i + 1
    return 结果

# ── 数组_各元素翻倍 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素翻倍（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素翻倍）
def 各翻倍(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 2)
        i = i + 1
    return 结果

# ── 数组_各元素减半 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素减半（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素减半）
def 各减半(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] / 2)
        i = i + 1
    return 结果

# ── 数组_各元素取反 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素取反（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素取反）
def 各取反(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(0 - 列表[i])
        i = i + 1
    return 结果

# ── 数组_各元素加百分 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素加百分（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素加百分）
def 各加百分(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 1.1)
        i = i + 1
    return 结果

# ── 数组_各元素减百分 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素减百分（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素减百分）
def 各减百分(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] * 0.9)
        i = i + 1
    return 结果

# ── 数组_各元素取整 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素取整（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素取整）
def 各取整(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(int(列表[i]))
        i = i + 1
    return 结果

# ── 数组_各元素取小数 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素取小数（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素取小数）
def 各取小数(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(列表[i] - int(列表[i]))
        i = i + 1
    return 结果

# ── 数组_各元素取符号 ──
# 输入: ['列表'] → 输出: 列表
# 积木：数组_各元素取符号（数组领域，自动生成）
# 契约：输入 [列表] → 输出 [数]（数组高级操作：数组_各元素取符号）
def 各取符号(列表):
    结果 = []
    i = 0
    while i < len(列表):
        结果.append(1 if 列表[i] > 0 else ((0 - 1) if 列表[i] < 0 else 0))
        i = i + 1
    return 结果
