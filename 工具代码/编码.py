# -*- coding: utf-8 -*-
"""光明积木 · 编码领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 十六进制编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：十六进制编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：十六进制编码）
def 十六(文本):
    return 文本.encode('hex')

# ── 十六进制解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：十六进制解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：十六进制解码）
def 十六解(文本):
    return 文本.decode('hex')

# ── 二进制编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：二进制编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：二进制编码）
def 二进制(文本):
    return int(输入).encode('bin')

# ── 八进制编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：八进制编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：八进制编码）
def 八进制(文本):
    return int(输入).encode('oct')

# ── ASCII编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：ASCII编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：ASCII编码）
def ASCII(文本):
    return int(输入).encode('chr')

# ── ASCII解码 ──
# 输入: ['文本'] → 输出: 数
# 积木：ASCII解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 数（编码操作：ASCII解码）
def ASCII解(文本):
    return 文本.decode('ord')

# ── Unicode编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Unicode编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Unicode编码）
def Unicode(文本):
    return 文本.encode('unicode')

# ── Unicode解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Unicode解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Unicode解码）
def Unicode解(文本):
    return 文本.decode('unicode')

# ── URL安全编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL安全编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：URL安全编码）
def URL安全(文本):
    return 文本.replace('=', '~').replace('/', '_')

# ── URL安全解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL安全解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：URL安全解码）
def URL安全解(文本):
    return 文本.replace('~', '=').replace('_', '/')

# ── Base32编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base32编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Base32编码）
def B32编码(输入):
    return 输入.encode('base32')

# ── Base32解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base32解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Base32解码）
def B32解码(输入):
    return 输入.decode('base32')

# ── Base16编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base16编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Base16编码）
def B16编码(输入):
    return 输入.encode('base16')

# ── Base16解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Base16解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Base16解码）
def B16解码(输入):
    return 输入.decode('base16')

# ── QuotedPrintable ──
# 输入: ['文本'] → 输出: 文本
# 积木：QuotedPrintable（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：QuotedPrintable）
def QP编码(输入):
    return 输入.encode('qp')

# ── QuotedPrintable解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：QuotedPrintable解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：QuotedPrintable解码）
def QP解码(输入):
    return 输入.decode('qp')

# ── URL编码增强 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL编码增强（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：URL编码增强）
def URL编码增强(输入):
    return 输入.encode('url')

# ── URL解码增强 ──
# 输入: ['文本'] → 输出: 文本
# 积木：URL解码增强（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：URL解码增强）
def URL解码增强(输入):
    return 输入.decode('url')

# ── Unicode转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Unicode转义（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Unicode转义）
def Unicode转义(输入):
    return 输入.encode('unicode-escape')

# ── Unicode反转义 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Unicode反转义（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Unicode反转义）
def Unicode反(输入):
    return 输入.decode('unicode-escape')

# ── Latin1编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Latin1编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Latin1编码）
def Latin1编码(输入):
    return 输入.encode('latin1')

# ── Latin1解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：Latin1解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：Latin1解码）
def Latin1解码(输入):
    return 输入.decode('latin1')

# ── UTF7编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：UTF7编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：UTF7编码）
def UTF7编码(输入):
    return 输入.encode('utf-7')

# ── UTF7解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：UTF7解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：UTF7解码）
def UTF7解码(输入):
    return 输入.decode('utf-7')

# ── ROT13编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：ROT13编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：ROT13编码）
def ROT13(输入):
    return 输入.encode('rot13')

# ── ROT13解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：ROT13解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：ROT13解码）
def ROT13解(输入):
    return 输入.decode('rot13')

# ── 字符集转换 ──
# 输入: ['文本'] → 输出: 文本
# 积木：字符集转换（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：字符集转换）
def 字符集转换(输入):
    return 输入.encode('utf-8')

# ── 字符集反转 ──
# 输入: ['文本'] → 输出: 文本
# 积木：字符集反转（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码操作：字符集反转）
def 字符集反转(输入):
    return 输入.decode('utf-8')

# ── 编码_HEX转Base64 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_HEX转Base64（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（十六进制转Base64）
def HEX转Base64(输入):
    return 文本.encode('base64')

# ── 编码_Base64转HEX ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Base64转HEX（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Base64转十六进制）
def Base64转HEX(输入):
    return 文本.decode('base64').encode('hex')

# ── 编码_字符串转二进制 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_字符串转二进制（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（字符串转二进制）
def 字符串转二进制(输入):
    return 文本.encode('bin')

# ── 编码_二进制转字符串 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_二进制转字符串（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（二进制转字符串）
def 二进制转字符串(输入):
    return 文本.decode('bin')

# ── 编码_字符串转八进制 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_字符串转八进制（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（字符串转八进制）
def 字符串转八进制(输入):
    return 文本.encode('oct')

# ── 编码_八进制转字符串 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_八进制转字符串（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（八进制转字符串）
def 八进制转字符串(输入):
    return 文本.decode('oct')

# ── 编码_URL安全编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_URL安全编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（URL安全编码）
def URL安全编码(输入):
    return 文本.replace('=', '~').replace('/', '_')

# ── 编码_URL安全解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_URL安全解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（URL安全解码）
def URL安全解码(输入):
    return 文本.replace('~', '=').replace('_', '/')

# ── 编码_Unicode编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Unicode编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Unicode编码）
def Unicode编码(输入):
    return 文本.encode('unicode-escape')

# ── 编码_Unicode解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Unicode解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Unicode解码）
def Unicode解码(输入):
    return 文本.decode('unicode-escape')

# ── 编码_UTF8编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF8编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（UTF8编码）
def UTF8编码(输入):
    return 文本.encode('utf-8')

# ── 编码_UTF8解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF8解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（UTF8解码）
def UTF8解码(输入):
    return 文本.decode('utf-8')

# ── 编码_UTF16编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF16编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（UTF16编码）
def UTF16编码(输入):
    return 文本.encode('utf-16')

# ── 编码_UTF16解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF16解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（UTF16解码）
def UTF16解码(输入):
    return 文本.decode('utf-16')

# ── 编码_ASCII编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_ASCII编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（ASCII编码）
def ASCII编码(输入):
    return 文本.encode('ascii')

# ── 编码_ASCII解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_ASCII解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（ASCII解码）
def ASCII解码(输入):
    return 文本.decode('ascii')

# ── 编码_Latin1编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Latin1编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Latin1编码）
def Latin1编码(输入):
    return 文本.encode('latin-1')

# ── 编码_Latin1解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Latin1解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（Latin1解码）
def Latin1解码(输入):
    return 文本.decode('latin-1')

# ── 编码_Base85编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Base85编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_Base85编码）
def B85编码(输入):
    return 文本.encode('base85')

# ── 编码_Base85解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Base85解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_Base85解码）
def B85解码(输入):
    return 文本.decode('base85')

# ── 编码_Base91编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Base91编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_Base91编码）
def B91编码(输入):
    return 文本.encode('base91')

# ── 编码_Base91解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_Base91解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_Base91解码）
def B91解码(输入):
    return 文本.decode('base91')

# ── 编码_百分号编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_百分号编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_百分号编码）
def 百分号编码(输入):
    return 文本.replace(' ', '%20').replace('/', '%2F')

# ── 编码_百分号解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_百分号解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_百分号解码）
def 百分号解码(输入):
    return 文本.replace('%20', ' ').replace('%2F', '/')

# ── 编码_UTF7编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF7编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_UTF7编码）
def UTF7编码(输入):
    return 文本.encode('utf-7')

# ── 编码_UTF7解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_UTF7解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_UTF7解码）
def UTF7解码(输入):
    return 文本.decode('utf-7')

# ── 编码_EBCDIC编码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_EBCDIC编码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_EBCDIC编码）
def EBCDIC编码(输入):
    return 文本.encode('ebcdic')

# ── 编码_EBCDIC解码 ──
# 输入: ['文本'] → 输出: 文本
# 积木：编码_EBCDIC解码（编码领域，自动生成）
# 契约：输入 [文本] → 输出 文本（编码高级操作：编码_EBCDIC解码）
def EBCDIC解码(输入):
    return 文本.decode('ebcdic')
