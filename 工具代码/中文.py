# -*- coding: utf-8 -*-
"""光明积木 · 中文领域工具代码

自动生成于 2026-08-10
基于光明积木库 v0.2.0
"""

import math, time, random, hashlib, zlib

# ── 数字转中文 ──
# 输入: ['数'] → 输出: 文本
# 积木：数字转中文（中文领域 L0）
# 契约：输入 数(整数) → 输出 文本（中文读法，如 123 → 一百二十三）
# 支持到万亿级；负数前缀「负」；10~19 按口语省略首位「一」
def 念数(数值):
    数字表 = ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    权表 = ["", "十", "百", "千"]
    级表 = ["", "万", "亿", "万亿"]
    整值 = int(数值)
    if 整值 == 0:
        return "零"
    负号 = 0
    if 整值 < 0:
        负号 = 1
        整值 = 0 - 整值
    原值 = 整值
    结果 = ""
    级号 = 0
    while 整值 > 0:
        本级 = 整值 % 10000
        整值 = 整值 // 10000
        if 本级 > 0:
            段文 = ""
            权号 = 0
            零标 = 0
            余量 = 本级
            while 余量 > 0:
                当位 = 余量 % 10
                if 当位 == 0:
                    if len(段文) > 0:
                        零标 = 1
                else:
                    片段 = 数字表[当位] + 权表[权号]
                    if 零标 == 1:
                        片段 = 片段 + "零"
                        零标 = 0
                    段文 = 片段 + 段文
                余量 = 余量 // 10
                权号 = 权号 + 1
            if 整值 > 0 and 本级 < 1000:
                段文 = "零" + 段文
            结果 = 段文 + 级表[级号] + 结果
        级号 = 级号 + 1
    if 原值 >= 10 and 原值 < 20:
        结果 = 结果[1:len(结果)]
    if 负号 == 1:
        结果 = "负" + 结果
    return 结果

# ── 中文转数字 ──
# 输入: ['文本'] → 输出: 数
# 积木：中文转数字（中文领域 L0）
# 契约：输入 文本 → 输出 数（中文读法转整数，如 一百二十三 → 123）
# 与「数字转中文」互逆；支持 十/百/千/万/亿 与省略首位的「十三」
def 读数(文本):
    数字表 = ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    总计 = 0
    级值 = 0
    当前 = 0
    负号 = 0
    i = 0
    while i < len(文本):
        字 = 文本[i]
        if 字 == "负":
            负号 = 1
        else:
            数位 = 0 - 1
            k = 0
            while k < 10:
                if 字 == 数字表[k]:
                    数位 = k
                k = k + 1
            if 数位 >= 0:
                当前 = 数位
            elif 字 == "十":
                if 当前 == 0:
                    当前 = 1
                级值 = 级值 + (当前 * 10)
                当前 = 0
            elif 字 == "百":
                级值 = 级值 + (当前 * 100)
                当前 = 0
            elif 字 == "千":
                级值 = 级值 + (当前 * 1000)
                当前 = 0
            elif 字 == "万":
                总计 = 总计 + ((级值 + 当前) * 10000)
                级值 = 0
                当前 = 0
            elif 字 == "亿":
                总计 = (总计 + 级值 + 当前) * 100000000
                级值 = 0
                当前 = 0
        i = i + 1
    结果 = 总计 + 级值 + 当前
    if 负号 == 1:
        结果 = 0 - 结果
    return 结果

# ── 人民币大写 ──
# 输入: ['数'] → 输出: 文本
# 积木：人民币大写（中文领域 L0）
# 契约：输入 数(金额) → 输出 文本（财务大写，如 1234.56 → 壹仟贰佰叁拾肆元伍角陆分）
# 四舍五入到分；整元补「整」；负数前缀「负」
def 大写(数值):
    大字表 = ["零", "壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖"]
    权表 = ["", "拾", "佰", "仟"]
    级表 = ["", "万", "亿", "万亿"]
    负号 = 0
    金额 = 数值
    if 金额 < 0:
        负号 = 1
        金额 = 0 - 金额
    分总 = int((金额 * 100) + 0.5)
    元数 = 分总 // 100
    角数 = (分总 // 10) % 10
    分数 = 分总 % 10
    整文 = ""
    if 元数 == 0:
        整文 = "零"
    else:
        级号 = 0
        余元 = 元数
        while 余元 > 0:
            本级 = 余元 % 10000
            余元 = 余元 // 10000
            if 本级 > 0:
                段文 = ""
                权号 = 0
                零标 = 0
                余量 = 本级
                while 余量 > 0:
                    当位 = 余量 % 10
                    if 当位 == 0:
                        if len(段文) > 0:
                            零标 = 1
                    else:
                        片段 = 大字表[当位] + 权表[权号]
                        if 零标 == 1:
                            片段 = 片段 + "零"
                            零标 = 0
                        段文 = 片段 + 段文
                    余量 = 余量 // 10
                    权号 = 权号 + 1
                if 余元 > 0 and 本级 < 1000:
                    段文 = "零" + 段文
                整文 = 段文 + 级表[级号] + 整文
            级号 = 级号 + 1
    结果 = 整文 + "元"
    if 角数 == 0 and 分数 == 0:
        结果 = 结果 + "整"
    else:
        if 角数 > 0:
            结果 = 结果 + 大字表[角数] + "角"
        else:
            结果 = 结果 + "零"
        if 分数 > 0:
            结果 = 结果 + 大字表[分数] + "分"
    if 负号 == 1:
        结果 = "负" + 结果
    return 结果

# ── 中文数字 ──
# 输入: ['数'] → 输出: 列表
# 由 光明积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：中文 · 名称：中文数字
# 描述：中文数字能力：对数值给出 人民币大写 与 中文读数
# ── 积木：人民币大写（中文）──
def 大写(数值):
    大字表 = ["零", "壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖"]
    权表 = ["", "拾", "佰", "仟"]
    级表 = ["", "万", "亿", "万亿"]
    负号 = 0
    金额 = 数值
    if 金额 < 0:
        负号 = 1
        金额 = 0 - 金额
    分总 = int((金额 * 100) + 0.5)
    元数 = 分总 // 100
    角数 = (分总 // 10) % 10
    分数 = 分总 % 10
    整文 = ""
    if 元数 == 0:
        整文 = "零"
    else:
        级号 = 0
        余元 = 元数
        while 余元 > 0:
            本级 = 余元 % 10000
            余元 = 余元 // 10000
            if 本级 > 0:
                段文 = ""
                权号 = 0
                零标 = 0
                余量 = 本级
                while 余量 > 0:
                    当位 = 余量 % 10
                    if 当位 == 0:
                        if len(段文) > 0:
                            零标 = 1
                    else:
                        片段 = 大字表[当位] + 权表[权号]
                        if 零标 == 1:
                            片段 = 片段 + "零"
                            零标 = 0
                        段文 = 片段 + 段文
                    余量 = 余量 // 10
                    权号 = 权号 + 1
                if 余元 > 0 and 本级 < 1000:
                    段文 = "零" + 段文
                整文 = 段文 + 级表[级号] + 整文
            级号 = 级号 + 1
    结果 = 整文 + "元"
    if 角数 == 0 and 分数 == 0:
        结果 = 结果 + "整"
    else:
        if 角数 > 0:
            结果 = 结果 + 大字表[角数] + "角"
        else:
            结果 = 结果 + "零"
        if 分数 > 0:
            结果 = 结果 + 大字表[分数] + "分"
    if 负号 == 1:
        结果 = "负" + 结果
    return 结果
    # ── 积木：数字转中文（中文）──
def 念数(数值):
    数字表 = ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九"]
    权表 = ["", "十", "百", "千"]
    级表 = ["", "万", "亿", "万亿"]
    整值 = int(数值)
    if 整值 == 0:
        return "零"
    负号 = 0
    if 整值 < 0:
        负号 = 1
        整值 = 0 - 整值
    原值 = 整值
    结果 = ""
    级号 = 0
    while 整值 > 0:
        本级 = 整值 % 10000
        整值 = 整值 // 10000
        if 本级 > 0:
            段文 = ""
            权号 = 0
            零标 = 0
            余量 = 本级
            while 余量 > 0:
                当位 = 余量 % 10
                if 当位 == 0:
                    if len(段文) > 0:
                        零标 = 1
                else:
                    片段 = 数字表[当位] + 权表[权号]
                    if 零标 == 1:
                        片段 = 片段 + "零"
                        零标 = 0
                    段文 = 片段 + 段文
                余量 = 余量 // 10
                权号 = 权号 + 1
            if 整值 > 0 and 本级 < 1000:
                段文 = "零" + 段文
            结果 = 段文 + 级表[级号] + 结果
        级号 = 级号 + 1
    if 原值 >= 10 and 原值 < 20:
        结果 = 结果[1:len(结果)]
    if 负号 == 1:
        结果 = "负" + 结果
    return 结果
def 中文数字(值):
    赵果1 = 大写(值)
    赵果2 = 念数(值)
    return [赵果1, 赵果2]

# ── 简体转繁体 ──
# 输入: ['文本'] → 输出: 文本
# 积木：简体转繁体（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：简体转繁体）
def 简转繁(文本):
    return 文本.replace('体', '體')

# ── 繁体转简体 ──
# 输入: ['文本'] → 输出: 文本
# 积木：繁体转简体（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：繁体转简体）
def 繁转简(文本):
    return 文本.replace('體', '体')

# ── 取拼音首字母 ──
# 输入: ['文本'] → 输出: 文本
# 积木：取拼音首字母（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：取拼音首字母）
def 拼音首(文本):
    return 文本[0:1]

# ── 颠倒中文 ──
# 输入: ['文本'] → 输出: 文本
# 积木：颠倒中文（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：颠倒中文）
def 颠倒(文本):
    return 文本.reverse()

# ── 中文计数 ──
# 输入: ['文本'] → 输出: 数
# 积木：中文计数（中文领域，自动生成）
# 契约：输入 [文本] → 输出 数（中文处理：中文计数）
def 中计数(文本):
    return len(文本)

# ── 去中文 ──
# 输入: ['文本'] → 输出: 文本
# 积木：去中文（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：去中文）
def 去中文(文本):
    return 文本

# ── 取中文 ──
# 输入: ['文本'] → 输出: 文本
# 积木：取中文（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：取中文）
def 取中文(文本):
    return 文本

# ── 中文补零 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文补零（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文补零）
def 补零(文本):
    return '0' + 文本

# ── 中文去空格 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文去空格（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文去空格）
def 中去空(文本):
    return 文本.replace(' ', '')

# ── 中文加空格 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文加空格（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文加空格）
def 中加空(文本):
    return 文本.split('').join(' ')

# ── 中文数字转整数 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文数字转整数（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文数字转整数）
def 中文转整数(文本):
    return 文本.replace('一', '1').replace('二', '2').replace('三', '3').replace('四', '4').replace('五', '5').replace('六', '6').replace('七', '7').replace('八', '8').replace('九', '9').replace('零', '0')

# ── 整数转中文数字 ──
# 输入: ['文本'] → 输出: 文本
# 积木：整数转中文数字（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：整数转中文数字）
def 整数转中文(文本):
    return 1

# ── 中文括号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文括号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文括号）
def 中文括号(文本):
    return '(' + 文本 + ')'

# ── 中文书名号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文书名号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文书名号）
def 中文书名号(文本):
    return '《' + 文本 + '》'

# ── 中文引号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文引号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文引号）
def 中文引号(文本):
    return '「' + 文本 + '」'

# ── 中文逗号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文逗号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文逗号）
def 中文逗号(文本):
    return 文本 + ','

# ── 中文句号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文句号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文句号）
def 中文句号(文本):
    return 文本 + '。'

# ── 中文顿号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文顿号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文顿号）
def 中文顿号(文本):
    return 文本 + '、'

# ── 中文冒号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文冒号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文冒号）
def 中文冒号(文本):
    return 文本 + ':'

# ── 中文分号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文分号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文分号）
def 中文分号(文本):
    return 文本 + '；'

# ── 中文问号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文问号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文问号）
def 中文问号(文本):
    return 文本 + '？'

# ── 中文感叹号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文感叹号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文感叹号）
def 中文感叹号(文本):
    return 文本 + '！'

# ── 中文省略号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文省略号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文省略号）
def 中文省略号(文本):
    return 文本 + '……'

# ── 中文破折号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文破折号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文破折号）
def 中文破折号(文本):
    return 文本 + '——'

# ── 中文间隔号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文间隔号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文间隔号）
def 中文间隔号(文本):
    return 文本 + '·'

# ── 中文连接号 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文连接号（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文连接号）
def 中文连接号(文本):
    return 文本 + '—'

# ── 中文书名号包裹 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文书名号包裹（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文书名号包裹）
def 中文书名号包裹(文本):
    return '《' + 文本 + '》'

# ── 中文引号包裹 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文引号包裹（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文引号包裹）
def 中文引号包裹(文本):
    return '「' + 文本 + '」'

# ── 中文括号包裹 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文括号包裹（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文处理：中文括号包裹）
def 中文括号包裹(文本):
    return '(' + 文本 + ')'

# ── 中文_金额大写 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_金额大写（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_金额大写）
def 金额大写(文本):
    return 文本

# ── 中文_日期大写 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_日期大写（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_日期大写）
def 日期大写(文本):
    return 文本

# ── 中文_数字大写 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_数字大写（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_数字大写）
def 数字大写(文本):
    return 文本.replace('1', '壹').replace('2', '贰').replace('3', '叁').replace('4', '肆').replace('5', '伍').replace('6', '陆').replace('7', '柒').replace('8', '捌').replace('9', '玖').replace('0', '零')

# ── 中文_数字小写 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_数字小写（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_数字小写）
def 数字小写(文本):
    return 文本.replace('壹', '1').replace('贰', '2').replace('叁', '3').replace('肆', '4').replace('伍', '5').replace('陆', '6').replace('柒', '7').replace('捌', '8').replace('玖', '9').replace('零', '0')

# ── 中文_金额转数字 ──
# 输入: ['文本'] → 输出: 数
# 积木：中文_金额转数字（中文领域，自动生成）
# 契约：输入 [文本] → 输出 数（中文高级处理：中文_金额转数字）
def 金额转数字(文本):
    return 文本

# ── 中文_数字转金额 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_数字转金额（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_数字转金额）
def 数字转金额(文本):
    return 文本

# ── 中文_星期转换 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_星期转换（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_星期转换）
def 星期转换(文本):
    return 文本.replace('周一', '星期一').replace('周二', '星期二').replace('周三', '星期三').replace('周四', '星期四').replace('周五', '星期五').replace('周六', '星期六').replace('周日', '星期日')

# ── 中文_月份转换 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_月份转换（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_月份转换）
def 月份转换(文本):
    return 文本.replace('1月', '一月').replace('2月', '二月').replace('3月', '三月').replace('4月', '四月').replace('5月', '五月').replace('6月', '六月').replace('7月', '七月').replace('8月', '八月').replace('9月', '九月')

# ── 中文_全角转半角 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_全角转半角（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_全角转半角）
def 全角转半角(文本):
    return 文本.replace(',', ',').replace('。', '.').replace('！', '!').replace('？', '?').replace('(', '(').replace(')', ')')

# ── 中文_半角转全角 ──
# 输入: ['文本'] → 输出: 文本
# 积木：中文_半角转全角（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（中文高级处理：中文_半角转全角）
def 半角转全角(文本):
    return 文本.replace(',', ',').replace('.', '。').replace('!', '！').replace('?', '？').replace('(', '(').replace(')', ')')
