# 语料脚手架报告（v0.27）

> 生成文件：`评估/语料500.json`；脚手架脚本：`评估/语料脚手架.py`

## 一、总览

- **总条数**：554（目标 ≥500，已达成 ✅）
- 现有真实条目：200（调参 70 / 留出 130，段保持不变）
- 新派生条目：354（调参 258 / 留出 96）
- **合计调参 / 留出**：328 / 226（留出占比 40.8%）
- 新派生 段 切分：确定性哈希（导出名 md5 %4==0 → 留出，新派生留出占比约 27.1%，约 25%；留出只测不调）。合计留出占比偏高（40.8%）是因为既有 200 条真实语料已含 130 条留出，本脚手架未改动其 段。

## 二、召回验证通过率

- 派生候选总数（含被过滤掉的含块名候选）：369
- 经 hybrid_select 召回校验通过（导出名落入 top-3）：354
- 最终保留（每块上限 3 条，均衡覆盖）：354
- **验证通过率：95.93%**（保留 / 候选）
- 校验口径：零 token 调 `hybrid_select(需求, 索引, top=3)`，目标块**导出名**出现在 top-3 候选即保留；仅保留召回成功的需求，确保语料真能测出 Hit@1 而非噪声。

## 三、每领域派生条数（新派生）

| 领域 | 派生条数 |
|------|--------:|
| 中文 | 18 |
| 几何 | 15 |
| 工具 | 66 |
| 数据 | 48 |
| 文本 | 42 |
| 日期 | 39 |
| 校验 | 21 |
| 格式 | 51 |
| 网络 | 18 |
| 财务 | 18 |
| 集合 | 18 |

- 被派生覆盖的领域数：11 / 12
- 未被派生覆盖的领域：生成

## 四、诚实声明（务必阅读）

1. **这是合成种子语料，不是人工采集的真实用户需求。** 每条派生需求由块的`描述/名称/领域` 经模板改写（刻意避免直接出现块名用字）生成，仅用于把语料规模从 200 推到 500+ 并为评分器提供机制化、可复现的种子。
2. **生产级评测仍需补充真实需求。** 真实用户的自然表述、长尾意图、扰动说法（迂回/口语/错别字）需从真实日志或人工采集中获得；本脚手架提供的是“200→500+”的量级跨越与可复现机制，不是质量替代。
3. **关于 `期望块` 字段：** 任务书称其应为块「导出名」，但经核对 `真实跑分.py` 以候选 `名称` 字段与 `期望块` 比对，且现有 200 条（Hit@1=1.0）实际存储的也是块 `名称`。为使 `语料500.json` 能被评分器直接正确消费，本脚手架将 `期望块` 取为块 **名称**（与候选 `名称` 同字段）；召回校验阶段仍以块 **导出名** 锁定目标块。对于 名称==导出名 的块二者等价。
4. **未触碰索引与生产链路：** 全程只读 `索引.json`/`真实语料.json`/日志，未注册/注销任何块，未写入 `生成/`。仅产出 `语料500.json` 与本报告。
