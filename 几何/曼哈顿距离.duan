# 积木：曼哈顿距离（几何领域 L0）
# 契约：计算两点间的曼哈顿距离（横纵差绝对值之和）
导出 曼哈顿
段落 曼哈顿 接收 x1，y1，x2，y2：
    设 dx 为 x2 减 x1
    设 dy 为 y2 减 y1
    如果 dx 小于 0：
        设 dx 为 0 减 dx
    如果 dy 小于 0：
        设 dy 为 0 减 dy
    返回 dx 加 dy
