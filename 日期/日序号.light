# 积木：日序号（日期领域 L0）
# 契约：输入 年, 月, 日 → 输出 数（该日期是当年的第几天，1 起）
导出 日序号
段落 日序号 接收 年入, 月入, 天入：
    设 常表 为 [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]
    设 岁值 为 整数(年入)
    设 月值 为 整数(月入)
    设 累计 为 0
    设 i 为 0
    当 i 小于 月值 减 1：
        设 累计 为 累计 加 常表[i]
        设 i 为 i 加 1
    如果 月值 大于 2：
        如果 岁值 模 400 等于 0：
            设 累计 为 累计 加 1
        否则如果 岁值 模 100 等于 0：
            设 累计 为 累计 加 0
        否则如果 岁值 模 4 等于 0：
            设 累计 为 累计 加 1
    返回 累计 加 整数(天入)
