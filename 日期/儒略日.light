# 积木：儒略日（日期领域 L0）
# 契约：输入 年, 月, 日 → 输出 数（儒略日序数，用于任意两日期做天数运算）
# 用标准 Fliegel 公式，所有除法的被除数恒为非负，规避 整数() 向零截断的坑
# 命名注意：不可用「己」做变量名——它会被转译成 self
导出 儒略日
段落 儒略日 接收 年入, 月入, 天入：
    设 岁值 为 整数(年入)
    设 月值 为 整数(月入)
    设 天值 为 整数(天入)
    设 补月 为 整数((14 减 月值) 除 12)
    设 年基 为 岁值 加 4800 减 补月
    设 月基 为 月值 加 12 乘 补月 减 3
    设 月项 为 整数((153 乘 月基 加 2) 除 5)
    设 年项 为 365 乘 年基
    设 闰甲 为 整数(年基 除 4)
    设 闰乙 为 整数(年基 除 100)
    设 闰丙 为 整数(年基 除 400)
    返回 天值 加 月项 加 年项 加 闰甲 减 闰乙 加 闰丙 减 32045
