# 积木：时间格式（日期领域 L0）
# 契约：输入 时间戳(数), 模板(文本) → 输出 文本（把 Unix 时间戳格式化成可读日期）
# 命名注意：参数不可叫「模板」——「模」是取模运算符，会被词法器切开
导出 时间格式
段落 时间格式 接收 秒数, 样式：
    设 串式 为 样式
    如果 串式.长度() 等于 0：
        设 串式 为 "%Y-%m-%d"
    返回 格式化时间(秒数, 串式)
