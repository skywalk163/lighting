# 积木：日期文本（日期领域 L0）
# 契约：输入 年, 月, 日 → 输出 文本（补零成 YYYY-MM-DD）
导出 日期文本
段落 日期文本 接收 年份, 月份, 日子：
    设 年文 为 字符串(整数(年份))
    设 月文 为 字符串(整数(月份))
    设 日文 为 字符串(整数(日子))
    当 年文.长度() 小于 4：
        设 年文 为 "0" 加 年文
    如果 月文.长度() 小于 2：
        设 月文 为 "0" 加 月文
    如果 日文.长度() 小于 2：
        设 日文 为 "0" 加 日文
    返回 年文 加 "-" 加 月文 加 "-" 加 日文
