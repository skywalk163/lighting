# 积木：闰年（日期领域 L0）
# 契约：输入 年(数) → 输出 逻辑（是否闰年，格里高利历规则）
# 命名注意：局部名不得是参数名的前缀，否则词法器会把参数切碎
导出 判闰年
段落 判闰年 接收 年入：
    设 岁值 为 整数(年入)
    如果 岁值 模 400 等于 0：
        返回 真
    如果 岁值 模 100 等于 0：
        返回 假
    如果 岁值 模 4 等于 0：
        返回 真
    返回 假
