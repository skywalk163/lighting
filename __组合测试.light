# 由 光明积木桥接 v0（选块 + 内联粘合）自动合成

# 需求：对一组数字求和

# ── 积木：求和（数据）──
段落 求和 接收 列表：
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 结果 为 结果 加 列表[i]
        设 i 为 i 加 1
    返回 结果

# ── 积木：数组求和（数组）──
段落 数组求和 接收 输入：
    返回 数组求和(输入)

# ── 积木：求和数组（数组）──
段落 求和数组 接收 列表：
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 结果 为 结果 加 列表[i]
        设 i 为 i 加 1
    返回 结果

设 赵料 为 [1, 2, 3, 4, 5]。

设 赵果1 为 求和(赵料)。
设 赵果2 为 数组求和(赵料)。
设 赵果3 为 求和数组(赵料)。

打印(赵果1)。
打印(赵果2)。
打印(赵果3)。
