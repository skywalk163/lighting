# 积木：文本_提取数字（文本领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文本高级操作：文本_提取数字）
导出 提取数字
段落 提取数字 接收 文本：
    返回 文本.替换('零', '0').替换('一', '1').替换('二', '2').替换('三', '3').替换('四', '4').替换('五', '5').替换('六', '6').替换('七', '7').替换('八', '8').替换('九', '9')
