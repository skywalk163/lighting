# 积木：包含（文本领域 L0）
# 契约：输入 文本, 子 → 输出 逻辑（子串是否出现在文本中，真/假）
导出 含有
段落 含有 接收 文本, 子：
    设 总长 为 长度(文本)
    设 子长 为 长度(子)
    如果 子长 等于 0：
        返回 真
    如果 子长 大于 总长：
        返回 假
    设 上限 为 总长 减 子长
    设 i 为 0
    当 i 小于等于 上限：
        设 匹 为 1
        设 j 为 0
        当 j 小于 子长：
            如果 文本[i 加 j] 不等于 子[j]：
                设 匹 为 0
            设 j 为 j 加 1
        如果 匹 等于 1：
            返回 真
        设 i 为 i 加 1
    返回 假
