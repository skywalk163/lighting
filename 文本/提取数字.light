# 积木：提取数字（文本领域 L0）
# 契约：输入 文本 → 输出 列表（按出现顺序抽出其中所有数字，支持小数）
导出 抽数
段落 抽数 接收 文本：
    设 结果 为 []
    设 缓冲 为 ""
    设 有点 为 0
    设 i 为 0
    当 i 小于 长度(文本)：
        设 字 为 文本[i]
        设 是数 为 0
        如果 字 大于等于 "0" 且 字 小于等于 "9"：
            设 是数 为 1
        如果 是数 等于 1：
            设 缓冲 为 缓冲 加 字
        否则如果 字 等于 "." 且 长度(缓冲) 大于 0 且 有点 等于 0：
            设 缓冲 为 缓冲 加 字
            设 有点 为 1
        否则：
            如果 长度(缓冲) 大于 0：
                如果 有点 等于 1：
                    结果.追加(转浮点(缓冲))
                否则：
                    结果.追加(转整数(缓冲))
                设 缓冲 为 ""
                设 有点 为 0
        设 i 为 i 加 1
    如果 长度(缓冲) 大于 0：
        如果 有点 等于 1：
            结果.追加(转浮点(缓冲))
        否则：
            结果.追加(转整数(缓冲))
    返回 结果
