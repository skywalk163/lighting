# 积木：去数字后反转（文本领域，自动生成）
# 契约：输入 [文本] → 输出 文本（去数字后反转）
导出 净数反
段落 净数反 接收 文本：
    返回 文本.替换('0', '').替换('1', '').替换('2', '').替换('3', '').替换('4', '').替换('5', '').替换('6', '').替换('7', '').替换('8', '').替换('9', '').反转()
