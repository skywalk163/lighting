# 积木：拼接文本（文本领域 L0）
# 契约：输入 列表, 分隔 → 输出 文本（用分隔符把列表元素连成一串）
导出 串接
段落 串接 接收 列表, 分隔：
    设 结果 为 ""
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 i 大于 0：
            设 结果 为 结果 加 分隔
        设 结果 为 结果 加 转字符串(列表[i])
        设 i 为 i 加 1
    返回 结果
