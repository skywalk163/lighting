# 积木：切分文本（文本领域 L0）
# 契约：输入 文本, 分隔 → 输出 列表（按分隔符切成若干段）
导出 切开
段落 切开 接收 文本, 分隔：
    设 结果 为 []
    设 总长 为 长度(文本)
    设 分长 为 长度(分隔)
    如果 分长 等于 0：
        结果.追加(文本)
        返回 结果
    设 缓冲 为 ""
    设 i 为 0
    当 i 小于 总长：
        设 匹 为 0
        如果 (i 加 分长) 小于等于 总长：
            设 匹 为 1
            设 j 为 0
            当 j 小于 分长：
                如果 文本[i 加 j] 不等于 分隔[j]：
                    设 匹 为 0
                设 j 为 j 加 1
        如果 匹 等于 1：
            结果.追加(缓冲)
            设 缓冲 为 ""
            设 i 为 i 加 分长
        否则：
            设 缓冲 为 缓冲 加 文本[i]
            设 i 为 i 加 1
    结果.追加(缓冲)
    返回 结果
