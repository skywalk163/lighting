# 积木：去字母（文本领域，自动生成）
# 契约：输入 [文本] → 输出 文本（文本操作：去字母）
导出 去字母
段落 去字母 接收 文本：
    返回 文本.替换('a', '').替换('b', '').替换('c', '').替换('d', '').替换('e', '').替换('f', '').替换('g', '').替换('h', '').替换('i', '').替换('j', '').替换('k', '').替换('l', '').替换('m', '').替换('n', '').替换('o', '').替换('p', '').替换('q', '').替换('r', '').替换('s', '').替换('t', '').替换('u', '').替换('v', '').替换('w', '').替换('x', '').替换('y', '').替换('z', '').替换('A', '').替换('B', '').替换('C', '').替换('D', '').替换('E', '').替换('F', '').替换('G', '').替换('H', '').替换('I', '').替换('J', '').替换('K', '').替换('L', '').替换('M', '').替换('N', '').替换('O', '').替换('P', '').替换('Q', '').替换('R', '').替换('S', '').替换('T', '').替换('U', '').替换('V', '').替换('W', '').替换('X', '').替换('Y', '').替换('Z', '')
