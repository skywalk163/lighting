# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：文本 · 名称：聚合_文本_文本_6项
# 描述：自动织成：对文本输入并行执行 去空格、转小写、转大写、反转文本、文本长度、提取数字，返回结果列表

# ── 积木：去空格（文本）──
段落 净字 接收 文本：
    返回 文本.去除空白()

# ── 积木：转小写（文本）──
段落 小化 接收 文本：
    返回 文本.转小写()

# ── 积木：转大写（文本）──
段落 大化 接收 文本：
    返回 文本.转大写()

# ── 积木：反转文本（文本）──
段落 颠倒 接收 文本：
    设 结果 为 ""
    设 i 为 长度(文本) 减 1
    当 i 大于等于 0：
        设 结果 为 结果 加 文本[i]
        设 i 为 i 减 1
    返回 结果

# ── 积木：文本长度（文本）──
段落 字长 接收 文本：
    返回 文本.长度()

# ── 积木：提取数字（文本）──
段落 抽数 接收 文本：
    设 结果 为 []
    设 缓冲 为 ""
    设 有点 为 0
    设 i 为 0
    当 i 小于 长度(文本)：
        设 字 为 文本[i]
        设 是数 为 0
        如果 字 大于等于 "0" 且 字 小于等于 "9"：
            设 是数 为 1
        如果 是数 等于 1：
            设 缓冲 为 缓冲 加 字
        否则如果 字 等于 "." 且 长度(缓冲) 大于 0 且 有点 等于 0：
            设 缓冲 为 缓冲 加 字
            设 有点 为 1
        否则：
            如果 长度(缓冲) 大于 0：
                如果 有点 等于 1：
                    结果.追加(转浮点(缓冲))
                否则：
                    结果.追加(转整数(缓冲))
                设 缓冲 为 ""
                设 有点 为 0
        设 i 为 i 加 1
    如果 长度(缓冲) 大于 0：
        如果 有点 等于 1：
            结果.追加(转浮点(缓冲))
        否则：
            结果.追加(转整数(缓冲))
    返回 结果

段落 聚合_文本_文本_6项 接收 文本：
    设 赵果1 为 净字(文本)。
    设 赵果2 为 小化(文本)。
    设 赵果3 为 大化(文本)。
    设 赵果4 为 颠倒(文本)。
    设 赵果5 为 字长(文本)。
    设 赵果6 为 抽数(文本)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4, 赵果5, 赵果6]。

导出 聚合_文本_文本_6项
