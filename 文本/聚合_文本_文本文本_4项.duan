# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：文本 · 名称：聚合_文本_文本文本_4项
# 描述：自动织成：对文本输入并行执行 包含、切分文本、编辑距离、文本相似，返回结果列表

# ── 积木：包含（文本）──
段落 含有 接收 文本, 子：
    设 总长 为 长度(文本)
    设 子长 为 长度(子)
    如果 子长 等于 0：
        返回 真
    如果 子长 大于 总长：
        返回 假
    设 上限 为 总长 减 子长
    设 i 为 0
    当 i 小于等于 上限：
        设 匹 为 1
        设 j 为 0
        当 j 小于 子长：
            如果 文本[i 加 j] 不等于 子[j]：
                设 匹 为 0
            设 j 为 j 加 1
        如果 匹 等于 1：
            返回 真
        设 i 为 i 加 1
    返回 假

# ── 积木：切分文本（文本）──
段落 切开 接收 文本, 分隔：
    设 结果 为 []
    设 总长 为 长度(文本)
    设 分长 为 长度(分隔)
    如果 分长 等于 0：
        结果.追加(文本)
        返回 结果
    设 缓冲 为 ""
    设 i 为 0
    当 i 小于 总长：
        设 匹 为 0
        如果 (i 加 分长) 小于等于 总长：
            设 匹 为 1
            设 j 为 0
            当 j 小于 分长：
                如果 文本[i 加 j] 不等于 分隔[j]：
                    设 匹 为 0
                设 j 为 j 加 1
        如果 匹 等于 1：
            结果.追加(缓冲)
            设 缓冲 为 ""
            设 i 为 i 加 分长
        否则：
            设 缓冲 为 缓冲 加 文本[i]
            设 i 为 i 加 1
    结果.追加(缓冲)
    返回 结果

# ── 积木：编辑距离（文本）──
段落 编辑距离 接收 甲文, 乙文：
    设 长甲 为 字符串长度(甲文)
    设 长乙 为 字符串长度(乙文)
    设 前行 为 []
    设 位次 为 0
    当 位次 小于 长乙 加 1：
        前行.追加(位次)
        设 位次 为 位次 加 1
    设 行号 为 0
    当 行号 小于 长甲：
        设 新行 为 [行号 加 1]
        设 列号 为 0
        当 列号 小于 长乙：
            设 删价 为 前行[列号 加 1] 加 1
            设 插价 为 新行[列号] 加 1
            设 换价 为 前行[列号]
            如果 字符串获取(甲文, 行号) 等于 字符串获取(乙文, 列号)：
                设 换价 为 换价
            否则：
                设 换价 为 换价 加 1
            设 优价 为 删价
            如果 插价 小于 优价：
                设 优价 为 插价
            如果 换价 小于 优价：
                设 优价 为 换价
            新行.追加(优价)
            设 列号 为 列号 加 1
        设 前行 为 新行
        设 行号 为 行号 加 1
    返回 前行[长乙]

# ── 积木：文本相似（文本）──
段落 距辅 接收 甲文, 乙文：
    设 长甲 为 字符串长度(甲文)
    设 长乙 为 字符串长度(乙文)
    设 前行 为 []
    设 位次 为 0
    当 位次 小于 长乙 加 1：
        前行.追加(位次)
        设 位次 为 位次 加 1
    设 行号 为 0
    当 行号 小于 长甲：
        设 新行 为 [行号 加 1]
        设 列号 为 0
        当 列号 小于 长乙：
            设 删价 为 前行[列号 加 1] 加 1
            设 插价 为 新行[列号] 加 1
            设 换价 为 前行[列号]
            如果 字符串获取(甲文, 行号) 等于 字符串获取(乙文, 列号)：
                设 换价 为 换价
            否则：
                设 换价 为 换价 加 1
            设 优价 为 删价
            如果 插价 小于 优价：
                设 优价 为 插价
            如果 换价 小于 优价：
                设 优价 为 换价
            新行.追加(优价)
            设 列号 为 列号 加 1
        设 前行 为 新行
        设 行号 为 行号 加 1
    返回 前行[长乙]

段落 文本相似 接收 左文, 右文：
    设 长左 为 字符串长度(左文)
    设 长右 为 字符串长度(右文)
    设 顶长 为 长左
    如果 长右 大于 顶长：
        设 顶长 为 长右
    如果 顶长 等于 0：
        返回 1.0
    设 距值 为 距辅(左文, 右文)
    返回 四舍五入(1 减 (距值 除 顶长), 4)

段落 聚合_文本_文本文本_4项 接收 文本, 子：
    设 赵果1 为 含有(文本, 子)。
    设 赵果2 为 切开(文本, 子)。
    设 赵果3 为 编辑距离(文本, 子)。
    设 赵果4 为 文本相似(文本, 子)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4]。

导出 聚合_文本_文本文本_4项
