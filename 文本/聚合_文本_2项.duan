# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：文本 · 名称：聚合_文本_2项
# 描述：自动织成：对文本输入并行执行 包含、切分文本，返回结果列表

# ── 积木：包含（文本）──
段落 含有 接收 文本, 子：
    设 总长 为 长度(文本)
    设 子长 为 长度(子)
    如果 子长 等于 0：
        返回 真
    如果 子长 大于 总长：
        返回 假
    设 上限 为 总长 减 子长
    设 i 为 0
    当 i 小于等于 上限：
        设 匹 为 1
        设 j 为 0
        当 j 小于 子长：
            如果 文本[i 加 j] 不等于 子[j]：
                设 匹 为 0
            设 j 为 j 加 1
        如果 匹 等于 1：
            返回 真
        设 i 为 i 加 1
    返回 假

# ── 积木：切分文本（文本）──
段落 切开 接收 文本, 分隔：
    设 结果 为 []
    设 总长 为 长度(文本)
    设 分长 为 长度(分隔)
    如果 分长 等于 0：
        结果.追加(文本)
        返回 结果
    设 缓冲 为 ""
    设 i 为 0
    当 i 小于 总长：
        设 匹 为 0
        如果 (i 加 分长) 小于等于 总长：
            设 匹 为 1
            设 j 为 0
            当 j 小于 分长：
                如果 文本[i 加 j] 不等于 分隔[j]：
                    设 匹 为 0
                设 j 为 j 加 1
        如果 匹 等于 1：
            结果.追加(缓冲)
            设 缓冲 为 ""
            设 i 为 i 加 分长
        否则：
            设 缓冲 为 缓冲 加 文本[i]
            设 i 为 i 加 1
    结果.追加(缓冲)
    返回 结果

段落 聚合_文本_2项 接收 文本：
    设 赵果1 为 含有(文本)。
    设 赵果2 为 切开(文本)。
    返回 [赵果1, 赵果2]。

导出 聚合_文本_2项
