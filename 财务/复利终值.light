# 积木：复利终值（财务领域 L0）
# 契约：输入 数, 数, 数 → 输出 数（本金按年利率复利若干期后的终值）
导出 复利终值
段落 复利终值 接收 本金, 年率, 期数：
    设 倍率 为 1 加 年率
    设 结值 为 本金
    设 位次 为 0
    当 位次 小于 整数(期数)：
        设 结值 为 结值 乘 倍率
        设 位次 为 位次 加 1
    返回 四舍五入(结值, 2)
