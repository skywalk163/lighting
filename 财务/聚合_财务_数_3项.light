# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：财务 · 名称：聚合_财务_数_3项
# 描述：自动织成：对数输入并行执行 个税、保留分、聚合_财务_数_2项，返回结果列表

# ── 积木：个税（财务）──
段落 税 接收 月收：
    设 应纳 为 月收 减 5000
    如果 应纳 小于等于 0：
        返回 0
    否则如果 应纳 小于等于 3000：
        返回 应纳 乘 0.03
    否则如果 应纳 小于等于 12000：
        返回 应纳 乘 0.1 减 210
    否则如果 应纳 小于等于 25000：
        返回 应纳 乘 0.2 减 1410
    否则如果 应纳 小于等于 35000：
        返回 应纳 乘 0.25 减 2660
    否则如果 应纳 小于等于 55000：
        返回 应纳 乘 0.3 减 4410
    否则如果 应纳 小于等于 80000：
        返回 应纳 乘 0.35 减 7160
    否则：
        返回 应纳 乘 0.45 减 15160

# ── 积木：保留分（财务）──
段落 留分 接收 数值：
    返回 四舍五入(数值, 2)

# ── 积木：聚合_财务_数_2项（财务）──
段落 税 接收 月收：
    设 应纳 为 月收 减 5000
    如果 应纳 小于等于 0：
        返回 0
    否则如果 应纳 小于等于 3000：
        返回 应纳 乘 0.03
    否则如果 应纳 小于等于 12000：
        返回 应纳 乘 0.1 减 210
    否则如果 应纳 小于等于 25000：
        返回 应纳 乘 0.2 减 1410
    否则如果 应纳 小于等于 35000：
        返回 应纳 乘 0.25 减 2660
    否则如果 应纳 小于等于 55000：
        返回 应纳 乘 0.3 减 4410
    否则如果 应纳 小于等于 80000：
        返回 应纳 乘 0.35 减 7160
    否则：
        返回 应纳 乘 0.45 减 15160

段落 留分 接收 数值：
    返回 四舍五入(数值, 2)

段落 聚合_财务_数_2项 接收 月收：
    设 赵果1 为 税(月收)。
    设 赵果2 为 留分(月收)。
    返回 [赵果1, 赵果2]。

段落 聚合_财务_数_3项 接收 月收：
    设 赵果1 为 税(月收)。
    设 赵果2 为 留分(月收)。
    设 赵果3 为 聚合_财务_数_2项(月收)。
    返回 [赵果1, 赵果2, 赵果3]。

导出 聚合_财务_数_3项
