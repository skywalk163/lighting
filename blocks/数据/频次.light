# 积木：频次统计（数据领域，自动生成）
# 契约：输入 [列表] → 输出 字典（统计各元素出现次数）
导出 频次
段落 频次 接收 列表：
    设 计数 为 {}
    设 i 为 0
    当 i 小于 长度(列表)：
        设 键 为 列表[i]
        如果 计数.包含(键)：
            计数[键] 为 计数[键] 加 1
        否则：
            计数[键] 为 1
        设 i 为 i 加 1
    返回 计数
