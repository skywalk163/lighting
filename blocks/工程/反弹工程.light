# 积木：各元素反弹（工程领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素反弹变换）
导出 反弹工程
段落 反弹工程 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 大于 1 则 2 减 列表[i] 否则 列表[i])
        设 i 为 i 加 1
    返回 结果
