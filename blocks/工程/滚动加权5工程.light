# 积木：滚动加权5（工程领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（5期加权移动）
导出 滚动加权5工程
段落 滚动加权5工程 接收 列表, 窗宽：
    设 结果 为 []
    设 i 为 0
    当 i 加 窗宽 小于 等于 长度(列表)：
        设 窗口 为 []
        设 j 为 0
        当 j 小于 窗宽：
            窗口.追加(列表[i 加 j])
            设 j 为 j 加 1
        结果.追加((窗口[0] 乘 1 加 窗口[1] 乘 2 加 窗口[2] 乘 3 加 窗口[3] 乘 4 加 窗口[4] 乘 5) 除 15)
        设 i 为 i 加 1
    返回 结果
