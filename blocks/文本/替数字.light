# 积木：替换数字（文本领域，自动生成）
# 契约：输入 [文本] → 输出 文本（将数字替换为#）
导出 替数字
段落 替数字 接收 文本：
    返回 文本.替换('0', '#').替换('1', '#').替换('2', '#').替换('3', '#').替换('4', '#').替换('5', '#').替换('6', '#').替换('7', '#').替换('8', '#').替换('9', '#')
