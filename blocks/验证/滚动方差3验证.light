# 积木：滚动方差3（验证领域，自动生成）
# 契约：输入 [列表, 数] → 输出 [数]（3期滚动方差）
导出 滚动方差3验证
段落 滚动方差3验证 接收 列表, 窗宽：
    设 结果 为 []
    设 i 为 0
    当 i 加 窗宽 小于 等于 长度(列表)：
        设 窗口 为 []
        设 j 为 0
        当 j 小于 窗宽：
            窗口.追加(列表[i 加 j])
            设 j 为 j 加 1
        结果.追加((窗口[0] 乘 窗口[0] 加 窗口[1] 乘 窗口[1] 加 窗口[2] 乘 窗口[2]) 除 3 减 均值 乘 均值)
        设 i 为 i 加 1
    返回 结果
