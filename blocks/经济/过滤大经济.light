# 积木：过滤大于（经济领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤大于阈值）
导出 过滤大经济
段落 过滤大经济 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 阈值：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
