# 积木：各元素取绝对值（体育领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素取绝对值）
导出 各绝对体育
段落 各绝对体育 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 大于 0 则 列表[i] 否则 (0 减 列表[i]))
        设 i 为 i 加 1
    返回 结果
