# 积木：成对加（法律领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对相加）
导出 成对加法律
段落 成对加法律 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(表甲[i] 甲 加 乙 表乙[i])
        设 i 为 i 加 1
    返回 结果
