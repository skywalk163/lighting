# 积木：成对平均值（数组领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对取平均值）
导出 对平均
段落 对平均 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(表甲[i] (甲 加 乙) 除 2 表乙[i])
        设 i 为 i 加 1
    返回 结果
