# 积木：各元素双曲正弦（格式领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素双曲正弦）
导出 双曲正弦格式
段落 双曲正弦格式 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加((列表[i].指数() 减 (0 减 列表[i]).指数()) 除 2)
        设 i 为 i 加 1
    返回 结果
