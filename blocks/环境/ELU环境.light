# 积木：各元素ELU（环境领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素ELU）
导出 ELU环境
段落 ELU环境 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 大于 0 则 列表[i] 否则 (列表[i].指数() 减 1))
        设 i 为 i 加 1
    返回 结果
