# 积木：过滤范围（几何领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（过滤在范围内）
导出 过滤范围几何
段落 过滤范围几何 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 甲 且 列表[i] 小于 乙：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
