# 积木：成对差值（生物领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对绝对差）
导出 成对差值生物
段落 成对差值生物 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(表甲[i] 如果 甲 大于 乙 则 甲 减 乙 否则 乙 减 甲 表乙[i])
        设 i 为 i 加 1
    返回 结果
