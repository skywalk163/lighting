# 积木：各元素截断到01（生物领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素截断到0-1）
导出 截01生物
段落 截01生物 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 0 则 0 否则 (如果 列表[i] 大于 1 则 1 否则 列表[i]))
        设 i 为 i 加 1
    返回 结果
