# 积木：各元素剪裁（工具领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素剪裁到区间）
导出 剪裁工具
段落 剪裁工具 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 小于 甲 则 甲 否则 (如果 列表[i] 大于 乙 则 乙 否则 列表[i]))
        设 i 为 i 加 1
    返回 结果
