# 积木：成对角度（工具领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对计算角度）
导出 成对角度工具
段落 成对角度工具 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(表甲[i] 反正切(乙 除 甲) 表乙[i])
        设 i 为 i 加 1
    返回 结果
