# 积木：各元素软阈值（医学领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（各元素软阈值）
导出 软阈值医学
段落 软阈值医学 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(如果 列表[i] 大于 阈值 则 列表[i] 减 阈值 否则 (如果 列表[i] 小于 (0 减 阈值) 则 列表[i] 加 阈值 否则 0))
        设 i 为 i 加 1
    返回 结果
