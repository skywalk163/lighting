# 积木：成对比值（随机领域，自动生成）
# 契约：输入 [数, 数] → 输出 [数]（成对计算比值）
导出 成对比随机
段落 成对比随机 接收 表甲, 表乙：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(表甲) 且 i 小于 长度(表乙)：
        结果.追加(表甲[i] 甲 除 (甲 加 乙) 表乙[i])
        设 i 为 i 加 1
    返回 结果
