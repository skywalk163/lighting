# 由 光明积木桥接 v0（选块 + 内联粘合）自动合成

# 需求：排序后取前3个

# ── 积木：排序（数据）──
段落 排序 接收 列表：
    设 长度 为 长度(列表)
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度：
        结果.追加(列表[i])
        设 i 为 i 加 1
    设 i 为 0
    当 i 小于 长度：
        设 j 为 i 加 1
        当 j 小于 长度：
            如果 结果[i] 大于 结果[j]：
                设 缓存 为 结果[i]
                设 结果[i] 为 结果[j]
                设 结果[j] 为 缓存
            设 j 为 j 加 1
        设 i 为 i 加 1
    返回 结果

# ── 积木：前k大元素（排序）──
段落 前k大元素 接收 列表：
    设 k 为 3
    设 排序后 为 排序(列表)
    设 结果 为 []
    设 起 为 长度(排序后) 减 k
    如果 起 小于 0：
        设 起 为 0
    设 i 为 起
    当 i 小于 长度(排序后)：
        结果.追加(排序后[i])
        设 i 为 i 加 1
    返回 结果

# ── 积木：前k小元素（排序）──
段落 前k小元素 接收 列表：
    设 k 为 3
    设 排序后 为 排序(列表)
    设 结果 为 []
    设 i 为 0
    当 i 小于 k 且 i 小于 长度(排序后)：
        结果.追加(排序后[i])
        设 i 为 i 加 1
    返回 结果

设 赵料 为 [5, 3, 8, 1, 9, 2, 7]。

设 赵果1 为 排序(赵料)。
设 赵果2 为 前k大元素(赵料)。
设 赵果3 为 前k小元素(赵料)。

打印(赵果1)。
打印(赵果2)。
打印(赵果3)。
