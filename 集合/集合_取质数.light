# 积木：集合_取质数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取质数）
导出 质数
段落 质数 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 等于 2 或 列表[i] 等于 3 或 列表[i] 等于 5 或 列表[i] 等于 7 或 列表[i] 等于 11：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
