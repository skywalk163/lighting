# 积木：集合_取素数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（集合高级操作：集合_取素数）
导出 素数
段落 素数 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 大于 1：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
