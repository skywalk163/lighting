# 积木：秒转天时分秒（时间领域，自动生成）
# 契约：输入 [数] → 输出 文本（秒数转为天时分秒）
导出 秒转天时分秒
段落 秒转天时分秒 接收 输入：
    返回 文本(整数(输入 除 86400)) 加 '天' 加 文本(整数((输入 模 86400) 除 3600)) 加 '时' 加 文本(整数((输入 模 3600) 除 60)) 加 '分' 加 文本(输入 模 60) 加 '秒'
