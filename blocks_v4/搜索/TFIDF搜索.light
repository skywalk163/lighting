# 积木：TFIDF搜索（搜索领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（TF-IDF搜索）
导出 TFIDF搜索
段落 TFIDF搜索 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        结果.追加(列表[i] 乘 对数(总 除 (i 加 1)))
        设 i 为 i 加 1
    返回 结果
