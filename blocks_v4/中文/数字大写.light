# 积木：数字大写（中文领域，自动生成）
# 契约：输入 [文本] → 输出 文本（数字转为大写）
导出 数字大写
段落 数字大写 接收 文本：
    返回 文本.替换('1', '壹').替换('2', '贰').替换('3', '叁').替换('4', '肆').替换('5', '伍').替换('6', '陆').替换('7', '柒').替换('8', '捌').替换('9', '玖').替换('0', '零')
