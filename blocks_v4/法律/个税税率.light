# 积木：个人所得税税率（法律领域，自动生成）
# 契约：输入 [数] → 输出 数（计算综合所得适用税率）
导出 个税税率
段落 个税税率 接收 输入：
    返回 如果 输入 小于 36000 则 0.03 否则 (如果 输入 小于 144000 则 0.1 否则 (如果 输入 小于 300000 则 0.2 否则 (如果 输入 小于 420000 则 0.25 否则 (如果 输入 小于 660000 则 0.3 否则 (如果 输入 小于 960000 则 0.35 否则 0.45)))))
