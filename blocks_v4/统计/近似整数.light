# 积木：计数近似整数（统计领域，自动生成）
# 契约：输入 [列表] → 输出 数（计数近似整数的个数）
导出 近似整数
段落 近似整数 接收 列表：
    设 结果 为 0
    设 i 为 0
    当 i 小于 长度(列表)：
        设 结果 为 如果 列表[i] 减 整数(列表[i]) 小于 0.001 则 结果 加 1 否则 结果
        设 i 为 i 加 1
    返回 结果
