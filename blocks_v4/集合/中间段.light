# 积木：取中间段（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（取中间段元素）
导出 中间段
段落 中间段 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 i 大于 等于 甲 且 i 小于 乙：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
