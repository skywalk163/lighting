# 积木：取3的倍数（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出3的倍数）
导出 3倍数
段落 3倍数 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 模 3 等于 0：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
