# 积木：取小于均值（集合领域，自动生成）
# 契约：输入 [列表] → 输出 [列表]（筛选出小于均值的元素）
导出 小于均值
段落 小于均值 接收 列表：
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(列表)：
        如果 列表[i] 小于 均值：
            结果.追加(列表[i])
        设 i 为 i 加 1
    返回 结果
