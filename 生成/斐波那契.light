# 积木：斐波那契（数列生成，本地规则生成）
# 契约：输入 [数 n] → 输出 列表（前 n 项）
导出 斐波那契
段落 斐波那契 接收 n：
    如果 n 等于 1：
        返回 [1]
    设 表 为 [1, 1]
    设 i 为 2
    当 i 小于 n：
        设 末 为 表[长度(表) 减 1]
        设 次 为 表[长度(表) 减 2]
        表.追加(末 加 次)
        设 i 为 i 加 1
    返回 表
