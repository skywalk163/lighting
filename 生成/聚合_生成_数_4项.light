# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：生成 · 名称：聚合_生成_数_4项
# 描述：自动织成：对数输入并行执行 斐波那契、阶乘、素数、聚合_生成_数_3项，返回结果列表

# ── 积木：斐波那契（生成）──
段落 斐波那契 接收 n：
    如果 n 等于 1：
        返回 [1]
    设 表 为 [1, 1]
    设 i 为 2
    当 i 小于 n：
        设 末 为 表[长度(表) 减 1]
        设 次 为 表[长度(表) 减 2]
        表.追加(末 加 次)
        设 i 为 i 加 1
    返回 表

# ── 积木：阶乘（生成）──
段落 阶乘 接收 n：
    设 结果 为 1
    设 i 为 1
    设 上限 为 n 加 1
    当 i 小于 上限：
        设 结果 为 结果 乘 i
        设 i 为 i 加 1
    返回 结果

# ── 积木：素数（生成）──
段落 素数 接收 n：
    如果 n 小于 2：
        返回 0
    设 i 为 2
    当 i 小于 n：
        如果 n 模 i 等于 0：
            返回 0
        设 i 为 i 加 1
    返回 1

# ── 积木：聚合_生成_数_3项（生成）──
段落 斐波那契 接收 n：
    如果 n 等于 1：
        返回 [1]
    设 表 为 [1, 1]
    设 i 为 2
    当 i 小于 n：
        设 末 为 表[长度(表) 减 1]
        设 次 为 表[长度(表) 减 2]
        表.追加(末 加 次)
        设 i 为 i 加 1
    返回 表

段落 阶乘 接收 n：
    设 结果 为 1
    设 i 为 1
    设 上限 为 n 加 1
    当 i 小于 上限：
        设 结果 为 结果 乘 i
        设 i 为 i 加 1
    返回 结果

段落 素数 接收 n：
    如果 n 小于 2：
        返回 0
    设 i 为 2
    当 i 小于 n：
        如果 n 模 i 等于 0：
            返回 0
        设 i 为 i 加 1
    返回 1

段落 聚合_生成_数_3项 接收 n：
    设 赵果1 为 斐波那契(n)。
    设 赵果2 为 阶乘(n)。
    设 赵果3 为 素数(n)。
    返回 [赵果1, 赵果2, 赵果3]。

段落 聚合_生成_数_4项 接收 n：
    设 赵果1 为 斐波那契(n)。
    设 赵果2 为 阶乘(n)。
    设 赵果3 为 素数(n)。
    设 赵果4 为 聚合_生成_数_3项(n)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4]。

导出 聚合_生成_数_4项
