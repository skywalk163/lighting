# 积木：按句拆分
导出 按句拆分
段落 按句拆分 接收 文本：
    设 结果 为 []
    设 当前 为 ""
    设 i 为 0
    当 i 小于 文本.长度()：
        设 字符 为 文本[i]
        设 当前 为 当前 加 字符
        如果 字符 等于 "。" 或 字符 等于 "！" 或 字符 等于 "？"：
            结果.追加(当前)
            设 当前 为 ""
        设 i 为 i 加 1
    如果 当前 不等于 ""：
        结果.追加(当前)
    返回 结果
