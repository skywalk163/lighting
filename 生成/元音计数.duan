# 积木：元音计数
导出 元音计数
段落 元音计数 接收 文本：
    设 小写文本 为 文本.转小写()
    设 计数 为 0
    设 i 为 0
    当 i 小于 小写文本.长度()：
        设 字符 为 小写文本[i]
        如果 字符 等于 "a"：
            设 计数 为 计数 加 1
        否则如果 字符 等于 "e"：
            设 计数 为 计数 加 1
        否则如果 字符 等于 "i"：
            设 计数 为 计数 加 1
        否则如果 字符 等于 "o"：
            设 计数 为 计数 加 1
        否则如果 字符 等于 "u"：
            设 计数 为 计数 加 1
        设 i 为 i 加 1
    返回 计数
