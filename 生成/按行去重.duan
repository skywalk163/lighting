# 积木：按行去重
导出 按行去重
段落 按行去重 接收 文本：
    设 行表 为 文本.分割("\n")
    设 结果 为 []
    设 i 为 0
    当 i 小于 长度(行表)：
        设 行 为 行表[i]
        如果 结果.包含(行) 等于 假：
            结果.追加(行)
        设 i 为 i 加 1
    返回 结果.连接("\n")
