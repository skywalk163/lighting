# 积木：统计字母出现次数
导出 统计字母出现次数
段落 统计字母出现次数 接收 文本：
    设 字符表 为 []
    设 i 为 0
    当 i 小于 长度(文本)：
        如果 文本[i] 不等于 " "：
            字符表.追加(文本[i])
        设 i 为 i 加 1
    字符表.排序()
    设 结果 为 []
    设 当前字符 为 ""
    设 计数 为 0
    设 j 为 0
    当 j 小于 长度(字符表)：
        如果 当前字符 等于 ""：
            设 当前字符 为 字符表[j]
            设 计数 为 1
        否则如果 当前字符 等于 字符表[j]：
            设 计数 为 计数 加 1
        否则：
            设 条目 为 []
            条目.追加(当前字符)
            条目.追加(计数)
            结果.追加(条目)
            设 当前字符 为 字符表[j]
            设 计数 为 1
        设 j 为 j 加 1
    如果 当前字符 不等于 ""：
        设 条目 为 []
        条目.追加(当前字符)
        条目.追加(计数)
        结果.追加(条目)
    返回 结果