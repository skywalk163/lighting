# 积木：Markdown标题转HTML
导出 Markdown标题转HTML
段落 Markdown标题转HTML 接收 标记文本：
    设 层级 为 0
    设 i 为 0
    当 i 小于 长度(标记文本) 且 标记文本[i] 等于 "#"：
        设 层级 为 层级 加 1
        设 i 为 i 加 1
    如果 层级 等于 0：
        返回 标记文本
    设 内容 为 ""
    当 i 小于 长度(标记文本) 且 标记文本[i] 等于 " "：
        设 i 为 i 加 1
    当 i 小于 长度(标记文本)：
        设 内容 为 内容 加 标记文本[i]
        设 i 为 i 加 1
    设 开标签 为 "<h" 加 层级 加 ">"
    设 闭标签 为 "</h" 加 层级 加 ">"
    返回 开标签 加 内容 加 闭标签
