# 积木：情感判断
导出 情感判断
段落 情感判断 接收 文本：
    设 i 为 0
    设 结果 为 "正面"
    当 i 小于 长度(文本)：
        如果 文本[i] 等于 "差"：
            设 结果 为 "负面"
        如果 文本[i] 等于 "悔"：
            设 结果 为 "负面"
        设 i 为 i 加 1
    返回 结果