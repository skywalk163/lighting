# 积木：中位数
导出 中位数
段落 中位数 接收 序列：
    设 排序表 为 []
    设 i 为 0
    当 i 小于 长度(序列)：
        排序表.追加(序列[i])
        设 i 为 i 加 1
    排序表.排序()
    设 计数 为 长度(排序表)
    设 中 为 整数(计数 除 2)
    如果 计数 模 2 等于 0：
        返回 ((排序表[中 减 1] 加 排序表[中]) 除 2)
    否则：
        返回 排序表[中]
