# 积木：方差（数据领域，本地规则生成）
# 契约：输入 [列表] → 输出 数（方差）
导出 方差
段落 方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 计数)
