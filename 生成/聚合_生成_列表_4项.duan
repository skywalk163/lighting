# 由 段言积木层级生成 v0.13 自动合成（成语式宏展开）
# 层级：1 · 领域：生成 · 名称：聚合_生成_列表_4项
# 描述：自动织成：对列表[数]输入并行执行 方差、中位数、样本方差、聚合_生成_列表_3项，返回结果列表

# ── 积木：方差（生成）──
段落 方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 计数)

# ── 积木：中位数（生成）──
段落 中位数 接收 序列：
    设 排序表 为 []
    设 i 为 0
    当 i 小于 长度(序列)：
        排序表.追加(序列[i])
        设 i 为 i 加 1
    排序表.排序()
    设 计数 为 长度(排序表)
    设 中 为 整数(计数 除 2)
    如果 计数 模 2 等于 0：
        返回 ((排序表[中 减 1] 加 排序表[中]) 除 2)
    否则：
        返回 排序表[中]

# ── 积木：样本方差（生成）──
段落 样本方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 (计数 减 1))

# ── 积木：聚合_生成_列表_3项（生成）──
段落 方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 计数)

段落 中位数 接收 序列：
    设 排序表 为 []
    设 i 为 0
    当 i 小于 长度(序列)：
        排序表.追加(序列[i])
        设 i 为 i 加 1
    排序表.排序()
    设 计数 为 长度(排序表)
    设 中 为 整数(计数 除 2)
    如果 计数 模 2 等于 0：
        返回 ((排序表[中 减 1] 加 排序表[中]) 除 2)
    否则：
        返回 排序表[中]

段落 样本方差 接收 序列：
    设 计数 为 长度(序列)
    设 和 为 0
    设 i 为 0
    当 i 小于 计数：
        设 和 为 和 加 序列[i]
        设 i 为 i 加 1
    设 均值 为 和 除 计数
    设 平方和 为 0
    设 j 为 0
    当 j 小于 计数：
        设 差 为 序列[j] 减 均值
        设 平方和 为 平方和 加 (差 乘 差)
        设 j 为 j 加 1
    返回 (平方和 除 (计数 减 1))

段落 聚合_生成_列表_3项 接收 序列：
    设 赵果1 为 方差(序列)。
    设 赵果2 为 中位数(序列)。
    设 赵果3 为 样本方差(序列)。
    返回 [赵果1, 赵果2, 赵果3]。

段落 聚合_生成_列表_4项 接收 序列：
    设 赵果1 为 方差(序列)。
    设 赵果2 为 中位数(序列)。
    设 赵果3 为 样本方差(序列)。
    设 赵果4 为 聚合_生成_列表_3项(序列)。
    返回 [赵果1, 赵果2, 赵果3, 赵果4]。

导出 聚合_生成_列表_4项
